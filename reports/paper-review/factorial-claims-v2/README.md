# Integrated continuation-claim calibration

Status: **integrated in the isolated checkout, not deployed or source-published**. This milestone
corrects the manuscript and its automatic interpretation, not a training run or numerical result.
The original synthetic counterexamples, candidate and validation remain unchanged under `../factorial-claims-v1/`.
The dated [integrated validation receipt](validation.json) binds the current sources, archived
predecessors, tests, layout checks and read-only controller observations. It is not a release receipt.

## What changed

- `paper/main.tex` and `src/embed_optim/state_operator_factorial_publication.py` now use the
  tested, narrower endpoint interpretation. State, operator and interaction are final 50K
  continuation contrasts. They do not decompose the primary-training gain; positive interaction
  alone does not establish beneficial continuation or full-trajectory co-adaptation.
- All point estimates, intervals, decision labels, thresholds and branch selection are unchanged.
  Only the three reviewed publication-text functions differ from the archived renderer. The
  scientific and implementation protocol files, including their historical wording, are preserved
  byte-for-byte. A separate publication policy explicitly supersedes only the overstrong prose.
- The conceptual figure now separates weight/representation measurements, held-out retrieval
  prediction and conditional continuation. Its vector layout was visually reviewed; an actual
  rendering test checks that every non-empty box label remains inside its box. The PDF has no
  creation-time stamp. This is a schematic, not a plot of model outcomes.
- `scripts/render_state_operator_pending.py` regenerates only a development placeholder with no
  invented scores. It refuses a non-development include, a symlink escape, or any existing
  scientific summary/publication. It is not a new pipeline command.

## Exact fourth preparation layer

`configs/dense_no_packing_state_operator_claim_wording_amendment.json` binds the reviewed sources,
unchanged numerical dependencies and original counterexamples. The publication protocol refreshes
only its declared source identities and interpretation policy. The completion protocol changes
only its publication binding, the new amendment binding and timestamp.

`before/` preserves ten predecessor files, including both protocols, manuscript, renderer,
figure generator, development include, auditor, two tests and paper guide. The first three
preparation layers are still fully reconstructed. Their factorial-publication lookup is redirected
only to this one declared, exact predecessor file; its original byte count and SHA remain required.
No general source override, weaker main gate or command change is accepted.

[The actual projection](successor-projection.json) checks all five consumer loaders, the same
eleven primary/dimension steps, all 36 original factorial steps, all arguments and the four exact
checkout-local path relocations. Current prepared identities are:

| Item | Identity |
| --- | --- |
| Required complete main | `4380a3631ec196307c775cdf4ca766298075de0312ad9d7158c2795577424a56` |
| Isolated successor | `41fdf48c1275dbbb1972cbd2e19aa8b41a5458b0ae55377731f95b8c7792d749` |
| Host projection | `7aafd7af0c010984cbcf30c07449e68d4d21e7df23e35f499b023d88fc9e71fd` |

The earlier `ca0c7f5b...` / `6749baa1...` projections are the unchanged three-layer history, not
the current prepared contract. Live main/factorial still run `4152531e...` / `6605090d...`, with
zero post-training steps. No real lease, migration or controller takeover occurred.

## Verification and retained diagnostic attempts

- `full-tests-final.xml`: **1,202 tests pass**, zero failures, errors, skips or exclusions.
- `figure-wording-tests.xml`: **41 pass**, including all 27 decision combinations, no altered
  estimates/labels, the unchanged abstract word reserve, protective placeholder checks, actual
  figure bounds and a complete explicitly synthetic paper/layout build.
- `documentation-contract-tests.xml` and `documentation-contract-tests-final.xml`: **160 pass**
  in each documentation/handoff check; the latter follows the 05:30 progress refresh.
- `synthetic-layout-audit.json`: the explicitly labelled, complete synthetic fixture ends main
  text on page 6. Its values are test inputs, never experiment results.
- `draft-layout-audit.json`: the real pending draft ends main text on page 6; the conservative
  expanded abstract bound is 178/200 words. The compiled PDF contains neither prohibited
  implementation-narrative terms nor Type 3 fonts, and the log has no overfull horizontal boxes.
- `strict-paper-audit.json`: **expected exit 1**, because primary/dimension evidence and all three
  scientific includes remain pending. A six-page draft is not a submission-ready result.
- `focused-tests.xml` retains four initial failures: three old mutation tests expected a later
  error than the new immutable-predecessor gate, and one still expected the superseded prose.
  The repaired tests check both the new earlier rejection and the original semantic reconstruction;
  no production gate was weakened. `focused-tests-v2.xml` passes all 95 then-existing checks.
- `full-tests.xml` is the earlier 1,201-test pass before final figure line wrapping and the new
  bounds regression. It is not relabelled as the final-source run. The final full receipt is separate.

Read-only contract validation, from the isolated checkout:

```bash
CUDA_VISIBLE_DEVICES='' HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 \
PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=2 OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 \
PYTHONPATH=/root/embedding-optimizer-story-refactor/src \
python -B -m scripts.audit_successor_contract --repository /root/embedding-optimizer-story-refactor
```

This prints a plan without acquiring a lease, writing a runtime ledger or executing any step.
Recompute on the actual host after an approved deployment; do not substitute the projected hash
for that check. Earlier source-bound receipts remain dated records: compare their prior source
identities with the byte-identical archive, not the now-amended canonical manuscript.

## Remaining science and authority

At 05:30 UTC the matrix is still 10/12 complete, with 52 remotely covered checkpoints. Both exact
NorMuon handles are running; the artifact steps are 1413 and 1303. The earlier 05:08 snapshots
remain unchanged; `progress-snapshot-final.json` and `wandb-observation-final.json` hold this refresh.
No complete primary 840-unit
BEIR grid, full weight-space/retrieval bridge, functional-dimension result or factorial result exists.
The publication correction does not invent any of these findings.

The separate narrow post-processing handoff, WIP source-publication exception and rejected exact-
scope HF withdrawal remain unanswered. An automatic goal continuation grants none of them. Do not
merge this branch into live main, start a competing controller, delete any checkpoint or inspect or
intervene on `gpu.py`. The real numerical training/evaluation sources remain unchanged.
