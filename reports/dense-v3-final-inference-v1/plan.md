# Complete endpoint inference from existing genuine outputs

Declared 2026-09-10 17:52 UTC, after all twelve final scores and the validation-only
choices were observed. This is not a new preregistration. It executes the existing
September 3 final-checkpoint statistical definitions, unchanged in the v3 protocol.

Use every one of the 12 x 14 final task cells at step 3907. Compute both frozen
families: equal four-rate averages within optimizer (primary estimand), and the
three loss-only validation-selected recipes (secondary estimand). Preserve all
three contrasts and 50,000 common paired-task bootstrap draws, seed 20260903,
fixed observed standard errors, linear quantiles and simultaneous max-T bounds.
No outcome-based rate, feature, task, checkpoint or bootstrap choice is introduced.

Read the complete, anonymously recovered 660-file snapshot, authenticate it and
reconstruct native task scores/selector before inference. Invoke byte-authenticated
original pure functions via their unchanged AST nodes, not a modified whole-grid
reader. Compare real-number outputs to the already existing independent scalar
algorithm. Save all task effects, all six intervals, raw-scale values and a clear
two-family figure. Neither family pools the four learning rates as training seeds.

The original whole-840-cell outcome consumer remains unchanged and unpassed. This
endpoint-only calculation is not complete outcome/trajectory admission, a scientific
release, query-level inference, multiseed robustness or a mechanism result. It must
not install manuscript findings or remove pending markers. Later complete outcome
production must reproduce these fixed endpoint tables while also verifying every
required intermediate cell; no missing cell is imputed by this work.

Keep the original training, geometry, evaluation and functional sources/queues
unchanged. No GPU/lease/historical-controller operation, external write, source
publication or destructive action is needed. Retain every actual failure/source
version if encountered. The whole NAACL/reproducibility objective stays active.
