# Reviewed transition proposal — not execution authorization

The prepared numerical source now passes bounded reference, gradient, checkpoint and entrypoint
checks. The next useful work is the source/protocol and runtime transition, not another search for
the already localized old replay discrepancy. Formal deployment remains unapproved and unperformed.

## Scientific invariants

Keep the exact DenseOn base revision and 500k-query/seven-negative data manifest. Start all revised
primary runs from that base, **not** from an old fine-tuned checkpoint. Retain the 12 declared
optimizer/rate recipes, one epoch, seed 42, global batch 128, temperature .02, context 8192, clipping,
scheduler and checkpoints 782/1563/2345/3126/3907. Do not interpret four rates as four independent
training seeds. A stability failure at a declared rate remains a result; it is not permission to
discard the run or tune the rate on BEIR.

Retain the frozen 14-task exact full-corpus BEIR task revisions, nDCG@10, 840 task-checkpoint units,
the 4096-row validation-only recipe selector, paired task-bootstrap/max-T procedure and the
primary all-rate versus secondary validation-selected distinctions. Old results remain ineligible
as replacements for missing revised results. Historical outcomes have already been seen: a new
execution lock is prospective for the revision, not a claim that the original study was preregistered.

The scientific spine remains weight trajectories → functional dimension utility → retrieval.
Operator-built-in spectral flattening and engineering fixes are not contributions. The fixed
source-state/operator crossed continuation is a bounded mechanism probe, not a decomposition of
the primary gain. No new mechanistic conclusion follows from the diagnostics in this archive.

## Required source and protocol review

1. Review the exact sealed candidate and its acceptance receipts. Checkpoints must bind numerical
   policy plus the full planned run/data/model/scheduler/source identity; a numerical receipt alone
   is not a complete resume authorization. The new source has not yet repeated the separate-process
   controlled continuation, nor a full natural-data horizon; those boundaries remain explicit.
2. Create a **new** primary execution/evaluation/durability contract for the candidate source and
   namespace. Preserve every old lock and ledger. Update source-bound outcome, geometry, bridge,
   dimension and publication consumers against the new primary identities, without changing their
   scientific definitions or weakening rejection of an old/incomplete input.
3. Review the crossed continuation separately: its routed AdamW control and two-rate operator
   semantics are not currently accepted by the new primary-only policy. It must inherit the new
   single-normalization/restore contract through an explicitly tested implementation, not by
   bypassing the primary guard or merely replacing the old trainer hash.
4. Test the transition and all downstream result gates. The present eleven source-contract failures
   are not waived. A future whole-repository pass must include negative tests for old run IDs,
   legacy checkpoints, mismatched numerical contracts, partial outputs and changed provenance.

## Evidence-preserving runtime handoff

Before any deployment, recheck the exact original paused handles and ledger digest. Preserve the
four-step completed prefix, active-BEIR state, logs, backup receipts and lease lineage. The earlier
zero-step migration is inapplicable. A reviewed explicit retirement/transition must not execute the
old controller's unsafe fresh-ledger resume path or accidentally dispatch another old evaluator.

The new launch must use a committed reviewed source identity and new output, log, W&B, HF and
evaluation-cache namespaces. No source replacement in a running checkout, duplicate controller,
checkpoint overwrite or protected GPU-helper interaction. Recheck startup/data-order identity,
all-rank state saving and per-checkpoint remote digests before relying on unattended training.

The intended order after authorization is: common natural-data execution preflight → 12 primary
recipes in two disjoint four-GPU pools → sealed checkpoint backup and accepted primary audits →
complete BEIR/validation and weight/dimension analyses → bounded crossed continuation → factual
paper rendering and portable release checks. Evaluation may overlap the final training run only
under a reviewed disjoint-resource handoff and only on accepted revised checkpoints.

## Publication and authority gates

The repository currently prohibits commits before the manuscript has no pending result macros.
That rule prevents publishing an intermediate correction before collecting its new results;
development-stage source publication requires explicit owner direction, not a self-issued waiver.
Formal deployment/retraining and the live ledger transition also remain reviewed actions.

GitHub issue update/comment attempts previously returned integration 403. Do not retry with
alternate credentials or identities. Restore the integration's write permission through its normal
owner-controlled path before expecting the remote progress issue to contain this milestone.
HF historical deletion remains safety-rejected; no retry or decomposition of that action is part
of this transition proposal.
