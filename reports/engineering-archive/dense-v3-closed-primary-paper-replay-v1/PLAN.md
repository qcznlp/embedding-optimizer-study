# Current complete-paper numerical portability

Owner authorization: continue the complete Dense-only NAACL/reproducible-artifact goal.
The previous turn completed the real 638-file continuation-probe backup and replay.
This is a new integrated reconstruction check, not a retry of those completed jobs.

The first executable phase closes the actual current primary result graph:

1. Authenticate the already completed genuine native assembly and existing document
   authorization; preserve old failures and unknown coordinator exits exactly.
2. Copy the actual numerical inputs, original package modules and unchanged formatters
   into a separate closed directory. A saved recipe catalog is metadata only, never
   a replacement PrimaryV3Contract or successful checkpoint-admission callback.
3. In a fresh CPU process, refuse old producer-file reads and network connections;
   rerun all ten outcome tables, original/exact bridge families, complete functional
   inference, all 108 exploratory controls, and both 60-state empirical figures.
4. Require exact original output bytes and decisions. Do not change any numerical
   source, tolerance, rate/stage/task population, undefined value or old guard.

This phase does not repeat model encoding, SVD, coordinate ablation or GPU training.
The preserved native measurement/admission chain remains upstream. The Python I/O
refusal is not represented as OS isolation or a physical second-host experiment.

Complete-paper portability still additionally requires actual factorial inference,
all original complete scientific lineage, reconstruction of the four generated
includes and strict fresh PDF compilation/inspection. Current actual factorial and
document waiters remain unchanged; this work does not substitute a synthetic result,
install an authoritative paper, publish WIP source, or claim the overall goal complete.
