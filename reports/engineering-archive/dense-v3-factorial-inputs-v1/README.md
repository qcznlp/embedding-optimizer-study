# Genuine factorial input binding

The CPU Trainer component is already accepted in its explicitly bounded scope;
do not repeat its completed diagnostics. This next step connects the unchanged
prospective branch sample selection and calibration sample selection to the
actual corrected primary data and fixed 60% source checkpoints. It does not
launch a GPU task, replace a source protocol, admit a formal branch, change a
scientific choice, or reuse historical gradients.

First authenticate the fixed 50K branch and revised 500K input identities. Check
every selected query/positive/seven-negative group and metadata field, not only
sample IDs. Reconstruct the original selection independently. Reconstruct all
32 fixed calibration identities in their original eight-by-four order and compare
every selected field to the corrected parent. Preserve any mismatch and stop;
do not choose replacement examples or re-freeze old hashes to obtain a pass.

Initial execution root: `/tmp/dense-v3-factorial-input-audit.LNUormRA`.
Data evidence is separate from
source-checkpoint readiness, numerical calibration and full-run admission.

## Actual results — September 10, 2026

Both new audit processes are terminal/exit zero. The complete data comparison
checks the full 500K parent ledger and all **21 fields in all 50,000 branch rows**.
The original proportional seeded selection reconstructs exactly. None of the six
revised primary positions occurs in this branch; every query, positive, seven
negatives and metadata value matches the revised parent. No re-sampling, text
change or data replacement is needed. `data-first.json` has SHA
`94ea1b27631cf9270f445e6406e79c0c470bd346564a4057cee1f742923d103f`.

The same audit reconstructs all **32** calibration identities and their original
eight groups of four. Their exact order, selection digest, source counts and all
21 selected fields match the unchanged common-state specification and revised
primary parent. None of the revised positions occurs in this selection. This
does **not** reuse the old gradients or establish the new calibrated rates.

The two original source choices are retained: corrected **AdamW 3e-5** and
**Muon 3e-4**, both at update **2345**. Fresh native checkpoint inspection agrees
with the accepted complete-primary provenance. Current local payloads also agree
with the original immutable remote inventories and audit receipts; this is not a
new network audit. The immutable source commits are in `states-first.json`.

Both real 149,014,272-parameter models load on CPU with all **134 tensors bitwise
equal** to their saved source weights before and after optimizer construction.
The observed context is 8192 and the saved deterministic attention flags are
retained. Each state supports both default-topology reset optimizers: identical
**88 hidden / 1 auxiliary-decay / 45 auxiliary-no-decay** named groups, zero
optimizer steps and no inherited gradient or moment state, including actual
Accelerate empty-state placement. `states-first.json` has SHA
`9958ecc4f23d9165838632c6bd7e24f1e64e3753eb5632206f2228411179e932`.

The constructor rate 3e-4 is explicitly diagnostic, not a calibrated branch rate.
CPU loading uses SDPA only to inspect saved weights; no forward/backward pass or
GPU numerical-equivalence claim follows. Actual calibration must still follow
the frozen BF16/FlashAttention/gradient-history protocol on the corrected states.

All **24** focused comparator tests pass, including changing every one of the
21 fields, dropping a field or row, and preserving nonascending calibration order.
These are bounded engineering checks, not a whole-repository green claim.

## Next work

Implement the v3 outer calibration and branch-input consumer without changing
the declared sample choices, two source choices, target scale, seeds, 50K horizon
or endpoint contrasts. Real GPU calibration, four-GPU default-topology Trainer
admission, the twelve formal branches and their complete outcomes remain required.
Primary evaluation and the validation/functional queues continue unchanged; the
GPU-priority request is still unanswered. No live source, kernel, controller,
manuscript, HF object or old protocol was changed by these input audits.
