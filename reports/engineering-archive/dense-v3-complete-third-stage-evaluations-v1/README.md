# Complete third-stage checkpoint evaluations

All twelve declared step-2345 configurations now have complete fourteen-task
retrieval outcomes. Accepted checkpoint coverage is **48 / 60**: all twelve
configurations at steps 782, 1563, 2345 and 3907. The twelve step-3126 outcomes
remain required. This is a descriptive outcome milestone, not a complete
five-stage trajectory, new significance test or mechanism finding.

The [third-stage table](tables/third-stage-checkpoint-scores.md) retains all
twelve rates and all 168 task scores. Only **six original pool-A checkpoints /
84 task values** are new. The six pool-B rows overlap the
[previous 42-state archive](../dense-v3-third-stage-pool-b-evaluations-v1/README.md);
they are not new runs. All previous 42 parsed checkpoint records remain exactly
unchanged. No rate was selected or discarded using these scores.

## Actual reconstruction

- The unchanged native reader, SHA-256
  `765f00465728ad6e18d299f2e064070d6cc525775a5bf1c7e71273c9497ca5b2`,
  completed at **2026-09-12 00:20:37 UTC**. Actual CPU session **78814**
  exited zero. It verifies both 56-file original source assemblies, source/data/
  training/checkpoint bindings, complete native task results and original worker
  records. No GPU/model evaluation was repeated.
- Independent reconstruction completed at **00:21:17 UTC**, actual chunk
  **3145d7**, exit zero. It checks **672 raw task scores, 672 original exit-zero
  workers and 768 score/metadata snapshots**, and reconstructs every exact
  rational mean. The 9,978,146-byte native bundle has SHA-256
  `cc7419b025af23952e1e0ef813849677d71c1d1756607dbdd4e3c262552a1179`.
- The new independent reader changes the preceding complete-stage reader only
  in its cohort, prior binding, counts and output labels. Static comparison
  verifies its six helper ASTs and score/worker/rational-mean loop unchanged
  after the stage-variable rename. This static check is not model acceptance.
- A fresh process running the archived source completed at **00:24:13 UTC**,
  actual chunk **42d6e9**, exit zero. All three CSV/Markdown files reproduce
  byte-for-byte. This is same-host source relocation, not a second-host model run.

See [commands.json](commands.json), [the native bundle](actual/complete-checkpoint-readback.json),
[the independent result](actual/independent-raw-reconstruction.json) and
[the archived-source replay](actual/archived-source-replay.json).
The original Torch import deprecation warning is retained. The source diff's
exit one denotes text differences, not an execution failure.

## State and boundaries

The [dated observations](observations.json) end at **00:24 UTC** with
**672 / 840** tasks and eight exact live original workers, now evaluating
step 3126. [Native exits](actual/native-primary-exits.json) retain 46 distinct
successful task workers observed during this goal turn. An intermediate zombie
snapshot is reconciled against the original zero exit and automatic next task;
no process was signalled or restarted.

Functional progress remains **one accepted pretrained vector state / zero feature
states**. The failed functional attempt, its numerical definitions and all
accepted vectors remain unchanged. No recovery authority, coordinator or GPU job
was created; the scoped owner confirmation is still pending. No crossed branch,
manuscript finding, statistical rule or external repository was changed.

The six new pool-A outcomes are **locally archived only at this milestone**.
They have not yet been added to the prior immutable HF evaluation backups.
The earlier six-state pool-B backup remains valid and unchanged. Portable
data backup, remaining evaluations, functional/crossed analysis and complete
paper/source release remain separate work.

The [prior handoff](before/CURRENT_EXPERIMENT.md) is preserved; the new
[verification](verification.json) records archive identities and protected-source
checks. This engineering record belongs outside the manuscript.
