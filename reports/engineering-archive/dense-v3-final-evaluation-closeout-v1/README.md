# Complete continuation evaluation: native pipeline closeout

At **2026-09-13 06:40:15 UTC**, the original exact observer `1088d5`
records **168/168** accepted task workers, all exit zero, no active evaluation
worker and no failed task or coordinator receipt. Every one of twelve
continuation endpoints has all fourteen original full-corpus task results.

Both original coordinator sessions have actual tool exit zero:

- Pool a, session 41515: terminal `d4b9d9`; native completion 06:39:13 UTC.
- Pool b, session 37455: terminal `6a4b9d`; native completion 06:40:07 UTC.

The observer's absent-handle exit field is null; actual OS/tool exits above
come from the original sessions, not inferred from absence or result files.
Do not poll or restart these completed evaluation sessions. The earlier
`365c69` quiet/live observation was not a terminal result.

Both native completion records bind their six run-level full-result receipts,
84 tasks each, the original evaluator source and unchanged authorization.
Their preserved copies and session/observer evidence are in this archive.
This is complete evaluation evidence, not statistical inference or a release.

## Downstream transition — 06:41 UTC

Original summary observer `9ac728 / exit zero` confirms the genuine complete
BEIR collector has exited zero at **06:40:59 UTC**. The original probe
collector is now live/R, PID 981654/start325341027; summary coordinator
813139/start320676124 remains live/S without failure or complete inference.
Do not rerun these original collectors, change their source or compute
partial-population replacement estimates.

Original resume observer `f7790a / exit zero` confirms **both real GPU
resume checks have started** after their pools completed scientific GPU work.
Both coordinators remain live/S; all eight registered ranks have matching
original identities (pool a S, pool b R at this instant), no reader or
completion yet, no failure. GPU admission is not endpoint equivalence.
Use the original observer for later rank states and endpoint results.

The existing document, combined replay and outcome-durability entries still
wait for the complete original inference. Final statistical results, paper
review/reconstruction, outcome durability, GPU resume equivalence and source
release remain required. No frozen scientific source or authority changed.
The protected helper and historical stopped processes remain untouched.

## Actual complete inference and native paper — 06:45 UTC

Summary session **4529 / 9793ed exits zero**. The native BEIR collector
finishes at 06:40:59 and the native probe collector at 06:44:14, both exit
zero. Complete original inference finishes at **06:44:15**; its readout SHA
is `b6b69d9b087a6797c54330064316aa9f9fe71887797ef59313efce8c3214781d`.
All six table cardinalities are 168, 4, 126, 3, 60 and 840. Independent exact
rational effects, cell moments and multiplicity-weight bootstrap agree within
the unchanged 2e-15 bound; maximum observed bootstrap difference 1.735e-18.
No original estimate was replaced and no partial-population inference was used.

| Original contrast | nDCG@10 points | Marginal 95% interval | Decision |
| --- | ---: | ---: | --- |
| Weight state: Muon-source minus AdamW-source | +0.322202 | [+0.047130, +0.678382] | Positive |
| Reset operator: Muon minus AdamW | -0.526440 | [-0.896572, -0.185547] | Negative |
| State-by-operator interaction | +0.207976 | [-0.122930, +0.555147] | Inconclusive |

These are the original three marginal 95% intervals, not simultaneous family
intervals. The design crosses two genuine fixed step-2345 source states with
two reset operators and three order seeds for 391 updates on the same 50K
queries. Initial update calibration does not match every later update; order
seeds do not replicate primary source training. Positive state effect is
bounded evidence about these reached states, not a pure hidden-rule effect
of the different primary recipes, arbitrary weight-space geometry, functional
mediation, or a universal Muon advantage. Negative continuation operator effect
does not establish universal AdamW superiority. The interaction is inconclusive.

Document session **65801 / a07a56 exits zero** at **06:44:48**. The complete
native paper includes all original findings; actual abstract length 161 words,
main text ends page 8. PDF SHA
`ca88d2c087d889f41ac22e4e1ee9e1753bf919ddb5b096603c108a62ca5aecef`.
This is a generated/strictly compiled complete document, not yet full visual
review, final deferred-prose integration, authoritative installation or release.
Combined replay is actually running at 06:45:21 (bf35c3): child
982873/start325366831, original coordinator live/S, no failure.
Outcome durability at 06:46:04 (3eaa11) has returned its upload and verified
remote hashes; actual anonymous full download remains live, not yet complete.

## Failed original GPU resumes: device-placement diagnosis

This supersedes the earlier started-only observation. Both original sessions
**6666 / e5a992** and **9153 / 083fed** terminate with **exit one**. All eight
rank exits are one, as the original observer **e87721** confirms at 06:43:16.
No endpoint comparison reader was run. These are failed actual GPU attempts,
not synthetic tests, successful resumptions or endpoint mismatches.

All eight logs report the same first-resumed-update error:
`state_steps is on cpu`, unlike the CUDA tensors required by fused AdamW.
The original `FactorialOptimizer.load_state_dict` delegates tensor placement
to PyTorch. Its base `_process_value_according_to_param_policy` deliberately
leaves `step` unchanged unless the saved group declares fused/capturable.
The custom optimizer's strict group schema contains neither flag, while
`_adamw_step` dynamically selects fused execution for CUDA parameters. Thus
CPU-loaded counters remain on CPU; fresh optimizer initialization instead
creates counters directly on each parameter's device. The error occurs in
the shared AdamW path for both the all-AdamW and Muon/auxiliary-AdamW cases.
The original fresh-state scientific runs and completed evaluations are not
reclassified as failed by this separately exposed restore-path defect.

The repair is **not implemented or launched**. It must be a separately
source-bound restoration-only device-placement extension, preserving all named
moment values/dtypes, counters, group settings, original numerical kernels and
strict endpoint comparison. Do not add flags to the frozen schema, switch
kernels, alter original files/checkpoints, relax tolerances or retry unchanged
entries. Preserve every failed attempt and use fresh output paths and both
lease namespaces for any reviewed successor. No engineering incident belongs
in the paper, including its appendix.

`capture_completion.py`, actual **825345 / exit zero**, copies/checks 88
native inference/document/failure files (3,242,117 bytes). Original table
bytes and collector bindings are checked, every document artifact matches
its native manifest, all eight original failure logs and source are retained.
This archive read does not recompute results or waive unfinished deliverables.

## Complete outcome durability and source-identical replay recovery

Backup session **35024 / d0cf11 exits zero** at **06:48:52 UTC**. All
**593 files / 5,199,649 bytes** are uploaded, remote-hash verified and actually
downloaded anonymously. Prior remote subtrees remain unchanged. Immutable
revision: `7766dd0ae83e36a4c16121703813f6a9eec63336`; manifest SHA:
`296c688d47617986d297044f901f0c0e1e41e552a1e06110cff8f6bf01770230`.
Use the [concrete restoration guide](../../../docs/continuation-outcomes-restoration.md).
Do not repeat this completed backup or poll its terminal handle.

Original combined session **56449 / ef4678 exits one**. The nested complete
primary numerical reconstruction exited zero, but its I/O report retained 48
refused probes of two nonexistent editable-import path-hook placeholders,
resolved against the outer coordinator's working directory. Each placeholder
occurs 24 times. No network request occurred, and the primary computation
itself reports no exception; the unchanged stricter outer zero-refusal check
correctly rejected the attempt. This failure is retained, not relabelled passed.

A source-identical, explicitly recorded execution-context correction ran the
original closed entry from its own `closed/primary` working directory, into
a new output directory. No code, manifest, audit hook, allowed-root rule,
observation, numerical function, tolerance or source binding changed; neither
placeholder was allowlisted. The original failed output is unchanged.

- New directory: `/tmp/dense-v3-combined-replay-cwd.iU9436g5`.
- Actual session **96872 / acf598 exits zero**.
- Completion: **06:53:05 UTC**.
- Original input manifest remains
  `746a545d3e59d86aab8d4ff6579abca3be9884f1ea824a702e4fa28bc85956b7`.
- Complete result SHA:
  `6a0a8a3657d88a20529db8d45d32c31c5b74be783c0abb9d57994b0009d3c275`.

Both original I/O readers now report no exception, no refused producer reads
and zero network attempts. All original primary and factorial numerics, all
six continuation tables/independent verification, full document sources and
extracted PDF text agree; the original strict layout/font checks pass. PDF
metadata need not be binary-identical. This is genuine full numerical/PDF
reconstruction under Python I/O hooks, not an OS sandbox or physical second
host, GPU resume equivalence, visual review, authoritative installation or
source release. Do not rerun it as unfinished work. Final source tooling must
document the component working directory when exposing this unchanged entry.

All sessions tracked in this closeout are now terminal. The next required
work is the separately bound GPU state-device restoration repair, full paper
visual/prose/reference integration and reviewed distribution/source release.
The two original GPU resume failures remain unresolved. The active goal is
not complete and not blocked by a valid running queue.
