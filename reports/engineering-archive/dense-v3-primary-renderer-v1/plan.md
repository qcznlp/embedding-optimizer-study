# V3 primary rendering — separate preparation

Started while independent joint replay 74613 is live. None of that replay's
thirteen bound sources, the manuscript, training cores or frozen protocols may
change. This directory is separate from the joint acceptance archive.

The old `corrected_publication._bridge_table` requires finite pooled feature
RMSE and Boolean usefulness. The accepted v3 numerical contract instead retains
undefined folds, undefined pooled comparisons and three-valued usefulness.
Its decisions use exact MSE, not displayed RMSE. Verify these concrete interface
boundaries with the existing numerical kernels before designing a v3 renderer.

The intended renderer must preserve all original nine geometry features, every
defined/undefined fold and separate association status. Baseline-equivalent,
resolved-but-unsupported and unidentifiable cases must not be conflated. A tiny
exact improvement is not statistical significance or a meaningful effect size.

No placeholder or reduced publication scope can replace complete checkpoint-
backed authoring, reviewed v3 source/runtime/release integration, portable
reconstruction or strict manuscript gates. Do not insert any synthetic rendering
into the paper. The old renderer and its historical protocol remain unchanged.
