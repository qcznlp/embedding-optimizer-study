# Complete v3 functional export and feature integration

Parent acceptance: `d09cc7681c3b948390f457df26fd478d67ee3cc9676dc11627b29e5b47d6e631`.
The literal kernel and all original probe texts/order already passed their bounded checks.
Do not rerun those as missing work or rewrite old sources/protocols/results.

Prepare a separate source-bound v3 contract and complete input/feature interfaces. Require the
immutable pretrained reference plus all twelve completed v3 runs at all five steps before export
or feature output. Preserve the original BF16-model/FP32-vector encoding, query/document prompts,
8192 limit, 224 rows, fourteen tasks and eight positive-first candidates. Read actual raw row values
against the accepted ordered identities and exact relocated five-file probe inventory.

Model export must verify actual loaded parameters against the saved checkpoint converted to the
declared dtype, inference mode/settings, and unchanged parameter state after encoding. Retain raw
vectors and complete source/loading provenance. Require an externally supplied trusted matrix-
manifest SHA-256 for later cache/analysis admission; self-rehashing an altered vector/manifest is
not an authentication anchor. State plainly that a reader does not repeat model encoding.

Feature production must use the already verified literal kernel, every state/mask/rotation and
task-first aggregation. Preserve explicit undefined relative margins and FP64 coordinate arrays.
Readback recomputes all values from the authenticated raw vectors. No old run-name parser, renamed
historical vectors, subset or selected-rate substitute may establish a primary identity.

Verification must cover complete synthetic interface wiring, real primary-missing CLI refusal,
actual copied probe identity admission, corrupted/rehash/relocated cases and fresh CPU readback.
Any reduced-dimensional or synthetic numerical fixture is explicitly engineering-only; no fake
primary model/BEIR results enter the manuscript. Formal GPU export requires a reviewed, committed
assembled primary/runtime/parent release; preparation does not execute it. Publication inference,
the four-name exact bridge and portable final publication closure remain separately verified work.
