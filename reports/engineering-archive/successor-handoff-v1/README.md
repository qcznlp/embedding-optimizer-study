# Combined successor dependency preparation

This directory retains seven byte-identical isolated protocol predecessors from before the
combined handoff update. It is not a live deployment or a completed scientific result.

The current source-bound amendment is
`configs/dense_no_packing_state_operator_handoff_repairs_amendment.json`. It preserves the prior
header-only amendment as history and now requires the separately prepared combined main target
`af646ecf89bfe87cf79a20eaf3495d134eeb912899e9c219a7917f96dfdc976c`.

- Outcome, bridge and sensitivity use the exact three candidates in `main-handoff-v1/after`.
- The narrative publication keeps its own scientific renderer; only its dependent protocol
  identities and an explicit non-scientific note change.
- The dimension handoff refreshes that publication identity, with all eleven commands intact.
- The factorial publication refreshes the existing dimension amendment identity only.
- The completion lock refreshes those parents and the exact required main contract. All 47
  prepared commands, original 36 factorial commands, arguments and full main gates are unchanged.

The read-only command is:

```bash
CUDA_VISIBLE_DEVICES='' OPENBLAS_NUM_THREADS=2 OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 \
  PYTHONPATH=/root/embedding-optimizer-story-refactor/src /usr/bin/python3 \
  -m scripts.audit_successor_contract --repository /root/embedding-optimizer-story-refactor
```

It reconstructs both exact amendment layers and exercises five actual consumer protocol loaders.
The resulting isolated contract is `a64b26589a890a83dcfc298f89a450017e5a78fff14eb1492d152fe402921fa6`;
the projection onto the original host is
`37a4485cb2f2cbdd8bf0dd11a49ae0b63dd94b601506e090aff8ec954a1ecaa5`.
Four checkout-local command arguments explain the difference. Recompute on the actual host after
an approved deployment; neither identity is the live `6605090d...` controller.

The first focused attempt is retained as `combined-successor-focused-tests.xml` (54 passing,
one failure). It exposed the stale factorial-publication dimension-amendment binding. After that
binding was refreshed and tested explicitly, `combined-successor-focused-tests-v2.xml` passes all
56 cases. The subsequent `combined-successor-full-tests.xml` passes all 1,071 tests, with no
failures, errors, skips or exclusions, in 145.892 seconds. Tests reject old and header-only main contracts, incomplete runs/backups/steps, changed
archives, rehashed scientific changes and altered dimension commands. They execute no training,
evaluation, real controller lease or migration.

Deployment remains a separate controlled sequence: narrow main repair and exact migration;
full completion of the repaired main; verified sole-owner narrative/dimension deployment;
byte-identical archival of the still-zero-step factorial ledger; host-side contract recomputation;
then only the successor. No HF deletion or history operation is authorized by this preparation.
