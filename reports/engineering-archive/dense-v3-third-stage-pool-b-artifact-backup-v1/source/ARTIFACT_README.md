# Corrected DenseOn: original pool-B step-2345 BEIR evaluations

This immutable data snapshot preserves the six newly accepted third-stage
checkpoints from the original evaluation pool B. Each checkpoint has all fourteen
pinned full-corpus BEIR tasks. The six recipes are AdamW 3e-6 / 1e-5, Muon
3e-4 / 3e-3, and NorMuon 1e-3 / 3e-3. Membership follows the original queue,
not performance. This is not the complete twelve-configuration step-2345 grid.

Contents: 108 original native score/metadata/complete-admission files, 168 original
started/exited worker records, two CSV tables, a data-only acceptance summary,
this README and an externally anchored artifact manifest: 281 files in total.
There are 84 task scores and six fourteen-task macro means. No task, selected
recipe, source state or high-rate result is omitted from this declared cohort.

The accepted host readback contains forty-two complete checkpoints; only six and
84 task values are new here. The earlier-stage and final datasets remain separate,
unchanged snapshots. The native readback itself embeds source snapshots and is
not uploaded. Its SHA-256 is
77a0d0bdb3251b86a3e1ab9d6c6fe56e96f9e8af388c16f3acd7bd4d035f3d24.
The accepted archive verification SHA-256 is
71d559b1e5602ba86239d6ad0bfccd0bdc14dda511a78814c980e8b62ee62ac2.
The independent raw reconstruction SHA-256 is
9b13b7a72c74881025703fc5fbea541b3bcfc06bb4f4e8c16f82998e0d400ec6.

Original local paths, commands and PIDs are provenance, never instructions to
execute. Recovery readers must use only the supplied snapshot and externally
trusted manifest digest. No model tensors, raw query/document examples,
executable source bodies or credentials are included.

This transports accepted aggregate task measurements. It is not another model
run, per-query ranking trace, statistical comparison, source release, full
trajectory result or mechanism finding. All 840 primary task cells, functional
analysis, the separate crossed continuation and the final NAACL/reproducibility
deliverables remain required.
