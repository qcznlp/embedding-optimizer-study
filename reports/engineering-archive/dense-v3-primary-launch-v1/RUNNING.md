# Owned calls: all finished

All observations are September 7, 2026 UTC. No handle below is live; do not poll
or restart it as missing work. No formal primary training handle exists yet.

| Handle | Observed terminal result | Scope |
| --- | --- | --- |
| 32387 | exit 0, 104 tests | actual assembled training suite, 12.976 seconds |
| 15614 | exit 0 | one-root primary contract, pinned runtime and inherited dispatch |
| 52815 | exit 0 | real input / all twelve identity preflight |
| 31117 | exit 0 | exact 2.1G training-data copy to the new launch namespace |
| 59150 | exit 0 | W&B read-only account/project availability |
| 14230 | exit 0 | actual launch-namespace training/validation content verification |

The two exact-source assembly commands also returned exit 0 immediately. The
validation-data copy returned exit 0 immediately. These are not GPU training jobs.

The September 7 15:02 UTC exact-path lock probe also returned exit 0 immediately
(no live session). `lease-readiness-first.json` preserves all sixteen path checks
and before/after checks of only the three approved dispatcher handles. Twelve
existing locks were unheld; four primary-namespace locks were absent. File bytes
and inodes are unchanged; no lock or child job remains held/running by the probe.
This does not reserve the GPUs or resolve the outstanding local commit authority.

Paused predecessor factorial Trainer work: 6141 and 10873 are terminal/fail;
2012 is terminal/pass (39 cases). The first API-adapter failures and both exact
sources remain preserved. Its actual numerical Trainer and real checkpoint-resume
experiment have not run; do not confuse the passing argument/loader/seal tests
with that missing integration proof or include that code in the primary snapshot.
