# Primary launch preparation after owner reprioritization

Observed through **2026-09-07 15:02 UTC**. The owner explicitly asks to confirm
correctness and promptly train. Primary training now takes precedence over new
factorial/analysis/paper-tool development. The full scientific goal is unchanged.

## Ready preparation

- One actual source root is assembled at `/root/embedding-optimizer-primary-v3`;
  the first identical assembly is `/tmp/dense-v3-primary-launch.pztjAW/assembled-first`.
  All 56 selected files have exact audited bytes, including real local import
  dependencies. The new unverified factorial Trainer is not included.
- **104 training tests pass**, zero failures/errors/skips, including numerical
  policy, run identity/save/restore admission, loss, optimizer references,
  collators and runtime. The XML contains exactly 104 testcase elements.
  This is the relevant training suite, not a claim that every legacy analysis
  interface in the different 963-case candidate has become compatible.
- The actual v3 contract loads with repository and training_root equal, and the
  real pinned runtime and inherited numerical dispatch verify successfully.
- Actual 500K training data, 4,096 validation data, pretrained file contents and
  all twelve recipe/execution/numerical identities pass read-only preflight.
  Shared multi-GB inputs are actually read once; each recipe is separately
  reconstructed. All 13 imported package modules are local and authenticated.
  No model update or primary job occurs in this CPU preflight.
- Byte copies of both data views are ready in
  `/root/embedding-optimizer-v3-experiment/data`. Actual contract re-reading of
  this launch namespace passes: training 500,000 rows / 18 files, content digest
  `eb68b47e9ee18be9962ccbfb0b8c76e62767ae744dde925b72b63a079ab24502`;
  validation 4,096 rows / 19 files, digest
  `89f1089d66d26bfaa42413aa80e6cf0fe5bdb8495a1160b30f17edb298103b84`.
- A read-only W&B API check confirms the existing personal entity matches and
  the declared project exists. Credentials were handled by the existing client;
  no secret was printed or copied into source.
- The exact project-lock probe in `lease-readiness-first.json` passes. Both
  checked directories are different real inodes: the primary protocol uses
  `/tmp/embedding-optimizer-primary-gpu-leases`, whereas the old evaluation
  namespace uses `/root/embedding-optimizer-study/logs/dense-only-runtime/gpu-leases`.
  All eight existing old-namespace locks and four existing primary-namespace
  locks are unheld at the probe; primary locks 0–3 do not exist. No lock was
  created, truncated or left acquired. Existing bytes/inodes remain unchanged.
  The exact three approved old dispatchers are still T before and after, with
  the unchanged old ledger. This is not a claim that all GPUs are generally
  idle, and a momentary probe does not reserve them for later training.

The corrected numerical core is unchanged from the prior actual full-DenseOn
four-rank natural-data preflight and the separate real save/restore diagnostics.
These scope-limited prior checks remain archived; they were not rerun as missing.

## Exact receipts

`source-assembly.json`: 15,970 bytes,
`e603ef175c0f691a1dd83c4d8b474d90da2fc67c8c65f5a77f3c3180e78332b8`.

`training-tests-first.xml`:
`913a2a4752de7c2ed07c4d33cf1d112b78f1daf103a0367fff232bb5435afd24`.

`preflight-first.json`: 16,038 bytes,
`54c3576b071538b2251ace07839e64dbc9e0d1b53c8f081a1451897d798209ad`.

Producer snapshots are in `source-current/`; the whole selected source plus new
handoff documents is in `assembled-source/`. No numerical source or old protocol
was modified to produce a passing result. The eleven old failures were each
inspected: six reject legacy analysis source hashes and five reject legacy
factorial source/completion hashes. They remain failures of those old interfaces,
not untested arithmetic bugs or a reason to change their historical locks.

## Immediate next action and outstanding authority

No formal run has started. The owner now authorizes primary training after its
necessary correctness checks. One separate question asks permission for a local
training-code commit while the paper remains pending, with no GitHub push. This
resolves the old pre-commit/final-paper rule versus committed-training-source
requirement; **no answer has yet been received**. Do not self-waive that rule,
repeat the same question, or resume optional tooling while waiting.

After that direction: freeze the exact reviewed training source and a separately
versioned training-only execution release, then start
AdamW 3e-5 and Muon 3e-4 on the two four-GPU pools. Complete every remaining declared
rate; retain all five checkpoint stages and verify durable copies. Keep the old
DRAFT and historical evidence unchanged. This staged training release is not final
paper publication, and all final scientific/repository requirements remain required.

The launch must recheck the exact stopped-controller handoff and keep the selected
pool's existing common project lease held for the entire child training lifetime,
in addition to the primary entrypoint's distinct frozen lease. An empty primary
namespace must not bypass common coordination. No old process is resumed, killed
or replaced by this preservation-only proposal; no bound protocol/source is edited
to pretend that the two lease directories are the same. This actual launch guard
and committed-source execution release have not yet been executed or accepted.
Do not repeat completed numerical checks or restart optional factorial/paper work
while awaiting the already-asked local training-code commit exception.

No old controller was resumed/killed/replaced, no protected helper was touched,
no GitHub write retried and no HF deletion retried. The old main ledger and actual
manuscript hashes remain unchanged. The old 12 runs/60 checkpoints remain held;
there are still zero accepted full-horizon v3 runs.
