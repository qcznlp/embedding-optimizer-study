# Primary training is the immediate priority

2026-09-07, following the owner's explicit instruction to confirm correctness and
promptly start training. The owner now conditionally authorizes the new primary
runs after their necessary correctness checks. Additional factorial/analysis/paper
tool development is paused and is not a primary-training prerequisite. The full
paper and mechanism deliverables remain required later; no scientific gate or
historical result is relabeled.

The eleven identity-candidate failures were inspected individually. Six concern
legacy execution-sensitivity/geometry/outcome/bridge source locks; five concern
legacy factorial source locks or completion. All reject the changed config/train
bytes. None is a loss, backward or optimizer numerical failure. The earlier whole
candidate is still not green: do not replace its hashes or call these tests passed.
The actual corrected primary sources already passed three real four-rank natural-
data runs and prior real checkpoint save/restore, with distinct stated scopes.

Immediate work: assemble unchanged audited numerical sources and actual v3
train/checkpoint/backup/evaluation consumers into one source root; verify its real
imports, pinned runtime, twelve recipes, revised data and necessary training tests.
Retain all three optimizers, four rates, 500K groups and five checkpoint stages.
Freeze/commit and deploy the training source only after the applicable authority
and verified execution boundary. Do not require new paper tables or factorial
experiments before primary training. A separate question now requests the local
training-code commit exception; it does not ask for redundant training approval.

The live checkout/controllers/leases and protected helper remain unchanged. No
GitHub write retry, remote publication or HF deletion is authorized by this change
of priority. No formal job has started at this initial preparation point.

Paused factorial work: 6141 is terminal/fail (39 cases, 6 new API-adapter failures),
10873 is terminal/fail with the same adapter issue in real four-rank CPU setup,
2012 is terminal/exit 0 (39 cases after the adapter fix). All owned handles are
finished. Source-first and source-retry preserve the new two modules, test and
worker. The actual Trainer numerical run/save/resume remains unverified and is
not included in this primary source assembly.
