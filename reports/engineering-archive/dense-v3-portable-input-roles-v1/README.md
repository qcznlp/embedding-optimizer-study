# Authenticated input roles and portable negative controls

2026-09-13. Engineering delivery only; no scientific result or manuscript changed.

## Actual results

The unchanged full distribution scanner now passes on a fresh wheel and sdist:
build and audit both exit zero, original tool session 90928 / terminal a3abee.
The preceding actual six-finding audit remains in the portable-handoff archive;
this task's intermediate one-finding failure is retained under `actual/`.
No scanner change, packaging exclusion, string encoding, historical evidence
deletion, negative-control removal or old-source hash replacement was used.

The changed input reader returns **exactly the accepted original input digest**
`817a87138b693cf6f8e491d3fd0f838ab39c7416d8e13935473362dbf9adedb7`
against the already relocated genuine 5.63 GB inputs, including both sealed
step-2345 source checkpoints. Pinned audit bytes and native checkpoint/durability
checks remain unchanged. During this read, original producer reads and networking
are rejected by a Python audit hook; zero rejections occurred. This is not an OS
sandbox, tensor deserialization, a physical second host or fresh numerical replay.

Verification driver session **71969**, terminal **2a8f07**, actual exit **0**:

- Genuine changed-reader read: exit zero; identical complete path-dependent result.
- **43** local location/pause/distribution tests: no failures, errors or skips.
- **58** calibration and missing-calibration cases: no failures, errors or skips,
  using an explicitly recorded diagnostic source assembly, not the root package.
- Intermediate fresh build: exit zero; original audit: exit one with only the
  remaining cold-reconstruction test's historical literal.

The final changed cold CLI test is separately **1/1 passing**, session **23255**,
terminal **cb01fc**, actual exit zero. It reconstructs the explicitly synthetic
fixture and exercises every original forbidden namespace before replay. It does
not recompute the genuine scientific results or claim synthetic primary admission.
The separate initial 24-case role/pause run also passed (80a144); it is not an
additional independent experiment. Ruff and diff checks pass (1ead5e).

## What changed

`factorial_v3_inputs.Locations` retains its five-field caller interface. Recorded
namespaces are derived lexically from the two SHA-pinned original audits using
the fixed scientific inputs' relative names. No recorded root is resolved or
opened. The more specific evidence role precedes its repository parent. The
same 49 data and eight verification bindings, both source seals and durability
receipts are checked as before; scientific/execution admission remains false.
Standalone `locate` now authenticates the input audits instead of relying on
hardcoded historical defaults. `load_inputs` authenticates them once and reuses
that checked mapping within the read. New controls cover all recorded inputs,
noncanonical paths, siblings/unknown roots, symlinked roles/descendants, modified
audit bytes and a deliberately synthetic alternate producer namespace.

The pause-guard test derives valid argv from its unchanged, exact historical
MODEL_ROOT and RESULT_ROOT constants; no actual process is inspected or signaled.
The missing-calibration test uses absent temporary location roles and still
refuses before genuine input access. Existing calibration escape/symlink tests
use authenticated recorded namespace identities rather than literal host paths.
The cold CLI gets the actual historical producer root from its own already
authenticated probe receipt; it still forbids that root, the current source and
the synthetic producer, and now actively verifies each refusal before replay.

Preimages of all changed existing source/tests are retained in `before/`. The
new input module is **not** silently substituted into any frozen 66-file source
parent. Those old source guards intentionally remain unchanged. The diagnostic
calibration assembly overlays the accepted numerical modules and this changed
input module, with a complete recorded module hash inventory. Its green tests
do not make the as-yet-unified root numerical package a validated training release.

## Remaining work and evidence

Complete genuine scientific-consumer/numerical-source integration, exact-resume
divergence localization and the reviewed committed source/runtime/release-parent
transition remain. Packaging admission alone is not full reproducibility or a
GitHub release. No GPU, optimizer kernel, original science, HF object, GitHub
write or protected helper was changed. Earlier denials and unknown exits remain.

Work directory: `/tmp/dense-v3-portable-input-roles.eOsTxin7`.
`actual-clean/` is the first passing full distribution audit. `actual-final/`
records the fresh build/audit after the first current handoff updates (40951 /
5984e1, actual exit zero). A subsequent documentation consistency read found
three stale remaining-work sentences; they were corrected without changing
science or code. `actual-handoff/` is the fresh build/audit of that final snapshot.
Source/test,
documentation and package snapshots plus their manifest accompany this report.
