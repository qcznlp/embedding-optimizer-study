# Reader-first repository homepage

Documentation-only closeout, 2026-09-13. The previous root README is preserved
byte-for-byte under `before/README.md`; it remains historical evidence, not an
instruction to restart old work. No numerical source, frozen input, scientific
definition, manuscript, existing result or running task is changed.

The homepage is reorganized around the research question, actual current status,
bounded primary findings, design, recovery entry points and development/release
boundaries. Long dated engineering/test narratives remain available in the
preserved predecessor, PROJECT_STATUS and their original evidence archives.
The original pending-conclusion block and attribution are retained. Complete
corrected source publication, full continuation inference, final paper/replay,
GPU resumes and outcome durability remain pending; no release is inferred from
this local edit. The original distribution failures are not rerun or waived.

Before editing, the six active authorization records were inspected for both
absolute and relative README bindings; none binds this root README. The native
document/replay implementations and unchanged pending-conclusion checks were
also inspected. A first text search used two nonexistent script basenames
(tool `be04ed`, exit 2); actual filenames were then discovered and read. This
was a read-only discovery miss, not an experimental failure or changed worker.

The entry-point findings are sourced from the complete primary trajectory and
endpoint reports, and the complete weight/functional comparator reports; no
partial continuation scores or synthetic diagnostic results become headlines.
Validation-selected and four-rate primary estimands remain distinct; task-level
uncertainty is not training-seed replication. The original
development commands are retained only for separate environments, not executed
in the running experiment stack.

Before-edit native observation `2034b6` exits zero at 02:44:37 UTC: 18/168
continuation BEIR tasks complete, eight registered FEVER workers live/R, both
coordinators live/S and no failed worker/coordinator receipt. Original sessions
41515/37455 remain live through native 50-second waits (`7bb1d8` / `930787`).
After-edit checks and their exact scoped evidence are recorded alongside this
file. They check a documentation change, not whole-experiment or release
completion. Do not repeat completed training, model reads or builds for this edit.

## Actual after-edit checks

The root README changes from **368 lines / 25,998 bytes** to **164 lines /
10,082 bytes**. Its SHA-256 is
`7a759f47192c8798f4bc09640edb67f31b2feb1eda9c91fe9e1dd7c243b46761`;
the preserved predecessor is
`fd1838874523b12660533e5dcaad0d53c6592bf95905ccd5d27ec5f15866c040`.
The before-copy matches the complete pre-edit tool read exactly. The already
measured twelve-trajectory image was visually inspected and reused unchanged;
no new figure, data point or result was generated.

Actual validation **f97237 / exit zero** parses the CommonMark document and
three tables, checks **34 local destinations including one image**, and
preserves the pending block and attribution byte-for-byte. Five external
links are retained, not freshly fetched or asserted available by this check.
All six frozen authorization hashes, both paper mains, the bibliography and
the original distribution scanner remain unchanged. Its command and complete
scoped record are retained in `document-validation-tool.json` and
`document-verification.json`.

One existing document-overclaim test runs in isolated CPU settings: session
**9802**, actual terminal **49de8d / exit zero**. The original JUnit file is
retained; this is one relevant case, not a full regression, experiment or
release audit. Dependency deprecation warnings remain observed, without any
environment installation/update. A wording patch first failed on an archive
line-context mismatch, changing neither target; the exact lines were read
and the corrected patch applied before validation. No historical failure or
scientific result was replaced.

At **02:56:03 UTC**, all six original observers exit zero: BEIR is still
**18/168**, all eight exact FEVER workers are live/R, all six downstream CPU
waiters are live/S, and no task/coordinator failure or actual downstream
result has appeared. Raw observations and tool results are preserved. Native
sessions 41515/37455 remain live through the subsequent 50-second wait
(`4852d3` / `831d44`). Only the homepage and this scoped documentation archive
are changed. This turn is **PROGRESS** in repository readability plus a
verified wait; full paper/results/publication requirements remain unmet.

A first one-off archive-integrity command had a Python syntax error
(`e7cd8a`, exit one) before executing or writing anything. The corrected
metadata-only command below does not replace or rerun a scientific guard.
