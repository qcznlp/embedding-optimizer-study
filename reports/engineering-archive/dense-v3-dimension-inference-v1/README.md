# Functional task inference and the genuine four-feature bridge

This is **analysis-code preparation, not an optimizer finding or formal experiment**. The full
v3 primary population does not exist. No primary vectors were encoded or admitted here, and no
manuscript include, numerical training source, controller or remote repository was changed.

## Connected interface

The [new draft](../../../configs/dense_primary_v3_dimension_inference_protocol.json) binds the
accepted [export/feature parent](../dense-v3-dimension-chain-v1/README.md), original geometry/outcome
bridge and exact named-family arithmetic. The public reader first admits all actual primary runs,
reconstructs original outcomes and geometry, and requires literal dimension-feature reconstruction
from externally anchored vectors. It never treats a self-rehashed summary as a trusted input.

Inference retains the original scientific family: fourteen paired task units, all four rates,
three co-primary features and three optimizer contrasts in **one nine-contrast** fixed-SE max-T
family, with 50,000 shared task resamples. A nonpositive standard error refuses the entire family.
The three required native directions must all pass simultaneous intervals; a failed joint rule
is not equivalence. Rotation readouts describe only the three tested shared bases.

All original nine weight-space columns remain in the complete 60-row outcome panel. These four
functional predictors are added under their own names, never substituted into old geometry slots:

- Helpful margin-attribution mass share.
- Helpful margin-attribution participation ratio.
- Degrading margin-attribution mass.
- Relative shortlist nDCG after 50% random coordinate removal.

Each uses four held-out-dose folds with 45 training and 15 test rows. Predictions and MSE decisions
use the already accepted exact-OLS numerical amendment. Every fold and predictor is retained,
including undefined or unsupported cases. No available-fold pooling, rounded-RMSE support rule,
learning-rate-as-seed inference, causal-mediation or unseen-task generalization claim is made.

The renderer retains all 68 descriptive points in four native vector panels and all nine contrast /
four prediction rows. Its contingent text distinguishes a failed native criterion, rotation
direction instability, an undefined predictor and an unsupported predictor. It can generate only
an engineering bundle here: manuscript installation remains separately gated.

## Verification scope

- **Independent statistical reference:** Python task reductions and Torch FP64 multiplicity-weighted
  bootstrap computation check all nine interval estimates, their shared critical value and desired
  directions, 27 rotation contrasts and 68 plot points.
- **Independent prediction reference:** SymPy rational full normal equations check 240 held-out
  predictions, 16 fold MSE comparisons, four pooled decisions and eight residual correlations.
  This is independent of production Fraction residualization, on an explicit nondegenerate fixture.
- Exact stage-only/null controls retain zero improvement, not spurious support. Invalid identities,
  duplicate/missing task/mask/rotation cells, non-finite inputs and zero-SE families are rejected.
- Complete downstream integration is tested with **explicitly simulated upstream admission**.
  Those fixtures are not real checkpoints, vectors or retrieved outcomes. The actual CLI rejects
  missing primary runs before later-source reads or output creation in both build and inspect modes.
- A fresh process copies the fixture and bundle into a new directory, recomputes all numerical
  results and rejects six altered/rehashed table or text variants. This is relocated **synthetic
  numerical replay**, not final portable reconstruction from primary checkpoint/vector payloads.
- The synthetic standalone PDF compiles without Type 3 fonts; its two pages were visually checked.
  This does not certify the full NAACL layout or install its artificial values in the paper.

The first audit's symbolic-rank error and exact source are retained in
[attempt-1](attempt-1/README.md). The correction adds five independent-oracle controls; production
inference and scientific rules did not change. All **64 new focused cases** and the isolated full
suite's **2,334 cases** pass, with no errors, failures or skips. A passing test count is not empirical
support for any optimizer. Exact receipts and the acceptance are retained here.

## Evidence and next work

- Source-bound draft: `d66274878d90e463b74a03cf6e09518ae01dcddfe76584542e3bece435713545`.
- Successful independent audit: `5b3f2e3db1ec345239e98cbd0bee6a21e78e9d85cf6dcb73994b9177c2862268`.
- Fresh relocated replay: `59455e14861cff4f08942b1ff949d77d680aeaa326903af34631f4e52d710669`.
- [Reproduction commands](commands.md), [preserved source](source-current/),
  [independent audit](audit.json), [fresh replay](replay.json), [acceptance](validation.json).

Next close the actual primary publication/portable chain. In particular, full-source gather still
records host-bound source paths; relocating an already-computed synthetic numerical bundle does
not solve end-to-end primary relocation or replace source authentication. Preserve all three
boundaries: vector production identity, feature reconstruction, and statistical publication.
Then integrate routed factorial controls and the reviewed assembled-source/runtime/release-parent
transition. Formal execution, publication and owner-authority gates remain unmet.
