# Complete corrected-v3 weight-artifact backup and actual recovery

Completed September 10, 2026. The already completed genuine weight measurements
now have a public, immutable off-host backup. This is numerical-artifact durability,
not source publication, a new scientific experiment or complete paper acceptance.

## Exact scope

| Native bundle | Files | Logical bytes |
| --- | ---: | ---: |
| Approximate geometry: all twelve runs / sixty states | 137 | 1,650,893,609 |
| Exact geometry: all twelve runs / sixty states | 137 | 3,325,690,821 |
| Original all-state update map | 10 | 3,272,744 |
| Full-dimensional companion readout | 7 | 1,380,282 |

All **291 native files** are retained, including raw stage records, complete
spectra, retained bases, original manifests, every table and all figures. Five
original provenance records, a README and the complete artifact manifest bring
the snapshot to **298 files / 4,981,840,933 bytes**. No code, training examples,
model inference, new SVD or retrieval score was added.

The complete original file populations were selected using their authenticated
native completion records and manifests, copied into a new staging directory,
hashed and scanned before any remote write. Approximate and exact definitions
remain separate; all rates, stages and original false gate flags are preserved.
The old map plan's `external_publication=false` describes its original local
measurement; this separate numerical backup does not rewrite that record or
release a source/scientific contract.

## Immutable destination and actual verification

- Dataset repository: `qcz/embedding-optimizer-study-analysis-artifacts`.
- New revision: **209b4517e64ac5373db47b04ada39104e9080016**.
- Original parent: `489c606076b0f8a0ef82adc7b85f1fb29da794bc`.
- Manifest: 74,798 bytes, SHA-256
  **9f53cdb8260f7b9d8f4c29db04159d02dfb7ff4e272cf9c9ba49cba842a1a841**.
- Content-addressed prefix:
  `corrected-dense-correctness-v3/weight-space/4400f1ce26ef423873a4eb509db48e046a07b90159f6d5ef10da084f5171107b/9f53cdb8260f7b9d8f4c29db04159d02dfb7ff4e272cf9c9ba49cba842a1a841`.

The actual upload returned at **12:10:24 UTC**. Anonymous full-prefix verification
completed at **12:10:32 UTC**, matching all 298 file sizes and LFS/Git digests.
All twenty original root entries retain their exact file/tree identities; only
the new namespace was added. No existing remote path, card or history was deleted
or overwritten. No WIP program source was uploaded.

The complete anonymous recovery into a fresh directory finished at
**12:19:06 UTC**, with all 298 actual files matching size, SHA-256 and original
remote digest. A separate execution of the recovery guide's standard-library
offline block finished at **12:21:16 UTC**, hashing every payload again without
importing the uploader/project code or accessing the network, GPU or a model.
Only the destination placeholder was substituted; executed block SHA is
`d14881f27df2f680892796e9f03edc6dba99a9a391c7389cca9dede554857383`.
The guide's whole-file SHA at execution is
`abd916862a88a98451e1d14bb7f687ad822dca1119526759c188a178b5f2734e`.

Use [weight-analysis-restoration.md](../../../docs/weight-analysis-restoration.md)
for exact locations and independent recovery. The actual transfer used explicit
per-file anonymous HF calls; the guide's convenience `snapshot_download` block
was syntax-checked, not executed as an additional second transfer. Its offline
verification block was actually executed as described above.

These checks recover a remote artifact on the same physical host. They do not
prove a second-host end-to-end analysis, code availability from a clean GitHub
clone, useful embedding dimensions, optimizer superiority or causal mediation.
Learning-rate cells are not independent training seeds. Full scientific outcomes
and the authorized source/paper release remain separate requirements.

## Preserved evidence and exact attempts

`actual/` contains original preflight, upload-start, returned-commit, remote-audit,
full-download and independent-guide logs, plus the original manifest and tests.
The upload receipt still has `durability_verified=false`: it preceded the
separate successful remote audit. No earlier record was rewritten.

The **23 focused tests pass**, including actual complete metadata selection,
path/symlink preservation, changed remote-content refusals, attempted-upload
retry refusal and embedded code/credential-shaped text rejection. They are
transport guard checks, not numerical experiments or a full-repository test run.
The actual-population check now lives in this archive's explicitly host-only
`test_actual_population.py`; the normal test tree contains only the 22 portable
guards. Both are exercised together by the final 23-case check. The original
first test source and XML are preserved, not replaced by the reorganized tests.
The exact source is preserved in `source/`; uploader SHA is
`02e41abf36397ad8c0b0ff2fedf9bdaedf5e4ae09de7d736592d8ae34157bf15`.

All four owned execution handles are terminal with actual exit zero:
**81722** preparation, **60351** upload/remote audit, **29738** full recovery,
and **19074** independent offline guide check. Do not rerun the one-shot upload
or treat these completed checks as missing work. The original numerical producers
were not restarted. The tests also exited zero without a continuing session.

The exact temporary root is `/tmp/dense-v3-weight-artifact-backup.7hBFjg`.
Its complete `staging/` and `download/` payloads are retained, not copied into Git
or silently deleted. `before/` preserves the three documentation preimages;
each matches the preceding recovery-handoff verification. Original native
measurements, primary source/checkpoints, and all prior/failed evidence remain.

## Existing experiment, separately observed

At **12:22 UTC**, [exact observations](observations.json) show primary BEIR
**22 / 840**, baseline **14 / 14**, eight primary workers alive, validation
**7 / 12**, and functional **0 / 61** waiting for full validation. No failure
receipt was present. All twelve primary trainings and sixty model backups remain
complete. These are timestamped observations, not permanently live handle claims.

The unanswered resource-priority question is still not approval. No GPU job,
frozen numerical/dispatch source, historical controller, manuscript or protected
helper changed. No GitHub retry, source publication, historical HF erasure or
resource handoff occurred. Continue the full existing evaluation/functional work,
then the bounded crossed continuation and complete NAACL/reproducibility deliverables.
