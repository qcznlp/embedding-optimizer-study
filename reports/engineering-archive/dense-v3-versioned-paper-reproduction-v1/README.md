# Complete numerical-to-reviewed-paper source composition

2026-09-13. **The installed-wheel command now completes both phases together:**
the original full primary/factorial numerical reconstruction and the reviewed
current manuscript. Actual execution **82694 / f9d233 / exit 0**; no GPU work.
See [usage](../../../docs/versioned-paper-reproduction.md).

## Version transition actually executed

The current primary-training code and original analytical contracts bind
different `config.py` and `optimizers.py` versions. The new outer entry verifies
both roles and the exact original 189-file closure, runs the unchanged numerical
entry in its own fresh process/component working directory, and uses the current
installed document component for the reviewed manuscript. It does not patch
imports in place, replace old hashes, reverse validated training code or label an
old publication contract as admitted.

Actual module provenance is **29 current modules from the extracted wheel**,
**26 original modules in the combined numerical process**, and **84 original
modules in the full primary child**. Both original Python I/O receipts report
null failure, zero network refusals and no refused producer reads. The parent
integration launcher also refuses network connections. This is observed
same-host process isolation, not an OS sandbox or a physical second-host test.

The original numerical entry recomputes all primary outcomes, validation
selection, original/exact weight and functional predictions, exploratory recipe
controls, six factorial tables, independent arithmetic/bootstrap checks and the
original complete-result document. It retains the original numerical functions,
populations, tolerances, generated-source comparisons and document checker.

All **eight shared result/figure inputs** match the reviewed snapshot exactly
before and after the new build. Seven are numerical output inputs; the eighth
is the unchanged conceptual design figure, not a measured finding. The reviewed
prose/bibliography retain their separate authenticated snapshot.

The resulting reviewed manuscript passes the strict checker: **158-word abstract,
eight-page main, 13 total pages, 25 fonts**. Completion SHA:
`b4dfd20b6323f767a760d2769543642c4de501ad45e01af601d1d9aea3fdd69e`.
PDF SHA: `822bc3e2e7cbdef626cce6df137b6579a8fedaa6c8cecf265f667705d64c66e0`.
PDF metadata may differ between compilations; the source and document checks,
not byte-identical whole-PDF metadata, define document reproduction.

## Actual checks

| Check | Observed result |
| --- | --- |
| 23 new source-isolation/refusal controls | 91556 / eb5cf1 / exit 0 |
| 194 combined source, distribution, paper and restoration tests | 80260 / fa5ece / exit 0 |
| Full wheel/sdist build | 53817 / 2ce05c / exit 0; complete build.log retained |
| Original unchanged full distribution audit | b20e25 / exit 0; complete, no findings |
| Actual wheel execution | 82694 / f9d233 / exit 0 |
| Final new-module style check | 572eb2 / exit 0 |

The original full audit covers 211 package modules, 218 data files, 105 console
scripts, 64 runtime configurations and 50 transitive execution configurations.
Wheel SHA `c3d60353ef597629d10097f76aa7da1247bd7b311b277fd2ce7ebde04443ee96`;
sdist SHA `9bcdeb8dce917f3e28fb6db1a7deed6fc74022c188eb6d8f1e78d906c4421be4`.
These are the actually executed snapshots; subsequent handoff-only edits do not
retroactively alter the captured package. Final source release will require its
own final assembled artifact checks.

New controls include genuine closure authentication, changed/missing/extra and
symlinked input rejection, duplicate/nonfinite JSON rejection, exact scientific
input joins, existing-output preservation, failure-before-document ordering,
empty child PYTHONPATH, component cwd, native child exit recording and timeout
cleanup restricted to the newly created owned CPU process group. Synthetic
orchestration controls do not claim real numerical admission; the separate
full wheel run above supplies that actual execution.

Initial Ruff 9f03e8 exits 1 for import ordering only; 1afc3d fixes that ordering
and exits zero. The sources were then formatted, tested and frozen for the real
execution. No numerical comparison or failed scientific receipt was changed.
Three later read-only summaries could not run because jq was absent (exit 127);
the Python JSON readback then succeeded. These tooling facts are not experiment
failures and no dependency was installed to address them.

## Remaining boundary and retained evidence

The historical 56/60-file publication contracts and their original failed
single-tree admission remain unchanged. The factorial's 66-file fresh-worker
source contract still refuses the changed input module. The new command is a
complete numerical/document consumer, not fresh-worker training admission,
checkpoint encoding, GPU restoration, an automatic manuscript installer or
remote publication. It reports repository publication as false.

The local code now provides the requested versioned scientific/document entry.
Remaining work is the reviewed default-entry/runtime/release-parent transition,
curated source/runtime assets and actual authorized source publication. Do not
rerun completed scientific experiments or this passed integration as missing.

`before/` preserves the immediately preceding handoff; `after/` the new handoff.
`source/` contains the executed module/tests and guide/package configuration;
`actual/` contains actual launch/build/tests, wheel/sdist, terminal history,
full completion/I/O receipts, factorial tables and reviewed document. Large
duplicate primary tables/runtime caches remain in the original work directory;
their complete numerical receipt and the immutable original closure bind them.
`manifest.json` hashes all archived files except itself.

Work: `/tmp/dense-v3-composed-reproduction.06yZQ4dP`, all sessions terminal.
Module SHA `d5013dba447d8affc61e547ea09d395499b3539b95468cc52aa63240640eeb11`;
launcher SHA `8a808d85508a2e31b236c251e172cac50dc186e6d20c22d1d0ef343a2f9c6a6f`.
No protected-helper access, process enumeration, stopped-controller action,
runtime upgrade, remote write or deletion occurred. No engineering narrative
or scientific result was added to or changed in the manuscript.
