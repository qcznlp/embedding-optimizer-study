# Active-v3 document component — not complete scientific admission

Observed 2026-09-07 12:48 UTC. The new document component checks actual installed
bytes, result-macro topology, an actually expanded abstract, and a fresh complete
PDF. **The complete checkpoint/factorial/release consumer is still unimplemented.**
There remain zero accepted full-horizon v3 primary runs. No synthetic result or
new constant file was installed in the real manuscript.

## Implemented and checked

- `primary_v3_manuscript.py` derives seven active constants from all twelve pinned
  v3 identities, their shared selected columns, stages and tasks. It emits no old
  discovery headlines or historical scores. This is declaration checking, not
  admission of completed training.
- All three result includes and the constant file must equal the supplied freshly
  generated bytes exactly. The component keeps the original document topology and
  engineering-narrative restrictions, requires every active result macro, rejects
  overrides/indirect file inputs/pending findings, and rechecks input identities
  after semantic inspection. The caller still must establish that the supplied
  bytes came from real full primary and routed-factorial authoring.
- The abstract is actually expanded. Unknown, recursive, duplicated, nested-only
  and parameterized findings are refused rather than counted as zero words. The
  existing nominal reservation check remains in addition to the new actual count.
  A deliberate 180-word result macro passes the old reservation check but is
  rejected by the actual-expansion check. This is an engineering counterexample,
  not an optimizer finding.
- `primary_v3_manuscript_build.py` copies only the nine exact document/bibliography/
  style/figure inputs into a new directory outside the source, and recompiles with
  shell escape disabled. It verifies the compiler's seven direct project inputs,
  actual bibliography source/style, consumed system dependencies, current PDF
  fonts, page topology and **all nine captions**. This includes the previously
  unlabeled functional-predictor table, in addition to all eight required labels.
  An old PDF/auxiliary file is not accepted as evidence of the fresh compilation.

## Evidence and its limits

The first focused text check **77334** is terminal/pass: 93 cases. The expanded
text/build-parser check **65097** is terminal/pass: 154 cases. Both complete XML
files are preserved here, with no failures/errors/skips and matching testcase
element counts. Initial source-schema assumptions were corrected before these
tests; the first new build import-order and unused-test-import lint findings and
their sources are retained under `source-first/`. No frozen numerical source,
training source, old paper audit/layout/Makefile, or actual paper source changed.

Full document call **3676** is terminal/pass. Its complete result is retained as
`complete-document-first.json`, 72,828 bytes, SHA-256
`da5c7d950e1e2c5daef1343c2c8794609ff0db853f1087f8108210d5870d8202`.
The underlying complete synthetic original/exact and functional publication is
the earlier accepted-generation population; its ten numerical outputs were not
recomputed by this document check. Factorial values are explicit presentation
fixtures, not a routed-factorial admission or bootstrap experiment.

The new compile verifies:

- actual expanded abstract: **166 conservative tokens**, with 32 in the primary
  finding and 40 in the factorial finding;
- legacy nominal reservation: 178; its primary allocation was only 30, so that
  per-macro allocation is **not** an actual upper bound (the total sample still
  fits). The new actual 200-word check supplies the required current-text bound;
- main endpoint page 7, eleven PDF pages, two main figures, one main table and six
  appendix tables, 25 embedded non-Type-3 fonts, no overflowing boxes or unresolved
  citations/references; seven direct compiler inputs and 249 system dependencies;
- visual inspection of **pages 1, 5 and 9 only**, retained under `visual/`: no
  overlap or clipping observed. The exact-sensitivity table is small but legible;
  not every PDF page was visually inspected.

The fixture has a conspicuous synthetic-results warning on its first page. The
actual manuscript remains pending and is refused before any output/compilation.
Neither a verified document nor synthetic source bytes establish the scientific
validity of prose, primary outcomes, causal claims, or final publication.

## Full regression and required next work

Whole-development regression **5462 is terminal/exit 0**, observed 13:05 UTC.
Its preserved XML reports 2,962 tests, zero failures/errors/skips, and 2,953
serialized testcase elements. The recurring nine-element count difference is
retained explicitly in [RUNNING.md](RUNNING.md). Keep the five source-current
files unchanged. No handle in this archive remains live; neither 5462 nor the
preceding 7103 regression should be polled or restarted as missing work.

The v3 factorial and reviewed release are prerequisites to the complete outer
consumer, not optional success receipts. Read-only review confirms that both the
candidate numerical policy's `require_optimizer_choice` and the primary deep
checkpoint reader's `group_contract` reject `hybrid_adamw`; simply copying old
factorial reports cannot close the branch. Its same 50K groups already have actual
revised-parent compatibility in the accepted revised-natural inputs; do not redo
the complete 500K preflight. Preserve original source states/design and old parent
bindings while implementing the v3 source linkage and routed training/completion.

Then join real primary authoring, full original/exact/functional portable replay,
actual v3 factorial evidence, this document component and the explicit committed
source/runtime transition into one strict consumer. Do not expose this component
as a release CLI or create a permanently-failing compatibility facade. Formal runs,
controller transition, commit/push, HF changes and remote publication remain gated.
