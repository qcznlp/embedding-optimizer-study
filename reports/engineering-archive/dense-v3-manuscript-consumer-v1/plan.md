# Complete active-v3 manuscript consumer

Started 2026-09-07 after the exact-publication regression completed. The preceding
goal/status turn made progress by observing owned handle 7103 to authoritative
terminal exit 0 and inspecting its complete XML. This turn preserves that result
and implements the missing active consumer, not another historical compatibility
path. There are still zero accepted full-horizon v3 primary runs.

## Required implementation

Preserve the legacy paper audit, ordinary draft build, all frozen numerical sources
and completed reconstruction parents. The explicit versioned consumer must join:

1. actual complete primary authoring and the original/exact ten-file publication;
2. externally anchored, freshly reconstructed portable evidence from local roles;
3. the full v3 routed state-by-operator continuation, including primary source-state
   linkage, fixed 50K groups, calibration, twelve resets and all sixty retained states,
   168 full-corpus cells, all probe outputs and the frozen three endpoint contrasts;
4. exact installed includes and active constants, full manuscript topology and claim
   boundaries, expanded abstract, current compiled floats/pages/fonts/source inputs;
5. the explicit reviewed assembled committed source/runtime/parent transition.

No copied historical summary, synthetic admission, hand-edited include, changed
digest, pending result, old source state or draft release flag may satisfy this
consumer. Pure numerical/component tests are distinguished from complete consumer
acceptance. Do not expose a success path with any required branch unimplemented.

The factorial and release dependencies must be real typed implementations, not
user-supplied callbacks, arbitrary success receipts or permanently-false guards.
If their integration requires an explicit new parent transition, preserve old
bindings and document that transition without authorizing or executing it.

## Authority and verification

CPU-only local preparation. No formal training, deployment, controller transition,
commit/push, HF write/deletion, or protected-helper interaction. No subagents.
Use apply_patch and new temporary output roots. Retain failing test inputs/sources.
The full goal remains active; engineering work is not a scientific result.
