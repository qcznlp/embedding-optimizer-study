# Revised inputs and actual untrained-base natural-data entrypoint

The [complete revised-data candidate](../dense-data-candidate-v1/README.md) is now observed through
the unchanged prepared Trainer contract, declared in a separate data amendment, and exercised by
three actual four-GPU diagnostic training calls. **This bounded preflight passes.** Old protocols,
data, checkpoints, failed diagnostics and manuscript remain unchanged. This is local engineering
evidence, not primary execution authorization or a scientific optimizer result.

## Actual inputs and explicit amendment

[revised-inputs.json](revised-inputs.json), SHA-256
`40b7d3e63b1c4a17b995507d5d96c042980acca96baa08f6c0c52dda66b1f1f9`, records actual generic data-reader
acceptance and the prepared Trainer's 500k-row input view. It derives twelve full-horizon identities
under new `verified-v3-` run IDs. Only the data and new namespace change: all model/source/runtime,
numerical settings and scientific hyperparameters match the preceding prepared identities.
The frozen untrained base and its original independent HF digest receipt remain the same.

The actual 4,096-row validation dataset is read. Every value in the three preserved derived subsets
matches its **revised** parent as well as its old parent; this is explicit new parent compatibility,
not deployment of the old consumers. No model is loaded or updated by this CPU preparation.

The [data amendment](data-amendment.json), SHA-256
`5051ec40c7e44ba677980fec422ed4a49ef851ef3e9fff9c29cc0f6fc3d8e4e1`, binds both parent versions,
both new datasets, all twelve identities and the exact preparation/diagnostic source closure.
It retains the parent primary evaluation/analysis rules and explicitly sets
`execution_authorized=false`. No old expected hash is replaced. The actual read-only loader
accepts the baseline and [rejects all 21 changed-file cases](amendment-admission.json), including old
input roots, missing payloads, changed quotas/rules, incomplete source closure and false release
authority. Every invalid copy remains under `/tmp/dense-amendment-admission.S6EZ7T`.

## Actual natural-data execution

The diagnostic uses the unmodified numerical/full-identity candidate at
`/tmp/dense-identity-source.8rUgLF`, immutable `lightonai/DenseOn-unsupervised@0edbd556...`,
and the same **288** original-field-preserving natural groups for all three algorithms.
The declared selector includes all six revised training groups; FIQA contributes 42 groups and
each other source contributes 41. Remaining positions use seed 20260906 and source interleaving.
This is coverage sampling, not a proportional training-distribution or optimizer-quality estimate.

The original `run_dense_natural_diagnostic.py` worker calls the actual prepared `run_training`
without patching it. AdamW 3e-5, Muon 3e-4 and NorMuon 3e-4 each start from the same untrained base
with fresh state, four ranks on devices 4–7, BF16/FA2, eight data workers per rank, microbatch eight,
accumulation four and three optimizer steps. Expected global query groups are 128/128/32.
All seven sources and all six replacements are retained. The 2,592 tokenized texts have maximum
observed length **7,679**, under the unchanged 8,192 context limit. This fixture does not itself
reach the maximum; it does not replace the earlier separate 8,192-token gradient control.
W&B is disabled. A cooperative lease protects the released project pool; original BEIR remains
stopped. No unrelated or protected helper process is inspected or signalled.

[natural-result.json](natural-result.json), SHA-256
`bf6e893e3b9e244242871227e28a3729acc17477dd3c989f7c647308bf332d36`, records three successful actual
entrypoint calls, **nine content-sealed diagnostic checkpoints**, matching final inference exports,
and complete model/optimizer/scheduler/run checks. First-step loss and gradient-norm logs are
finite and identical across the three initializations. This does not substitute for an independent
natural-batch gradient oracle or support a quality/performance comparison.

The launcher exits zero. A [fresh CPU invocation of the existing verifier](fresh-cpu-verification.json)
rechecks all saved inputs/source/artifacts and complete-run payloads successfully. This is not a
natural-data save/resume experiment, new-process-group bitwise training replay, another physical
host or an independent implementation of the reader. All earlier failed receipts remain failures.
The original FIQA-only prefix attempt is never relabelled successful: this uses a new declared
data version, sample and output root.

The retained diagnostic directory `/tmp/dense-natural-readiness.a4xZzR` contains **248 bound files /
15,283,665,146 bytes** (not counting its final receipt), including the nine checkpoints. Preserve
these payloads; only metadata is copied here. They have not been uploaded as primary artifacts.
The general Trainer's short-run throughput counters are not an accepted throughput comparison.

## Tests, handoff and remaining work

All **30 new focused tests** and **1,662 isolated regression cases** pass without failures/errors/
skips. The separate numerical candidate retains its earlier eleven unwaived old source-contract
failures among 963 tests. The actual whole-run admission is supported by the raw diagnostic files,
not inferred merely from unit tests. The final validation binds the predecessor's 32 records via
before-copies, rechecks external payloads, unchanged sources/manuscript/ledger and exact stopped
dispatchers, and briefly reacquires/releases the now-free diagnostic GPU lease.

Revised-input preparation, explicit data amendment and this bounded natural entrypoint preflight
are no longer missing work. **Next integrate a complete v3 primary execution/backup/evaluation,
validation-selection, outcome and weight-space/dimension/publication contract**, plus the separate
routed factorial control. Existing v2 primary consumers still intentionally bind old data and are
not a v3 release. Preserve the old ledger's four-step prefix, leases and artifacts during any
reviewed transition. Do not redo these completed diagnostics or merely replace old source hashes.

Formal training/deployment, the assembled committed source handoff, owner WIP-publication direction,
pending-results pre-commit gate and GitHub integration write access remain unresolved. No formal
run, original controller resume/transition, commit/push, HF write/deletion or protected-helper
interaction occurred. The old 12 runs / 60 preserved checkpoints remain on scientific hold.
Everything here is local-only engineering provenance and excluded from every manuscript section.
