# Actual functional result chain and first complete continuations

The complete [scientific readout](../../dense-v3-functional-inference-v1/README.md)
is actual corrected-primary evidence. All 61 vector and feature states are
complete; all nine task contrasts and 240 functional predictions have independent
numerical checks. No scientific kernel, old controller, helper or paper changed.

## Preserved computation history

The feature producer finished at **15:39:10 UTC**, session **47384**, exit 0
(`fb4502`). Feature manifest SHA is
`a5ef6021dc9ab535c5888e753f0a1397406a614f229894db28bd6c802f7353f5`;
coordinator completion SHA is
`29ad21b3cb6b7b90283b6da2f162472d9ac402298debcd6a17fef4e6a6750304`.

New readout session **58754** recomputed all 61 raw-vector states and passed
every native per-state CSV/NPZ comparison. Its final original context check
rejected extra statistical modules in the process import inventory, before
aggregate/inference output. Exit 1 (`426485`) and all 61 successful state
receipts remain unchanged in `original-readback-prefix/` and `entry/terminal.json`.
This was not a successful whole invocation and is not relabelled as one.

The separate successor authenticates the complete successful prefix against its
external snapshot, independently admits the original context before statistical
imports, reconstructs all four aggregate CSVs from the bound state payloads, and
passes the unchanged original before/after admission. It then calls the unchanged
estimators. No vector encoding or fourth coordinate recomputation was needed.
Session **37685** exits zero (`70ca86`); independent verifier **49034** exits
zero (`c5c7bc`). Original source guards and scientific definitions are unchanged.

The initial entry has twelve bounded identity controls. Real before/after
admission and full exact aggregate checks, not mocked native calls, validate the
successor. The 122 original raw-vector/attribution NPZ payloads remain at their
explicit paths in `retained-binary-references.json`; they were not copied into
the Git tree and are not yet a new off-host functional backup.

## First complete continuation pair and durability

Both AdamW-source branches under order seed 314159 completed all 391 steps:
reset AdamW at **15:46:23 UTC**, reset Muon at **15:46:09 UTC**. All eight actual
rank exits and both separate native whole-run reader exits are zero. All ten
checkpoint/model/moment/scheduler/four-rank-RNG payloads pass. GPU resume
equivalence and retrieval outcomes remain separate and unclaimed.

The next pair uses the Muon source, reset Muon / AdamW, same seed. At 15:53:56 UTC
it is at **56/391 and 58/391**, with all eight owned ranks live. Training
coordinators remain sessions **57000 / 45053**. Do not restart either pool or
take its transient finalization gaps for evaluation.

The new CPU/network-only backup watcher is session **16308**, PID **787110**,
started 15:46:52 UTC. Work directory:
`/tmp/dense-v3-factorial-backup-launch.75Chtt86`. Seven bounded controls pass.
The initial read-only preflight omitted PYTHONPATH and failed before outputs or
remote calls; the explicit unchanged 70-file assembly then passes actual
preflight (65757 / `bcc7f0`). Preserve both records.

Both first full runs are anonymously remote-verified in the model repository
`qcz/embedding-optimizer-study-checkpoints`, under the new prefix
`dense-v3-state-operator-v1/00c52f0bec979fb163a734ad0f9eadcd6ea09f7393133fd35c65bc62878ce11f/`:

| Run | Immutable revision |
| --- | --- |
| `factorial-v3-adamw_state-adamw-seed314159` | `a219dcab85bda040e9d46f6f7ec18ee1b0acf598` |
| `factorial-v3-adamw_state-muon-seed314159` | `e9ab737562e50a6ec0ff838f22e97301fda93d63` |

Each prefix includes `run/checkpoint-{step}`, final inference files and original
native/dispatch completion evidence. It adds only new artifacts: no old remote
path is overwritten/deleted and no WIP Python source is uploaded. Verification
compares every immutable remote size/digest and rereads the native complete run;
it does not claim a full fresh download or cross-host native resume. The watcher
continues for all twelve branches and must not be restarted.

## Archive and remaining work

`verification.json`, SHA
`cfbc928b35363235446403ba2093d0dd30b829f3a1c28cd86bcd402c2beab171`,
records 686 copied files and 122 retained binary references. Four prior ops docs
are in `before/`; original numerical sources are also retained in the scientific
report's `source-original/`. Subsequent readout/replay documentation is additional
to this fixed copied-file snapshot, not retroactively part of its count.

Next finish the remaining ten continuations and all declared outcomes; perform
functional comparator sensitivity and portable numerical replay; back up raw
functional/calibration data; then complete the source-bound paper/release gates.
No old controller, protected helper, GitHub 403 or rejected historical HF erasure
boundary was changed. The full research goal is not complete.
