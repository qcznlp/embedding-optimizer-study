# Full functional recovery-flow preparation

This is CPU-only, isolated preparation following the unanswered recovery request.
It does not grant authority, create a live authorization, start a model/GPU worker,
claim native model acceptance, or modify the original failed coordinator.

Extend the existing completed record-layout component (do not rerun its nineteen
tests) with the whole coordinator sequence: fresh namespace, exact 61-state order,
accepted-pretrained reuse, remaining sixty workers, complete vector finalization,
all-state feature producer/readback and stop-on-failure records. Execute unchanged
original worker, encode, finalize and feature bodies with explicit synthetic
dependencies. Preserve dual-descriptor routing and validation-priority bodies.

The integration candidate has exactly two coordinator edits: prepare the required
nested record parents after exclusive jobs-root creation; route pretrained through
a byte-preserving origin-authenticated reuse component. No numerical function is
edited. The reusable record observer must validate full chains, distinguish
reused pretrained evidence from new worker exits, and invoke only an injected
exact-handle observer for records without terminal receipts.

Tests cover the complete simulated sixty-new-plus-one-reused flow, all 61 feature
states, finalizer and feature failures, changed order/partial inputs, corrupted
baseline origin, changed source/authority/plan/handles, and complete nested
observation. All simulation outputs stay under a new temporary fixture root and
are explicitly synthetic. Original sources, failure and accepted-vector records
are rehashed before/after. No scientific result or manuscript change is planned.

This finite step does not deliver a production recovery launcher or deploy it.
Separate owner approval, reviewed source-bound recovery admission and authentic
native pretrained readback remain required before real dispatch.
