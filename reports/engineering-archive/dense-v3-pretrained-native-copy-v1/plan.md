# Genuine pretrained-vector relocation compatibility

CPU-only preparation, not recovery execution. Read the existing accepted pretrained
pair and its original worker/source/plan bindings. Verify all bound source inputs
and the eleven immutable pretrained checkpoint files. Use the unchanged native
checkpoint-backed vector reader on the original pair and on a byte-identical copy
under this fresh temporary directory. Compare every returned array and receipt;
only the returned absolute manifest location may differ. Do not re-encode.

Run bounded refusal controls for an incorrect external manifest anchor and a
different state plan. Preserve both original and copied vectors; do not create
any new campaign authorization, GPU lease, worker record or functional completion.
This proves only native copy compatibility for the already accepted pretrained
state, not the remaining sixty states, functional features, production recovery
admission or second-host execution. Original failed coordinator stays unchanged.
