# Complete continuation-probe durability and numerical replay

Dated evidence: **2026-09-12 21:01 UTC**, not a live heartbeat.
This goal turn made **PROGRESS**; the full NAACL/source-release goal remains active.

All **638 files / 586,119,383 bytes** are preserved at immutable public HF revision
`cff3f190e169548931fbd33eadcf1279439798e1`. The manifest is
`bcb092fe16e11150abc678a6b1e977497afbc6bf977b9dd9085cebff37004081`, under
`corrected-dense-correctness-v3/continuation-probes-v1/`.
See the [recovery guide](../../../docs/continuation-probe-restoration.md).

## Actual completed work

| Step | Original actual tool result |
| --- | --- |
| Complete source/payload/credential/raw-text screening | **75265**, terminal **64ac64**, exit zero |
| Additions-only HF upload and remote metadata hash comparison | **99623**, terminal **d1be8d**, exit zero |
| All 638 files anonymously downloaded and rehashed | **20175**, terminal **0cb11d**, exit zero |
| Copied-source numerical replay of actual downloaded data | **38605**, terminal **a166d1**, exit zero |
| Validate all sixty existing immutable checkpoint links | Terminal **34cfd3**, exit zero |

The new subtree preserves sixty complete checkpoint probes plus the pretrained
reference: **183 NPZ payloads**, all raw FP32/scoring FP16 vectors, original scores
and metrics, source/config/model bindings, actual worker exits/native verification,
and twelve original model manifest/remote-verification pairs. No full-corpus result,
example text, executable source or model checkpoint payload is newly uploaded.
The original public root and all earlier subtrees are unchanged.

The original per-state native readbacks are authenticated from their previously
completed records. No encoder, model-payload deserialization, SVD, checkpoint
training or original BEIR/probe coordinator is restarted. The standalone recovery
script itself performed the anonymous download, completing at **20:58:42 UTC**.

The numerical replay then used only that downloaded snapshot and three copied
source files, with `PYTHONPATH` empty and CUDA hidden. Both numerical functions
are byte-identical originals. All **61 score matrices**, FP32-to-FP16 projections
and **915 overall/task summaries / 5,490 metric values** reproduce exactly. No
producer package is imported; no missing task, tolerance change or historical
scientific summary supplies a substitute. The replay receipt SHA is
`ba4e764fe83b68f5a265876573a7022a989098b6d3f15ead04615091a38a59b3`.
This is numerical portability of these probe measurements, not independent
scientific confirmation, arbitrary cross-host bit identity or GPU-resume equivalence.

## Inputs and preservation

The upload used `/tmp/dense-v3-factorial-probe-durability.jLA9mqHt/backup.py`,
source `d58d4a7cc6d2c9cf774f79bf1c6daf014c319b292483963d77d6353015115134`,
preflight `8385c2ca754bd7498da074746bf7a3f49540f64e8f3671c082b546ab8ce4e284`.
Only the existing unchanged transport hashing/screening/remote comparison helpers
were reused. No old backup source/global or numerical function was patched.

All steps above are terminal. Do not rerun this backup, redownload/re-encode these
states as missing work, or retry its one-shot upload. Future result families need
their own non-overlapping addition and complete actual evidence.

This repository archive has **25 checksum-verified files / 1,030,580 bytes**;
manifest `7187391eca10184199d4b1a338030520a56a9d29be84956c18101b858efc5faf`.
It retains original tool responses, preflight/remote/download proofs, copied-source
replay and the standalone helper. The bulky NPZ files live in the public immutable
snapshot and the actual downloaded directory, not duplicate Git objects. `before/`
preserves the previous four operational handoffs. Later README/observations are
additional files, not changes to the archived inventory.

## Still active

At **21:15:13 UTC**, all eight exact registered ClimateFEVER workers are live/R;
both SciFact pilots passed, **2/168** continuation BEIR units are complete and
neither pool failed. Original inference **4529**, complete-document waiter
**65801**, and GPU-resume waiters **6666 / 9153** remain the active pipeline.
Do not alter or restart their frozen entries. No new GPU task is launched here.

Bounded log-tail reading finds no batch-progress counters; that absence is not
zero progress. All eight exact registered, nonblocking stack samples succeed
(tool terminal **38237a**, exit zero). Seven show corpus encoding/tokenization/
model forward and one shows a dataset metadata request at the sample instant.
Seven logs retain the original worker's OOM budget-halving fallback. The unchanged
worker implements that fallback itself; no agent restart or source modification
occurred. These samples are not continuous throughput or GPU-utilization evidence.
The original numerical/live bindings and authoritative manuscript hash still match.

The [final handoff](final-handoff.json) binds **17 additional files / 314,173 bytes**
under `final-observations/`, including all four current observer outputs, the
bounded log/stack samples and the updated recovery/operational documentation.
Its SHA is `978bfad620642ddfd9a6e3cc1028aaa89cf843958792d08b2d00de0275611861`.
Final preservation tool **4e6f94** exited zero and independently verified that all
25 original archive files, the live entries and authoritative manuscript remain
unchanged. These dated observations do not replace the original supervisors.

The remaining real work is complete BEIR and its original inference, actual full
paper generation/visual review, resulting data durability, queued GPU-resume
checks, full-paper/source portability and the existing tracking/package/release
requirements. GitHub 403 must not be retried or bypassed with another credential.
The authoritative manuscript, all frozen numerical/dispatch sources, protected
helper, stopped controllers and both lease namespaces remain unchanged.
