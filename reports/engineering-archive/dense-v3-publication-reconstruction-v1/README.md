# Complete original/functional publication reconstruction — engineering preparation

Both complete cold readers pass. All three raw branches, all original and
functional inference, and all seven publication outputs are reconstructed using
only the new archive's sources and local file roles. The result covers 487 output
files; the fresh child imports 83 archived package modules with the network and
original producer/source paths blocked. This is synthetic same-host relocation,
not model encoding, a real primary experiment or physical cross-host proof.

27 focused tests pass. They cover missing-primary authoring/CLI refusal, rejection
of simulated authoring, typed simulation markers, actual source-only relocation,
and ordered local-role provenance. The first full numerical audit also refuses
seven rehashed publication-output changes using only its own fresh expected bytes.
These are exact-output consumer controls, not seven additional raw reconstructions.

Whole-development regression 28989 is terminal/pass: **2,663 cases**, without
failures/errors/skips. Independent full replay **64182** also exits zero: all 487
outputs match exactly and all seven output controls are refused again. Follow
[RUNNING.md](RUNNING.md); no handle remains live. The validation.py checker requires both completed
cold results, the complete new test XML, preserved parent/source bindings and
unchanged original numerical cores. Its generated validation.json, when present
and verified, records only this bounded synthetic reconstruction acceptance.

The first fixture explicitly reuses the accepted joint fixture's complete raw
bytes. Its metadata marks upstream admission and all measurements as synthetic.
The actual archive authoring entry still starts with full checkpoint-backed
publication gather and rejects missing real runs before selecting any output.
A pure fixture/transport helper cannot supply that actual positive admission.

The [consumer review](manuscript-consumer-boundary.md) records a larger remaining
integration issue: the current paper entry still checks historical discovery and
causal-chain contracts in addition to the old two publication readers. A complete
versioned active-v3 consumer is required, preserving every current scientific gate.
The separately declared exact-feature sensitivity, routed factorial, reviewed
source/runtime transition, actual experiments and final release remain required.

No manuscript include, old numerical source or protocol was changed. Nothing was
committed, deployed, published or deleted remotely. No formal training/controller
action is authorized by this archive; all AGENTS.md restrictions remain in force.
