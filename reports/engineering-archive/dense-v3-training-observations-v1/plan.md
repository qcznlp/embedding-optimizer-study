# Complete primary training observations

2026-09-10. Produce actual descriptive telemetry for all twelve already completed
v3 primary runs while the unchanged GPU evaluation queues continue. No model,
optimizer, dataset, runtime, inference, source lock or dispatch source is changed.

Read the immutable all-twelve admission already used by the functional campaign;
authenticate the six native run metadata files and five saved Trainer-state JSON
files for every run. Do not reload model/optimizer tensors or repeat completed
training/deep-state checks. Preserve all native files and original admission flags.

Report all 4,692 logged loss/gradient/LR observations, all sixty checkpoint stages,
all sixty accepted timing segments and all twelve complete-run system records.
Use the existing descriptive trailing-ten-logged-observation summary, retain its
actual final observed step, and plot all four rates per optimizer. Do not invent
a step-3907 logged loss, interpolate raw loss timestamps, use selected runs or
compute a partial retrieval mean. Recorded gradient norms are sampled telemetry,
not an every-update clipping rate. Native full-run train_loss remains separately
labelled and is not replaced by an unweighted mean of logged windows.

Accepted timing is the existing sum of per-segment maximum-rank durations,
including checkpoint saves; do not subtract overhead or call this an isolated
optimizer-kernel benchmark. Preserve original checkpoint and optimizer byte sizes,
hardware label and four-rank count. Compare the generated systems table against
the unchanged original pure system_rows function and timings against the original
timing_summary function in an independent CPU read. Verify all raw/stage outputs
from the original metadata and a fresh local replay; inspect the generated figure.

This is a descriptive, post-observation supplement, not a newly preregistered
predictor or a change to primary inference. One run's metadata/examples were
inspected before this plan; no outcome-based selection or mechanistic claim is
made. Complete 840-cell retrieval, twelve validations, 61 functional states,
crossed continuation, final scientific consumption and NAACL/source release
remain required. No manuscript results or pending markers are changed. No public
source/data release or new HF write is part of this local measurement milestone.
