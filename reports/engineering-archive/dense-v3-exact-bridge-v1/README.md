# Exact measurement sensitivity: complete v3 bridge preparation

This milestone implements the separately named exact-feature sensitivity and its complete v3
reader. It preserves the original nine-feature bridge (`540c3c30...`) and its accepted numerical
correction (`f8a36ff6...`). New draft protocol: `270a908d...`. Every positive retrieval panel here
is synthetic; no actual primary optimizer, mechanism or useful-dimension result is produced.

## Scientific definitions remain distinguishable

The [prospective plan](measurement-plan.md) fixes five additional single-feature fits. There is no
new multivariable fit, feature selection, duplicate fit of the four unchanged nonspectral features,
or relabelling of an original feature slot.

| Added feature | Original counterpart | Interpretation difference |
|---|---|---|
| Exact nonzero-matrix saved-segment stable-rank fraction | Sketch stable-rank fraction | Accurate full-spectrum rank; denominator differs if zero matrices exist |
| Full-spectrum nonzero-matrix saved-segment entropy-rank fraction | Top-64 renormalized sketch entropy | Different entropy population and potentially a different matrix denominator |
| Exact nonzero-matrix cumulative stable-rank fraction | Cumulative sketch stable-rank fraction | Accurate full-spectrum rank; denominator differs if zero matrices exist |
| Exact saved-segment overlap to AdamW | Randomized-basis overlap to AdamW | Exact resolved projectors, complete same-stage comparator population |
| Exact cumulative overlap to AdamW | Randomized-basis cumulative overlap to AdamW | Exact resolved projectors, complete same-stage comparator population |

Every original/exact correspondence is reported. These are measurement-sensitivity comparisons,
not always pure approximation-error comparisons: full entropy and nonzero weighting are explicitly
different estimands. All denominator/status masses are retained. Muon/NorMuon use all four AdamW
rates; an AdamW cell uses the other three, never itself. If any required complete pair overlap is
undefined, the whole focal mean is undefined. Neither a resolved-only conditional overlap nor an
available-only average can silently stand in for it.

Missing primary rows/columns are errors. A legitimately undefined feature value keeps its row and
reason; its complete augmented prediction and association are undefined. The exact baseline
predictions remain available. There is no fit on only the convenient rows or folds.

The named-family helper uses the accepted exact rational OLS/MSE and rank-resolution policy. It
takes authentic original columns plus distinctly named additions; no original feature is filled,
overwritten or renamed to route another predictor. When given the original nine names, every
numerical record exactly matches the unchanged accepted nine-feature implementation.

## Complete input and output interface

`embed_optim.primary_v3_exact_bridge` first uses the original v3 bridge's full primary/validation/
BEIR/geometry admission and recomputation. It then verifies that actual original bridge bundle,
recomputes the exact-geometry bundle from admitted checkpoints, joins the full population and
rechecks all raw/table/source bindings after calculation. Missing primary input causes both actual
CLI modes to refuse before output. The unchanged original and exact geometry consumers are reused;
no held diagnostic or old training result is relabelled as eligible primary evidence.

The new bundle retains 60 augmented input rows, 20 fold rows, five summaries, five residual
association rows and 300 held-out predictions. It also includes all 300 row-level measurement
comparisons and five complete predictive-sensitivity comparisons with exact error differences.
Coverage and numerical diagnostics are authenticated in its evidence JSON. A changed and rehashed
CSV cannot self-authenticate because the reader recomputes every expected value from admitted inputs.

## Verification scope

All **48 final focused / 2,153 full-suite tests pass**, with zero failures/errors/skips. The initial
23-case positive receipt is preserved too. Tests cover original-family exact equivalence, full
rate/stage/comparator populations, denominator consistency, missing/undefined distinctions,
conditional-mean refusal, malformed numeric CSV, changed protocols and rehashed bundles. The
end-to-end wiring fixture explicitly simulates upstream model admission; it is not real primary data.
The separate training candidate's eleven old source-contract failures remain unwaived.

The [independent audit](rehearsal.json) (`a80544e0...`, 22:35 UTC) solves all twenty full augmented
systems using SymPy, independently of the production rational FWL implementation. All **300
held-out predictions, 20 fold MSE comparisons and five pooled decisions** match exactly. All ten
Pearson/Spearman values match independent exact-residual references. The
[fresh process replay](fresh-replay.json) (`8096eea0...`) repeats the complete tables, exact error
values, coverage, diagnostics and boundary controls. Each process rejects four altered/rehashed
bundles and ten altered protocols; both actual missing-primary CLI modes refuse without output.

The real-data check is deliberately smaller and has no retrieval outcomes: it authenticates the
already independently accepted exact-geometry table for three short diagnostic trajectories/nine
checkpoints, then verifies 27 named-field mappings (18 defined and nine undefined values). It does
not repeat full SVD, create a primary identity or invent BEIR scores. The incomplete three-run
diagnostic population is rejected as a primary AdamW-comparator population.

## Dimension-path numerical boundary

The old dimension publisher (`1120d125...`) has a separate floating OLS implementation. The same
fixed binary-exact null example, assigned to its four distinctly named dimension predictors,
reproduces a rounding-only usefulness flag in all four slots. These are **four copies of one
counterexample**, not four independent scientific findings. The complete old outputs are retained
in the audit receipt. The new named-family helper gives exact zero gain/false usefulness without
altering any original weight-feature value or dimension predictor name.

This verifies a reusable numerical component only. **The primary dimension export, ablation,
rotation, publication and release chain is not yet integrated with v3 inputs or this correction.**
The old dimension code/protocol is unchanged. Do not claim that testing four synthetic predictors
has completed functional-dimension analysis or that coordinate utility explains optimizer quality.

Preserve `/tmp/dense-exact-bridge-preparation.VGv9YL`,
`/tmp/dense-v3-exact-bridge-audit.XoMIzZ` and `/tmp/dense-v3-exact-bridge-replay.gJuW7Z`.
[commands.md](commands.md) gives reproduction commands; [validation.json](validation.json) binds
the final archive and checks parent/source/ledger/manuscript preservation. All owned calls exited.
No formal run, GPU worker, controller transition, commit/push, HF mutation or protected-helper
action occurred. Every implementation incident remains outside every manuscript section.

## Next work

Original and exact-feature bridge preparation are now complete; do not repeat those diagnostic
campaigns as missing tasks. Integrate v3 functional-dimension exports, complete interventions and
publication/reconstruction, retaining the fixed 224-query shortlist, all shared masks and rotations,
and the full-corpus claim boundary. Then finish separately routed factorial and reviewed assembled-
source/runtime/release-parent handoff. Real valid primary training, the 840-unit BEIR grid, mechanism
findings and a releasable NAACL paper remain uncompleted. Geometry alone is not the scientific story.
