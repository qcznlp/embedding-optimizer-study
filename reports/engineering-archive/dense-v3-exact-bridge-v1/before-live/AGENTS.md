# Agent handoff instructions

## V3 bridge correction and input integration — 2026-09-06 22:10 UTC

Read [/root/embedding-optimizer-story-refactor/PROJECT_STATUS.md](/root/embedding-optimizer-story-refactor/PROJECT_STATUS.md)
and its [bridge archive](/root/embedding-optimizer-story-refactor/reports/engineering-archive/dense-v3-bridge-v1/README.md).
The original-feature v3 bridge is now prepared under a separate exact-arithmetic/numerical amendment.
Both old null counterexamples give exact zero gain and undefined residual correlation in the new
consumer; the old function/protocol/evidence remain intact. The same nine feature values, baseline,
folds and mathematical support rule are retained. Undefined held-out extensions or unresolved
directions are never silently pooled away. Complete input admission precedes every output.

All 54 focused / 2,105 isolated tests pass. Independent full systems match 540 exact held-out
predictions, 36 fold errors, nine pooled decisions and eighteen associations; fresh replay passes.
All positive panels are synthetic. Preserve the initial independent reference Boolean-conversion
failure and source/partial outputs, plus the reference-only correction and exact rank controls.
The distinct numerical candidate's eleven old source-contract failures remain unwaived.

Bridge preparation is no longer missing. Next integrate separately declared exact-measurement
sensitivity and functional dimension/export/publication, routed factorial and reviewed assembled-
source/runtime/release-parent handoff. Do not relabel the frozen nine features to route a different
family. No formal run, GPU worker, controller transition, commit/push, HF mutation or protected-helper
action occurred. Original numerical cores, manuscript, stopped chain/ledger/lease, old 12/60
scientific hold and owner/access/approval gates remain. Engineering incidents enter no paper section.
Older headers are history where superseded.

## Complete exact geometry; unrepaired bridge boundary — 2026-09-06 21:40 UTC

Read [/root/embedding-optimizer-story-refactor/PROJECT_STATUS.md](/root/embedding-optimizer-story-refactor/PROJECT_STATUS.md)
and its [exact-geometry archive](/root/embedding-optimizer-story-refactor/reports/engineering-archive/dense-v3-exact-geometry-v1/README.md).
The complete separate exact consumer is prepared: all 88 hidden matrices / nine real diagnostic
checkpoints give 1,584 independently verified matrix/anchor records and complete fresh CPU replay.
All 45 focused / 2,051 isolated tests pass; the distinct numerical candidate's eleven old source
failures remain. The full primary positive fixture is synthetic, not a new scientific result.

A source-fixed synthetic counterexample confirms an unrepaired numerical boundary in the old
retrieval bridge: a baseline-redundant feature triggers its usefulness rule from a roughly 1.44e-16
RMSE rounding difference, and a zero theoretical residual receives finite correlations. The initial
case does not trigger support; nine repeated feature slots are one pathology. Preserve both cases
and all old sources. First declare and verify the numerical/identifiability correction, then integrate
v3 bridge/exact sensitivity, dimensions/publication and the reviewed assembled-source handoff.
Exact-geometry preparation itself is no longer missing; do not repeat completed diagnostic campaigns.

No formal training, GPU worker, controller transition, commit/push, HF mutation or protected-helper
action occurred. Original numerical cores, manuscript, stopped chain/ledger/lease, old 12/60
scientific hold and owner/access/approval gates remain. Engineering incidents belong in no
manuscript section. Older headers below are historical where superseded.

## Exact geometry diagnostic handoff — 2026-09-06 20:34 UTC

Read `/root/embedding-optimizer-story-refactor/PROJECT_STATUS.md` and its
`reports/engineering-archive/dense-geometry-robustness-v1/README.md`. Exact spectra/projectors on
twelve inherited short diagnostic matrices pass independent NumPy/PyTorch checks and fresh
recomputation; all 348 arrays repeat and four altered artifacts reject. Original rank-16 sketch
overlap with exact spaces is only about 0.13–0.27 for Muon/NorMuon, versus 0.90–0.94 for AdamW.
A 108-pair post-hoc shared-probe check and fresh replay show same-seed Muon/NorMuon means
0.63–0.92, different-seed 0.063–0.077, exact 0.30–0.81. Different seeds alone are not an accuracy fix.
These are measurement diagnostics, not primary findings. All 2,006 isolated tests pass; the distinct
numerical candidate's eleven source-contract failures remain. Preserve the pre-protocol unit-test
failure and revised floating-point reference, all frozen features and all old evidence.

Next prepare explicit exact/error-controlled primary geometry and integrate v3 bridge/dimension/
publication, routed factorial and assembled-source/runtime/release-parent handoff. Do not repeat
completed projector diagnosis or silently rewrite frozen approximate bridge features. No formal
training, GPU worker, controller transition, commit/push, HF mutation or protected-helper action
occurred. Original numerical cores, manuscript, stopped chain/ledger/lease and old 12/60 scientific
hold are unchanged. Existing owner/access/approval gates remain; engineering incidents enter no
manuscript section. Older headers below are historical where superseded.

## Complete v3 geometry handoff — 2026-09-06 19:55 UTC

Read `/root/embedding-optimizer-story-refactor/PROJECT_STATUS.md` and its
`reports/engineering-archive/dense-v3-geometry-chain-v1/README.md`. Complete v3 geometry production,
all-rate/stage summaries and numeric/basis readback are prepared. Initial global FP32 norm checks
failed independently despite kernel agreement; source/protocol/outputs are preserved. A separate
v2 correction uses FP64 global norms/cosines only, retaining ranks/seeds/spectra and all tolerances.
Actual retry checks 792 diagnostic matrix records, 2,112 norms, 1,056 cosines, 264 undefined cases
and twelve full-SVD controls. Fresh CPU replay passes; 8 altered protocols and 2 real rehashed
outputs reject. Both primary CLI actions refuse absent full v3 runs. Final 64 focused / 1,955
isolated tests pass; the distinct numerical candidate's eleven source-contract failures remain.

No primary geometry or optimizer conclusion exists. Diagnostic Muon/NorMuon sketch stable ranks
exceed exact controls by about 9–12%; add exact-spectrum/projector robustness before interpreting
absolute ranks/alignments. Do not alter frozen bridge features silently. Next integrate v3 bridge/
dimension/publication, routed factorial and reviewed source/runtime/parent handoff, not another
geometry-adapter rewrite or repeated norm diagnosis. All owned calls exited; no formal/GPU run,
controller transition, publication, HF mutation or protected-helper action occurred. Old sources,
paper, stopped chain/ledger/lease and 12/60 scientific hold are unchanged. Existing owner/access
gates remain. Engineering incidents belong in no manuscript section. Older headers are history.

## Changed-entry measurement handoff — 2026-09-06 18:54 UTC

Read `/root/embedding-optimizer-story-refactor/PROJECT_STATUS.md` and its
`reports/engineering-archive/dense-v3-geometry-v1/README.md`. A separate draft component corrects
the matrix-mass proxy to exact individual saved-entry counts. Real CPU inspection of 3 diagnostic
trajectories / 9 checkpoints passes 1,584 independent NumPy counts, then fresh-process replay;
six changed censuses and six changed amendments reject. Old source/results remain unchanged.
The base-symlink preflight failure is preserved; an authenticated identical ordinary-file copy
passes without relaxing the check. Final 45 focused / 1,891 isolated tests pass; the relative-path
test invocation's two wrong-checkout imports and unchanged absolute-path retry are both retained.
Always use absolute isolated PYTHONPATH entries. Separate candidate eleven failures remain.

This is only the changed-entry component. Next integrate complete v3 spectral/subspace geometry,
dimension/retrieval/publication, routed factorial and reviewed assembled-source/runtime/parent
handoff. Counts do not establish useful dimensions or an optimizer finding. The actual primary
CLI refuses missing full v3 runs. All owned calls exited; no formal run, controller transition,
publication, HF mutation or protected-helper action occurred. Original numerical cores, manuscript,
stopped BEIR chain/ledger/lease and old 12/60 scientific hold remain unchanged. Owner WIP/deployment
direction and existing access/approval gates remain. No part of this engineering milestone belongs
in the manuscript. Older headers are preserved history.

## V3 outcome handoff — 2026-09-06 18:13 UTC

Read `/root/embedding-optimizer-story-refactor/PROJECT_STATUS.md` and its
`reports/engineering-archive/dense-v3-outcomes-v1/README.md`. V3 validation, complete-grid and
statistical/system consumers are connected in preparation. The first fresh-process CSV replay
failure is preserved; a separate v2 serializer revision fixes column order without changing values,
the statistical kernel or any criterion. The actual independent scalar/CPU retry passes and
18 changed cases reject. The real primary CLI refuses absent full v3 runs; every positive table
here is synthetic. Final 69 focused / 1,846 isolated tests pass; the different candidate still has
eleven unwaived source-contract failures.

Next integrate weight-space/dimension/publication, routed factorial and reviewed assembled-source/
runtime/parent handoff. Do not repeat completed outcome checks or merely change old hashes/status.
WIP-commit and post-acceptance twelve-run rerun questions were sent again; no answer has arrived.
All owned calls exited. No GPU/formal run, controller transition, publication, HF mutation or
protected-helper action occurred. Original numerical cores, paper, stopped BEIR chain/ledger/lease
and old 12/60 scientific hold remain unchanged. Existing authority/access boundaries remain.
This local engineering milestone belongs in no manuscript section. Older headers are history.

## V3 validation handoff — 2026-09-06 17:49 UTC

Read `/root/embedding-optimizer-story-refactor/PROJECT_STATUS.md` and its
`reports/engineering-archive/dense-v3-validation-v1/README.md`. V3 validation scoring/readback and
full-grid recipe selection are implemented under a separate preparation-only protocol. Complete
4,096-row input admission passes. Three existing diagnostic models score the same declared 59
rows covering all 52 replacements and seven sources; scores/metrics match unchanged helpers and
all 134 parameter values remain unchanged. Actual maximum length is 1,379, not 8,192.
Fresh CPU readback accepts three bundles; 12 changed protocols and six rehashed semantic
corruptions reject. The actual selector refuses missing full v3 runs and produces no recipe.
All 67 focused / 1,777 isolated tests pass; the separate candidate's eleven source failures remain.
Next integrate v3 outcome/dimension/publication, routed factorial and reviewed assembled-source
handoff, not another validation rewrite or old-hash refresh. All owned calls exited. No formal
training, controller transition, source publication, HF mutation or protected-helper interaction
occurred. Original numerical cores/paper/stopped chain/ledger/lease and old 12/60 scientific hold
remain unchanged. Existing owner direction and access/approval gates remain. Local engineering
evidence is excluded from every manuscript section. Older headers are preserved history.

## V3 core-chain rehearsal handoff — 2026-09-06 16:49 UTC

Read `/root/embedding-optimizer-story-refactor/PROJECT_STATUS.md` and its
`reports/engineering-archive/dense-primary-v3-chain-v1/README.md`. The separate v3 core train/backup/
BEIR/whole-grid consumers are implemented and remain preparation-only. Actual CPU rehearsal accepts
two revised data partitions and twelve CLI plans, rejects 68 old-input/checkpoint, diagnostic and
draft-execution cases, and refuses the absent v3 primary grid. No v3 primary run is trained.
All 48 focused / 1,710 isolated tests pass; the different numerical candidate's eleven old source
failures remain unwaived. Next integrate v3 validation scoring/selection, outcome/dimension/
publication and the separate routed factorial control, then review the assembled-source handoff.
Do not repeat completed data/natural/core checks or replace old hashes. All owned calls exited.
Original sources/paper/stopped BEIR chain/ledger/lease and 12/60 scientific hold are unchanged.
No formal/GPU run, controller transition, publication, HF mutation or protected-helper action
occurred in this CPU milestone. Existing owner direction and access/approval gates remain.
This local-only engineering evidence belongs in no manuscript section. Older headers are history.

## Revised natural-data entrypoint handoff — 2026-09-06 16:34 UTC

Read `/root/embedding-optimizer-story-refactor/PROJECT_STATUS.md` and its
`reports/engineering-archive/dense-revised-natural-v1/README.md`. Actual revised 500k/4,096 inputs
have twelve full identities and three revised-parent subset checks. A new explicit data amendment
is preparation-only; its actual loader accepts the baseline and rejects 21 changed-file cases.
Three actual four-GPU runs from the untrained base complete three steps each on the same declared
seven-source/288-group fixture, including all six training replacements. Nine complete diagnostic
checkpoints/final exports and fresh CPU readback pass. Actual maximum length is 7,679, not 8,192.
Preserve `/tmp/dense-natural-readiness.a4xZzR` (15.28 GB plus receipt). No full epoch/all-rate campaign,
natural-gradient oracle or natural-data resume was tested. All 30 focused / 1,662 isolated tests
pass; the distinct numerical candidate's eleven old source-contract failures remain unwaived.
Next integrate complete v3 primary/validation/downstream and routed-factorial contracts. Do not
repeat completed data/natural diagnostics or merely refresh old hashes. All owned calls exited;
old numerical cores, paper, exact stopped BEIR chain/ledger/lease and 12/60 scientific hold remain.
No formal run, original-controller transition, publication, HF mutation or protected-helper action
occurred. Owner WIP/deployment direction and existing publication/access gates remain unresolved.
This local-only engineering milestone is excluded from every manuscript section. Older headers
are historical where superseded; no formal execution is authorized by this preparation receipt.

## Revised data preparation handoff — 2026-09-06 15:44 UTC

Read `/root/embedding-optimizer-story-refactor/PROJECT_STATUS.md` and its
`reports/engineering-archive/dense-data-candidate-v1/README.md`. The 6-training/52-validation-group
revision is now prepared separately and independently reconstructed in full. All non-target
values, totals and quotas are preserved; actual ID/content/query-isolation checks pass. Original
data is unchanged. Prepared data: `/tmp/dense-partition-candidate.kHXoGW`; independent generator-
writer reconstruction: `/tmp/dense-partition-reconstruction.HsE86I`. Preserve both and the archived
interrupted CPU attempt, whose 50,628 decisions match the completed retry's prefix byte-for-byte.
All 26 current focused and 1,632 isolated tests pass; the distinct numerical candidate's eleven
unwaived source-contract failures remain. Next prepare an explicit data/consumer amendment and
seven-source natural-data entrypoint checks, not another diagnosis or old-hash refresh.
All owned calls exited; no GPU/formal run, controller transition, publication, HF mutation or
protected-helper action occurred. The exact old stopped BEIR chain, numerical cores, manuscript
and 12-run/60-checkpoint scientific hold remain unchanged. Existing owner publication/deployment
direction and access/approval gates remain unresolved. This local milestone is not paper content.
Older "no replacement data" notices are superseded; no formal execution is authorized here.

## Exact query-partition handoff — 2026-09-06 15:12 UTC

Read `/root/embedding-optimizer-story-refactor/PROJECT_STATUS.md` and its
`reports/engineering-archive/dense-data-partition-v1/README.md`. Independent actual-string replay
confirms 84 train–validation pairs affecting 51 validation queries, plus one train–BEIR query pair.
All are raw-text equal; earlier source-qualified ID disjointness remains true, but does not prove
query-text isolation. The expanded proposal replaces six training and 52 validation groups while
preserving all other row values and source quotas. No replacement query or revised data exists.
Next implement/review original-population seeded selection and independent materialization;
protect original train/validation IDs/texts and all fourteen-task BEIR query texts. Do not rewrite
old locks/evidence, use the insufficient six-empty-group plan or select by optimizer outcomes.
All nine new focused and 1,606 isolated tests pass; the different candidate's eleven old source
gates remain unwaived. No GPU/formal run, controller transition, publication, HF mutation or
protected-helper interaction occurred. Existing scientific/authority holds remain in force.
This is local engineering provenance only and must not enter the manuscript. Older headers are
historical where superseded by this handoff and the canonical isolated status.

## Real-data preflight handoff — 2026-09-06 14:32 UTC

Read `/root/embedding-optimizer-story-refactor/PROJECT_STATUS.md` and its new
`reports/engineering-archive/dense-natural-readiness-v1/README.md`. Full actual row-ID linkage
passes, but five training groups and one frozen-validation group contain empty source document
text. Three upstream document origins are authenticated; query partitions remain disjoint.
The fixed natural-data prefix failed before any GPU lease/worker/model. No new training ran.
All 59 focused / 1,597 isolated tests pass; actual data eligibility and the separate candidate's
eleven source-contract failures remain unresolved. Next prepare/review the uniform six-group
data amendment while protecting held-out IDs; no replacement data exists yet. Do not weaken
the gate, skip the failure, rewrite old manifests or infer scientific results.
Original sources/checkpoints/BEIR chain and ledger remain preserved; no formal launch, controller
transition, publication, HF mutation or protected-helper interaction occurred. Authority and
publication/access boundaries below remain unchanged. This engineering milestone stays out of
the manuscript; the canonical isolated status supersedes older milestone headers.

## Whole-run/task/grid reader handoff — 2026-09-06 12:42 UTC

Read `/root/embedding-optimizer-story-refactor/PROJECT_STATUS.md` and the isolated
`reports/engineering-archive/dense-primary-completion-v1/README.md`. Whole-run, exact-task and
840-cell readers are implemented in a new source-bound proposal. Three complete diagnostic
baselines / nine full-model checkpoints pass; three continuation-only directories are rejected
as whole runs. One real SciFact diagnostic worker completed on a single leased GPU. Its unchanged
output passes the CPU reader, with undefined unused nAUC explicitly recorded and nDCG unchanged.
Original failures are preserved; the original scientific nDCG reader accepts the same raw file.

All 63 focused and 1,538 isolated tests pass; the separate candidate still has its earlier eleven
unwaived source-contract failures among 963 cases. The actual new full-grid CLI refuses missing
new primary runs. This is not formal training, primary evaluation or a scientific result. Next
prepare natural-data readiness and integrate validation/outcome/mechanism/publication consumers
with the new identity; retain the separate routed-factorial and runtime-handoff requirements.

All owned diagnostics exited; the old three-handle BEIR chain, ledger/lease and four-step prefix
remain stopped/preserved. Numerical cores, old protocols/checkpoints and manuscript are unchanged;
primary counts stay 12/12 and 60/60 on scientific hold. No formal launch, original BEIR resume,
controller transition, commit/push, GitHub write retry, HF write/deletion or protected-helper action
occurred. Owner WIP/deployment direction, the pre-commit gate and integration 403 remain unresolved.
The milestone is local only and must not enter the manuscript. Older headers are historical where
superseded by the canonical isolated status page.

## Primary contract prepared and rehearsed — 2026-09-06 11:48 UTC

Read `/root/embedding-optimizer-story-refactor/PROJECT_STATUS.md` and its
`reports/engineering-archive/dense-primary-contract-v1/README.md`. The actual common 500k data
and untrained base are authenticated; twelve full recipe identities and new shared primary
train/backup/evaluation consumers are prepared locally. The proposal remains a draft. Real
read-only rehearsal accepts twelve diagnostic checkpoints only as diagnostics and rejects sixty
old primary checkpoints plus three corrupt copies. Twelve actual CLI inspection plans and 51
focused tests pass; all 1,475 isolated tests pass. This does not clear the separate numerical
candidate's eleven unwaived source-contract failures among 963 tests.

Next finish whole-run/task completion gates, downstream analysis/publication integration,
natural-data readiness and the evidence-preserving runtime handoff. Do not repeat the resolved
missing-identity diagnosis, adopt old scores or bypass the factorial control's separate integration.
No formal execution is authorized by the draft. The owner deployment/WIP-publication questions,
pending-results pre-commit rule and previous GitHub integration 403 remain unresolved.

All diagnostics exited. Original numerical source, checkpoints, paper, old protocols and the
stopped three-handle BEIR chain/ledger/lease/four-step prefix are unchanged. Primary remains 12/12
executed and 60/60 preserved, on scientific hold. No formal training, upload, evaluation, runtime
transition, commit/push, GitHub write retry, HF deletion or protected-helper interaction occurred.
The milestone is local only; nothing here belongs in the manuscript. Older headers are history
where superseded by this update and the canonical isolated status page.

## Complete run identity integrated — 2026-09-06 10:58 UTC

The missing full-run admission gate is now implemented in a distinct prepared checkout,
`/tmp/dense-identity-source.8rUgLF`. Read the current isolated `PROJECT_STATUS.md` and
`reports/engineering-archive/dense-full-identity-v1/README.md`. The previous numerical candidate
`/tmp/dense-correction-source.0YHywN` and every original core file remain unchanged.

Actual four-GPU `run_training` now completes three baselines and three native checkpoint-2
continuations with byte-identical relocated data AND initial models. All twelve new diagnostic
checkpoints have complete 20-file payload seals, including all four rank RNG files. Final exports
equal scheduled weights. The subsequent CPU probe authenticates 258 real producer files; six
original/relocation controls accept, 36 changed recipe/data/runtime cases reject, and three one-byte
corrupted real optimizer copies reject before deserialization. No changed-recipe run was trained.
Do not keep treating complete identity as an unintegrated comparator or repeat that missing-gate
diagnosis. No identity/seal was retrofitted into old checkpoints.

The current candidate passes 79 focused cases; its full 963-case suite has the same 11 unwaived
old source-contract failures, no new full-suite failure and no skipped test. The isolated paper/
audit suite passes all 1,424 cases. Those are different source trees. The old 24/24 independent-
process bitwise receipt belongs to the numerical parent, not this new save/load integration.

Next prepare/review new primary execution, reload, durability and downstream contracts, natural-
data readiness and the evidence-preserving runtime handoff. Do not merely update eleven hashes,
reuse old scientific results, or bypass the factorial routed operator's separate integration.
Actual input/source-derived identity and payload integrity are implemented, but are not digital
signatures or a substitute for the independent trusted-digest remote audit and new protocol review.

All owned diagnostics exited. Preserve their temporary checkpoints, relocated input copies and
corrupted diagnostic copies; the latter are NOT valid continuation inputs. BEIR's exact original
three handles remain stopped, with unchanged ledger/lease/completed prefix. Primary status is
still 12/12 executed and 60/60 preserved, on scientific hold. No implementation detail belongs in
the manuscript. This milestone is local only: no deployment, formal training, source publication,
evaluation resume, HF deletion or protected-helper interaction occurred. Owner deployment/WIP-
publication questions remain unanswered; the pre-commit pending-results rule and prior integration
403 are not waived or bypassed. Older headers are preserved history where superseded.

## Predeployment verification — 2026-09-06 10:29 UTC

The exact prepared candidate now passes **24/24 new-process-group controlled replay comparisons**,
resuming the sealed baseline files without training another baseline. Its numerical source remains
unchanged. Read the current isolated `PROJECT_STATUS.md` and
`reports/engineering-archive/dense-predeployment-checks-v1/README.md`.

The actual numerical-only preload gate leaves a separate admission gap: 30 changed recipe/data/
runtime cases pass it, while six optimizer/accumulation controls reject. The probe stops before
setup/model loading or updates; this is not proof of historical configuration drift. A complete
identity proposal passes all 42 comparisons, including byte-identical data relocation, but it is
**not integrated into the actual Trainer/launcher** and no complete identity was retrofitted into
old checkpoints. Both admission campaigns retain exit 1 for the missing readiness condition.

All 1,415 isolated paper/audit regression cases pass. The different candidate suite remains at
923 cases with 11 unwaived frozen-source failures; do not conflate those results. The full replay
uses explicit diagnostic communication/backward controls, not default-DDP or cross-host guarantees.

Next derive and seal full actual recipe/data/runtime/source identity at run creation, bind every
checkpoint, and fail on mismatch before any resume side effect. Verify the real persistence and
admission path, then review new primary/downstream contracts and the evidence-preserving handoff.
Do not redo the completed origin/fresh-process probes or merely rehash old source locks. The
routed factorial control still needs its own integration.

All owned diagnostics exited. The original three stopped BEIR handles, ledger/lease/completed
prefix, six live numerical files and 60 primary checkpoints remain unchanged. The 12 completed
primary runs are still on scientific hold. No implementation incident belongs in the manuscript.
The milestone is local only; GitHub integration 403 was not retried or bypassed. Owner deployment/
development-publication questions remain unanswered. Do not deploy/retrain, commit/push WIP,
resume evaluation, use the obsolete zero-step migration, delete HF artifacts or touch the protected
GPU helper. Older milestone instructions below are preserved history where superseded.

## Integrated numerical correction prepared — 2026-09-06 09:40 UTC

The new detached candidate at `/tmp/dense-correction-source.0YHywN` implements explicit single
normalization, owned deterministic attention backward, official-additive NorMuon and numerical
checkpoint contracts. It passes 96/96 exact NorMuon updates on CPU and GPU separately, with Muon
unchanged; 9/9 short and 9/9 maximum-length full-gradient checks; 24/24 controlled same-group
checkpoint replay checks; and three real baseline plus three continuation `run_training` calls.
Read the current isolated `PROJECT_STATUS.md` and
`reports/engineering-archive/dense-correction-candidate-v1/README.md`.

Do not conflate checkouts: the candidate's full suite has 923 cases and **11 unwaived old source-
contract failures** (six actual compatibility failures were fixed); the isolated paper/audit suite
passes all 1,382 cases. The older control's independent-process result does not certify the new
source. Numerical receipts do not substitute for a full recipe/data/source resume contract.
The new source remains undeployed, uncommitted and unpublished.

All four candidate GPU diagnostics exited zero. Existing numerical source, primary checkpoints,
the three paused BEIR handles, ledger/lease and completed prefix remain unchanged. Old 12/12 runs
and 60/60 preserved checkpoints remain on scientific hold. Preserve both generations of candidate
source/evidence and all new synthetic checkpoints/fingerprints in their named temporary roots.
No implementation incident belongs in any manuscript section.

Next follow the new transition proposal: finish the remaining predeployment checks, review new
primary/downstream source contracts and the evidence-preserving runtime handoff, resolve owner
deployment/WIP-publication gates and GitHub integration write access, then execute the accepted
primary revision. Do not merely rehash the eleven old locks or reuse their scientific outputs.
The routed factorial AdamW control needs separate integration with the new primary-only policy.
The latest issue comment/update attempts were already rejected with integration 403; this local
milestone is not on GitHub, and no alternate credential/identity bypass is allowed.

No formal relaunch, unsafe old zero-step migration, unapproved commit/push, evaluation resume,
HF deletion or protected GPU-helper interaction is authorized by this header. All existing gates
remain in force. Older milestone instructions below are preserved history where superseded.

## Controlled GPU replay accepted — 2026-09-06 08:32 UTC

The isolated canonical-reduction + deterministic-backward control now passes all 9 short-input and
all 9 maximum-length full-gradient checks under the old tolerance, plus 24/24 strict same-group
and 24/24 new-process-group checkpoint comparisons. All 1,367 regression cases pass. Read the
current `PROJECT_STATUS.md` and
`reports/engineering-archive/dense-canonical-replay-v1/README.md` for exact scope and receipts.
The new process group resumes sealed files without another baseline; all later state/gradients,
data rows and per-rank RNG are checked. This closes the bounded replay-control task.

This is an explicit diagnostic intervention, not a production/performance recommendation.
The separate production double-normalization defect and NorMuon small-matrix epsilon
nonconformance remain unresolved there. All old failures are preserved. The 12 completed runs
and 60 preserved checkpoints remain on scientific hold; do not promote them into paper evidence.
No implementation incident belongs anywhere in the manuscript.

All four diagnostic launchers exited zero. The original three BEIR handles remain stopped with
the unchanged ledger/lease/completed prefix, and all six live numerical sources are unchanged.
Keep the 153 baseline checkpoint files (~13.48 GB), 36 fingerprint files (~78 MB), new continuation
checkpoints and earlier gradient payloads in their named temporary trees; none were uploaded.

Next: prepare the minimal normalization/reference correction and reviewed state-preserving
replication transition. Do not automatically adopt one-vector communication in formal training,
repeat the resolved origin diagnosis, run the obsolete zero-step migration, deploy/retrain,
resume BEIR, publish WIP source or delete HF artifacts. The existing publication/approval gates
below remain unchanged. Older milestones below are preserved history, superseded by this header
and the current status page where their counts or next steps differ.

## First-gradient origin — 2026-09-06 07:39 UTC

The isolated paired probe now explains the short-input replay discrepancy: local gradients are
identical, while changing DDP reduction layout from 16 buckets to one exactly reproduces the
observed difference. Default and deterministic-backward controls agree and match the previous
capture. This is floating-point reproducibility evidence, not a new serialization/optimizer bug
or paper result. The independent production double-normalization defect and scientific hold remain.
Read `/root/embedding-optimizer-story-refactor/reports/engineering-archive/dense-gradient-origin-v1/README.md`.
All 1,344 regression cases pass; both diagnostic launchers exited. BEIR remains stopped through
the original exact chain/lease, and numerical source, checkpoints and ledger remain unchanged.
Next use only an isolated fixed-order reduction/fresh-process restoration control; no formal
deployment, retraining, WIP publication or HF deletion is implied.

## Narrower replay result — 2026-09-06 07:10 UTC

The isolated real-GPU check now reproduces all 24 actual optimizer-plus-scheduler updates and
12 same-gradient controls bitwise exactly. Small first-gradient differences spread through BF16
conversion and Newton–Schulz on this fixture; their initial source remains unisolated. This is
engineering evidence, not an optimizer-quality result or acceptance of the still-failed full replay.
The detailed archive is under `/root/embedding-optimizer-story-refactor/reports/engineering-archive/dense-gpu-replay-localization-v1/`.
All 1,337 absolute-path regression cases pass. Diagnostic launchers have exited, BEIR remains
paused through the same exact retained chain, and live numerical source/checkpoints/ledger remain
unchanged. Keep the scientific hold and all deployment/publication/deletion boundaries below.

## Current operational/scientific override — 2026-09-06 06:29 UTC

The owner renewed work with correctness/scientific validity as the first priority. All 12 primary
training runs have finished and all 60 checkpoints are preserved, but **scientific promotion is
on hold**. Real four-GPU BF16/FA2 checks confirm duplicate accumulation normalization in unchanged
training source. An isolated candidate passes short-input tests; long-input kernel reproducibility
and GPU replay differences have explicit failed/control receipts, not blanket acceptance.

BEIR was paused safely at 05:56 UTC. Its original three CPU dispatchers remain stopped in place,
retaining their lease and four completed steps; their eight verified leaf workers exited. No
checkpoint, production numerical source or live ledger byte was changed. Do not launch a competing
controller, invoke an old zero-step migration, resume evaluation into a diagnostic pool, or use
broad process/process-group operations. Preserve the exact handles and creation times at
`/root/embedding-optimizer-story-refactor/reports/engineering-archive/dense-gpu-correctness-v1/handoff/before.json`.
All GPU audit launchers have now exited; the original BEIR chain remains paused pending further
correctness work. The external `gpu.py` utility and processes remain strictly untouched.

The up-to-date detailed handoff/evidence is in `/root/embedding-optimizer-story-refactor/PROJECT_STATUS.md`
and GitHub issue #41. The isolated work is not deployed/pushed. No formal retraining or HF deletion
has occurred. Never merge the whole isolated branch into this paused controller. Historical status
counts below are superseded. The manuscript must contain only real scientific findings, never
packing/padding or other implementation-incident narratives, including in the appendix.

## Goal and authoritative read order

The active goal is to determine whether AdamW, Muon, or NorMuon gives better optimization and
retrieval outcomes when adapting DenseOn on the same deterministic 500k-query, seven-negative
training set. The current phase is the 12-run independently padded corrective replication. LateOn
is no longer active work.

Read these sources in order before acting:

1. `PROJECT_STATUS.md` — canonical human-readable state, conclusions, blockers, and next steps.
2. `CURRENT_PROGRESS.json` — latest committed machine-readable training snapshot.
3. `README.md` — public study overview and reproducibility entry point.
4. `configs/dense_scope_amendment.json` — active Dense-only scope.
5. The frozen protocol relevant to the task. For the current phase, read
   `configs/dense_no_packing_execution_protocol.json`, its preflight parent, and
   `configs/dense_no_packing_evaluation_protocol.json`. Weight-space and retrieval-bridge work
   must additionally follow `configs/dense_no_packing_analysis_protocol.json` and the source-bound
   bridge implementation in `configs/dense_no_packing_bridge_implementation_protocol_v2.json`;
   corrected outcome aggregation must follow `configs/dense_no_packing_outcome_protocol.json`, and
   historical/corrected comparisons must follow
   `configs/dense_no_packing_sensitivity_implementation_protocol.json`. Final paper rendering
   must follow `configs/dense_no_packing_publication_protocol.json`.
6. For the prospective state-by-operator mechanism follow-up, read both
   `configs/dense_no_packing_state_operator_factorial_protocol.json` and
   `configs/dense_no_packing_state_operator_factorial_implementation_protocol.json`, plus its
   publication and completion locks and the result-blind abstract-compliance migration, then follow
   `docs/state-operator-factorial.md`. Do not execute it from changed source bytes or before the main
   corrected completion ledger is fully complete.
7. `docs/dense-no-packing-retrain.md` — exact corrected-matrix operational commands.

If `logs/dense-no-packing-v1/recovery-supervisor-state.json` exists, read it after
`CURRENT_PROGRESS.json`. It is the atomic control-plane state for the recovery defined in
`configs/dense_no_packing_control_plane_recovery.json`; do not launch a competing matrix while its
phase is `waiting_for_adopted_training` or `matrix_running`.

The legacy recovery state has no code-level heartbeat while its blocking matrix child runs. The
artifact-only next-pair guard is frozen in
`configs/dense_no_packing_matrix_handoff_guard.json` and publishes
`logs/dense-no-packing-handoff-guard/state.json`. It waits for both current AdamW runs to become
deeply complete, then gives the existing matrix five minutes to create a declared Muon successor
log or output. It yields if any successor artifact appears and invokes the unchanged recovery
supervisor only after the full absence grace and a final race check. Do not launch a second guard
while its lease is held, and never replace its artifact gates with process inspection.

If `logs/dense-no-packing-finalization/pipeline-ledger.json` exists, it is the atomic handoff state
for incremental corrected checkpoint backup and the post-training evaluation/analysis/publication
chain. Do not launch a competing corrected finalizer while its controller lease is held. Resume it
only with `python -m embed_optim.corrected_completion_pipeline --resume`; the exact operational
source/protocol/command contract must still match.

The owner-directed paper-only amendment caused the pre-amendment controller to fail closed at
2026-09-04 00:07 UTC. Its sole authorized contract transition is frozen in
`configs/dense_no_packing_completion_contract_migration.json`. If the ledger still has the exact
source contract named there and the controller lease is free, run
`python -m embed_optim.completion_contract_migration` once, then resume the controller normally.
The migration archives the original ledger byte-for-byte and verifies that the matrix, execution
protocol, controller, arguments, and command order did not change. Never generalize this path to
accept arbitrary contract drift.

The first resumed audit exposed a narrower operational bug: audit-only full-run verification erased
the stored upload commit identity. The hardening transition is frozen separately in
`configs/dense_no_packing_backup_provenance_migration.json`; it preserves the original upload
commit during audits and adds `corrected_checkpoint_backup.py` to the controller's own contract.
If the live ledger still has that protocol's exact source hash and the controller lease is free,
run `python -m embed_optim.completion_backup_contract_migration`, then resume normally. This second
one-time transition must not be substituted for any future source change.

A later result-blind manuscript topology preflight found that placing every corrected table in the
main paper pushed its endpoint to page 9. The exact non-scientific transition is frozen in
`configs/dense_no_packing_publication_layout_migration.json`: the all-rate retrieval answer remains
in the main narrative, while the complete geometry-bridge and execution-sensitivity tables are
invoked after the appendix boundary. After this change reaches the experiment checkout and the
controller releases its lease, migrate with
`python -m embed_optim.completion_contract_migration --protocol
configs/dense_no_packing_publication_layout_migration.json`, then resume the ordinary controller.
Do not use that migration for any other source drift.

A second result-blind narrative audit found that the frozen corrected renderer would update its
results section but not the abstract or main Conclusion. The exact publication-only transition is
frozen in `configs/dense_no_packing_publication_narrative_migration.json`: the same primary
all-rate finding is rendered into the abstract and main Conclusion, while the full historical
packed-selector claim moves to the appendix. After this change reaches the experiment checkout and
the controller releases its lease, migrate with
`/usr/bin/python3 -m embed_optim.completion_contract_migration --protocol
configs/dense_no_packing_publication_narrative_migration.json`, then resume the ordinary controller
with `/usr/bin/python3`. This migration was executed at 03:12 UTC on 2026-09-04; its distributable
receipt is `reports/dense-no-packing/publication-narrative-migration.json`. Do not rerun it or use it
for any other source drift.

If `logs/dense-no-packing-sealed-backup/state.json` exists, it is the independent per-checkpoint
durability state. Its supervisor only reads training artifacts and uses CPU/network resources. Do
not launch a duplicate while `logs/dense-no-packing-sealed-backup/supervisor.lease` is held. A
covered checkpoint means a hash-audited remote backup exists; it never means the run or study is
scientifically complete.

On the experiment host, `CURRENT_PROGRESS.json` may lag the logs. Refresh it only through the
artifact-only command below; it does not inspect system processes:

```bash
python -m embed_optim.corrected_progress --output CURRENT_PROGRESS.json
```

## Handoff discipline

Keep `PROJECT_STATUS.md` current whenever a meaningful run, failure, release gate, backup, or
scientific interpretation changes. At the same boundary, refresh `CURRENT_PROGRESS.json`, update
GitHub issue #41, and commit and push the documentation/evidence change once its checks pass. Do
not commit a new snapshot for every training step; the JSON is a durable handoff receipt, while the
command above is the live view. Never put claims based only on a running or partially written
checkpoint into the paper.

The active corrective phase is governed by `configs/dense_no_packing_execution_protocol.json`;
its engineering parent is `configs/dense_no_packing_preflight_protocol.json`, and corrected
checkpoint reloads are governed by `configs/dense_no_packing_evaluation_protocol.json`. The
weight-space operationalization and retrieval bridge are governed by
`configs/dense_no_packing_analysis_protocol.json`; the later executable bridge source binding is
governed by `configs/dense_no_packing_bridge_implementation_protocol_v2.json`. The v1 bridge
implementation lock is a superseded, never-executed receipt and must not be used. Validation
selection, max-T inference, and retrieval dynamics are governed by
`configs/dense_no_packing_outcome_protocol.json`; execution-path sensitivity is governed by
`configs/dense_no_packing_sensitivity_implementation_protocol.json`. Generate the complete
corrected paper tables only through `python -m embed_optim.corrected_publication`, which
verifies all four upstream manifests and the source-bound publication protocol. Do not hand edit
its generated paper include. Do not launch or interpret corrected runs from
an uncommitted matrix.

Use `python -m embed_optim.corrected_wandb_audit --allow-partial` for a read-only audit of active
corrected source runs. Omit the partial flag only after 12/12 training completion. This check may
verify W&B identity, configuration, and state, but it must not mutate source histories or supply a
scientific result.

The active paper scope is DenseOn only. LateOn files are historical provenance and must not be
promoted into primary inference or used to justify new computation.

The completion controller uploads a run after all five scheduled checkpoints are deeply complete.
If a machine-shutdown risk requires earlier durability, use
`python -m embed_optim.incremental_checkpoint_backup` only on an already sealed scheduled
checkpoint. Its receipt must report `scientific_completion=false`, verify the local payload is
stable, and compare Hugging Face LFS SHA-256 or Git-blob SHA-1 digests after upload. This operation
preserves a resumable state; it must never promote a partial run into a completed result.
For unattended coverage use `python -m embed_optim.sealed_checkpoint_supervisor`; it reuses the
same sealed-checkpoint uploader, fails closed on invalid receipts or a changed source contract,
and yields the final checkpoint to an active whole-run backup before applying its own fallback.

The state-by-operator follow-up has its own source-bound handoff at
`python -m embed_optim.state_operator_factorial_completion --resume`. Its ledger is under
`logs/state-operator-factorial/completion/`. It must wait for the exact completed main corrected
ledger, then runs the frozen calibration, six training waves, checkpoint backup, probe/BEIR
evaluation, summary, paper-only renderer, and release gates. Do not launch any factorial command in
parallel with that controller while its lease is held.

The result-blind abstract-compliance transition is recorded in
`configs/dense_no_packing_state_operator_abstract_compliance_migration.json`. It preserves every
scientific choice while enforcing the ACLPUB 200-word abstract maximum after both result macros
expand. A pre-transition factorial ledger with zero executed steps must be archived byte-for-byte
before resuming the controller against the refreshed source contract; never overwrite that ledger
or treat its waiting state as scientific output. The completed host transition is recorded in
`reports/state-operator-factorial/abstract-compliance-runtime-migration.json`; the active contract
starts with `6605090d` and remains at zero steps until the exact main corrected ledger completes.

Never inspect, read, edit, signal, stop, replace, or otherwise touch `gpu.py` or its processes. It
is outside this repository and automatically yields to study jobs.

Preserve all existing checkpoints and evidence. Use new output namespaces for corrected reruns;
never overwrite the 34 completed historical Dense runs. Treat protocol thresholds and failed gates
as results, not knobs to relax. In particular, the candidate-breadth width-7 reproduction failure
and `reports/candidate-breadth/packing_invariance.json` must remain disclosed.

Before committing, run the tests and release/audit commands appropriate to the changed surface,
check `git diff --check`, and verify that the manuscript has no pending result macros or Type 3
fonts. Do not push generated evidence or change GitHub pull-request state until its source-bound
audits pass. Never print or commit credentials.

For a fast repository handoff check, run `python scripts/portable_evidence.py --audit-only`, then the
strict Dense paper audit documented in `README.md`. The portable closure is the clean-clone evidence
boundary; the public Hugging Face checkpoint archive is required for full model-state
reconstruction. Never relax either audit to turn a failure into a pass.
