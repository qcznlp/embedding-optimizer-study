# Prospective exact-measurement sensitivity and named feature families

This plan is written before running a new sensitivity consumer. Parent bridge acceptance is
`f8a36ff6...`; no complete valid v3 primary result exists. Old approximate/diagnostic and historical
outputs have been seen. This is not retrospective preregistration of those observations.

Retain the complete original nine-feature bridge under `540c3c30...`. Add exactly five named
counterparts, fit separately under the same baseline, folds and exact-arithmetic numerical policy:

1. nonzero-matrix-weighted exact saved-segment stable-rank fraction;
2. nonzero-matrix-weighted full-spectrum saved-segment entropy-rank fraction;
3. nonzero-matrix-weighted exact cumulative stable-rank fraction;
4. mean exact saved-segment projector overlap to the same-stage AdamW rate population;
5. mean exact cumulative projector overlap to that same AdamW population.

There is no extra copy of the four original nonspectral features and no multivariable fit. Every
old/new correspondence is reported, not only those agreeing with a preferred story. For entropy,
full-spectrum normalization is a different estimand from top-64 renormalization. For the rank
averages, nonzero-matrix denominators differ from old zero-imputed averages when zeros occur.
Retain those denominators and labels; a comparison is sensitivity to both the named measurement
definition and its estimation, not necessarily pure sketch error.

Exact overlap averages all four AdamW rates for Muon/NorMuon and the other three for AdamW, never
self-comparison or outcome-based selection. If a required complete pair overlap is undefined,
the entire focal feature is undefined. Do not use its resolved-only conditional mean or silently
drop an AdamW rate. Missing primary rows are errors; a legitimately undefined measurement retains
its row and reason and yields an undefined complete prediction/association, not an available-case fit.

The named-family numerical helper takes the authentic complete original bridge panel plus new,
distinctly named columns. It never fills or relabels old feature slots to route another feature.
Verify exact equality with the existing nine-feature solver when explicitly given that same family.
The separate original four dimension predictors can use the same exact arithmetic after their own
v3 export/measurement admission is implemented; preparing that numeric interface alone is not a
completed functional-dimension analysis or a replacement for the frozen ablation/rotation plan.

The complete sensitivity consumer must freshly admit/recompute both original bridge evidence and
exact geometry before output, then check all raw bindings after joins. Positive orchestration tests
are labelled synthetic. Existing real short diagnostic rows may test geometry-only extraction and
zero/undefined semantics, but must never acquire synthetic primary identities or retrieval outcomes.

Scientific boundary: cumulative/saved-segment displacement geometry alone cannot establish useful
embedding dimensions or explain superior retrieval. The scientific chain still requires valid full
training, held-out retrieval, functional coordinate interventions/rotation controls and the bounded
state-by-operator follow-up. This preparation supplies no optimizer finding and no deployment authority.
