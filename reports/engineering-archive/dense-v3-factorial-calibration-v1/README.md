# Genuine v3 calibration integration

This is a new internal input/calibration component, not a source-release or GPU
dispatch authorization. It retains the two fixed step-2345 source choices, the
same 50K branch, all 32 calibration rows/eight histories, global Frobenius target
5e-4, four calibrated cells, three order seeds and 391-step continuation horizon.

The actual input reader consumes the separately accepted complete data/loading
evidence and freshly verifies native checkpoint seals and original immutable
durability receipts. Explicit external location roles do not fall back to old-host
paths. Old source protocols and their runtime/publication gates remain unchanged.

The new GPU gradient producer reuses the original gradient exporter while
verifying the genuine loaded weights, full topology and deterministic attention
policy. It cannot reuse or overwrite old gradients. The direction producer keeps
the original direction/norm math. The consumer verifies all eight shard identities
and recomputes all four rates from complete hidden-matrix norm rows; it expands all
twelve cells with fresh reset state, never calibration moments. CPU fixtures are
explicitly synthetic and are not actual GPU calibration or formal branches.

Work root: `/tmp/dense-v3-factorial-calibration.U5gTyB`. Initial assembled source is
`source-first`; every original primary file remains unchanged. The first focused
45-case CPU suite passed. Preserve its exact source and `tests-first.xml`.

The full primary BEIR matrix, validation selection and functional-vector campaign
retain priority. No GPU worker from this component has been launched, and the
one-GPU priority question is still unanswered. Actual GPU gradient/direction
save/readback and four-GPU DenseOn branch execution remain necessary, followed by
formal source/run identity, whole-run reading and all twelve branch outcomes.
This component has no scheduler; do not call its internal GPU producer outside a
separately admitted exclusive worker. No primary source, old controller, protocol,
manuscript, repository publication or remote object is changed by these checks.

## Completed bounded results

All CPU calls are terminal. The second and final actual input consumers pass:
two genuine native source checkpoints, the 50K branch, fixed 32-row selection and
66-file source assembly. Every one of the original 56 primary files remains
unchanged. The final actual read is `actual/actual-inputs-third.json`, SHA
`3640dc3a0deaa142d7e09eabfcc8450ab786db1b7302e04f470518557a63176f`.
This is fresh native content-seal/input verification, not model/gradient execution.

The final **57-case** CPU suite passes with no failures, errors or skips. It
contains an actual tiny-tensor safetensors/history-to-norm calculation checked
against independent NumPy FP64 Adam arithmetic and eight actual primary Muon
state transitions. Those tiny shapes are explicitly synthetic, not a full-width
GPU numerical check. The separate 88-name/full-coverage consumer fixture uses
synthetic norm data and tests all twelve cell mappings, eight original-history
shards and rehashed semantic refusals. Runtime, device, source state, gradient
order, storage policy, clipping, target/rates and external receipt bindings are
checked. No synthetic input is used to admit a real experiment.

The initial **45** and intermediate **56** passing cases are preserved as versions,
not extra independent tests or seeds. Lint and format checks pass on all four new
files. The first actual-input wrapper call (session 65567) exited 1 before input
admission: it addressed the parent source inventory as a flat identity rather than
its existing `identity` field. Only that new wrapper lookup was corrected; the
original source inventory, guards and numerical implementation were unchanged.
The exact failed wrapper is in `actual/audit_actual_inputs-first.py`. Its tool
stderr was observed but not separately captured to a log file. The successful
wrapper was subsequently formatted and its final version records its own digest.
Two initial lint issues (unused import/import order) were also corrected.

`actual/source-first`, `source-second` and `source-third` preserve the three
assembled versions. The second version adds external parent-receipt bindings;
the final version additionally validates the pinned recorded runtime and retains
the original explicit vector-norm call. `ops-before` preserves all four earlier
handoffs. `verify_archive.py` verifies this archive and the reversible documentation
addition without recomputing gradients, reloading models or touching a GPU.

## Next action

Preserve this prepared component and its fixed source. Do not relaunch completed
CPU calls. Primary evaluation and validation/functional work still have priority.
After an authorized resource/source handoff, the next calibration acceptance is
the actual single-GPU 32-row/eight-shard forward/backward, unchanged-weight check,
direction save/readback and independent first-result numerical verification. The
actual four-GPU default-topology continuation, formal source/run/checkpoint
identity and whole-run consumer are still required before twelve formal branches.
No old controller or frozen protocol may be silently adopted or rehashed to pass.
