# Current primary scientific consumer and complete continuation training

Local evidence snapshot, 2026-09-12. The complete goal remains active.

Latest update, **20:07 UTC**: all **60/60** continuation checkpoints are now
HF-verified and the backup coordinator exited zero. Both BEIR pilots passed
(**2/168** tasks), six full-corpus GPU workers are running, and two probe workers
have completed **25/60** checkpoints. The [final handoff](final-handoff.json)
binds these dated observations and all twelve original backup manifest/receipt
pairs. The earlier snapshot below is retained as history, not current counts.

All **12 crossed continuations and 60 native checkpoint reads are complete**.
The final Muon-source branches finished at **19:57:56 / 19:58:43 UTC**.
Both training supervisors returned actual exit zero, as did every rank and fresh
five-checkpoint reader. Their original records are copied under
[complete-training](complete-training), with source/copy digests in the
[archive manifest](archive-manifest.json). Do not restart these queues.

At 19:59:29 UTC both downstream pools admitted complete training. BEIR was still
reading its native inputs (**0/168** GPU tasks); the probe already had **1/60**
completed checkpoint results. The original backup receipts verified **50/60**
continuation checkpoints; the unchanged watcher was uploading the remaining
branches. These are dated observations, not a heartbeat. Read the root
[current handoff](../../../CURRENT_EXPERIMENT.md) for later counts.

## What this consumer actually completed

- Fresh original whole-run/checkpoint reading for all **12 primary runs / 60
  primary states**, all **840** native BEIR task results, all twelve complete
  validation results and validation-only selection. Every one of the 500,000
  training rows and all eighteen immutable data files are checked by the
  previously authorized view-history amendment.
- Original geometry admission, all **264** raw geometry-file hashes, complete
  joins to the already finished full tensor/SVD readbacks and reconstruction of
  all eight aggregate families. The expensive SVDs were not repeated.
- Original clean functional admission, the complete **61-state** native numeric
  chain, all raw arrays/features, all four aggregate CSVs, all nine inference
  families and decisions. Original numerical functions and generated TeX agree.
- Genuine `PrimaryV3Contract`, `PublicationContract` and `ExactPublicationContract`
  objects join all twelve whole runs and all sixty saved-model/seal identities.
  The unchanged outcome/bridge/publication functions regenerate all ten outcome
  families, fourteen weight candidates and nine functional table families.
  All **84 weight + 24 functional** exploratory comparator cells remain present.

The [accepted current assembly](actual/scientific-publication-v3/completed.json)
has SHA-256 `1941d86b42972a53dbca8ff86c8cc346282cc10f17446d62f66d67fcedb826cc`.
Its five numerical TeX fragments match the prior independent numeric display
byte-for-byte. This is a complete **current primary scientific-result assembly**,
not original single-selection-guard admission or a finished factorial/paper release.

## Read the paper preview

The [full development PDF](actual/paper-preview-v2/build/main.pdf) now contains
the actual primary/functional findings, all exploratory comparator tables, an
[observed weight-to-retrieval map](actual/trajectory-maps-v2/weight-to-retrieval-map.pdf)
and [every learning-rate trajectory](actual/trajectory-maps-v2/full-rate-retrieval-trajectories.pdf).
Both new vector figures retain all sixty real states; neither fits a projection,
selects winning runs or introduces a statistical test. The complete point table,
renderer and data bindings are retained. The first overlapping-legend version is
also preserved, followed by the corrected outside-legend display.

The [preview source](actual/paper-preview-v2/main.tex) distinguishes parameter
motion, coordinate utility, comparator-dependent prediction and conditional
continuation response. It does not claim that characteristic Muon spectra or
larger displacements explain retrieval. No tested weight or functional descriptor
passes the prediction criterion under every recipe-only comparator.

All twelve final preview pages were visually inspected. The main text ends on
page **8**, the currently visible draft abstract contains **145 conservative
tokens** including the explicitly pending finding, and all **25** font records
are embedded Type 1 or CID TrueType. The final compile has no overflow or unresolved
references. The factorial placeholders remain visible; this is not submission-ready.

The original strict abstract reader correctly refuses `ResultPending`. The
unchanged old float-layout reader also refuses the expanded figure/table inventory
(two actual trajectory figures, exact sensitivity and two recipe-control tables).
Those failed observations are retained, **not waived or called passes**. Final
integration needs a source-bound current complete-document consumer for this
explicit inventory, actual factorial outputs, clean active constants, exact
generated-byte checks, the same eight-page/200-word limits and no Type 3 fonts.
The authoritative `paper/main.tex` remains unchanged at
`45a3d297e08dfbb99908508a68ee71f077f29203bc3da8b017f6564769bc177e`.

## Preserved attempts and scope

[Original tool responses](actual/tool-responses.json) and
[later terminal training responses](actual/tool-responses-additional.json)
retain actual process results. The original outcome gather failed on the known
single-versus-double-selection fingerprint, and was not retried or modified.
The current native consumer uses the already authorized content/history reading.
The final original primary pair's unavailable supervisor exits remain unknown;
they are not converted into invented exit-zero records.

One new join test initially exposed duplicate geometry run acceptance. The final
coverage check rejects that input. Two subsequent actual assembly comparisons
failed on row order and CSV-string-versus-number representation respectively;
the preserved diagnostics show no changed numerical result. Corrections affect
only comparison representation: all 840 original scores are validated before
key sorting, and numeric CSV fields decode exactly without tolerance or boolean
coercion. Final controls: **9 native-entry and 19 join test methods pass**.
These controls are not independent scientific experiments.

The archive copies **434 files / 138,703,829 bytes**, rehashed on both sides.
It is not a public or committed-source release. Native upstream replay still
needs the producer/model paths; previously completed cold numeric replay and
public checkpoint/data restoration remain separate evidence. No old controller,
protected helper, frozen numerical source, HF deletion or GitHub write changed.

The next experiment step is the existing **168-task BEIR + 60-probe** queues,
then their already queued native collectors and three fixed factorial contrasts.
Actual prior task durations imply about **84 GPU-hours** of new BEIR work:
idealized 10.5 hours on eight GPUs or 14 hours on six, before tail/admission effects.
A practical planning range is 12–16 hours, not a deadline or scientific result.
Genuine GPU resume verification waits behind this scientific work.
