# Preserved failed audit and test attempt

The initial 41-case outcome suite passed. After adding 21 fully rehashed semantic controls,
the 62-case focused attempt has one assertion failure: a missing task file is correctly refused
as `Require an ordinary source/payload file`, but the new test incorrectly excludes the broad
word `source` from every error. The transport/input load already passed separately. The expected
refusal must be checked specifically; do not weaken the production file requirement.

The first strict cold audit, owned session 14580, terminated with exit 1 before numerical replay.
The access guard correctly blocked the old live checkout's editable-install search directory.
Torch's temporary-module import scans `sys.path`, which includes that path via the installed
environment. Preserve the guard; remove the forbidden search entries before importing the
archived package. This is an audit launcher/environment correction, not a scientific-kernel fix.

The exact pre-fix source, failed focused XML and cold stdout/stderr/fixture receipt are retained
here. The complete first input/relocated trees remain at
`/tmp/dense-v3-outcome-reconstruction-cold.EDmj0q`. The initial full regression is terminal exit 1:
2,481 cases and the same one failed test-message assertion. Its exact XML is retained. It ran
already collected pre-fix code; pytest's late traceback excerpt reflects the subsequently edited
file, so use `source/tests/test_primary_v3_outcome_reconstruction.py` for the actual failed code.
Both failed unit inputs and refused copies are preserved outside pytest's automatic cleanup at
`/tmp/dense-v3-outcome-failed-tests.zOfRNh`, in `focused-*` and `full-*` namespaces.
No production reader, validation/statistical kernel, source contract or scientific threshold has
been changed. A failing attempt supplies no bounded reconstruction acceptance or primary result.
