# Original outcomes from authenticated local primitives

Previous goal turn: **progress**. Preserve accepted vector parent
`a5ec82bafa467462dc17600ae2c041e0cf5f67f988bc028a0983d5e47c82aed1`, all 39 bindings,
all earlier failed evidence, and the separate first minimal raw-outcome fixture unchanged.
The current before-documents preserve the parent's four mutable front files and live handoff.

Implement a new CPU-only reader, without changing the existing validation, task, statistical,
vector or training kernels. Load the actual archived contracts and externally anchored local
manifest through the existing reconstruction input boundary. Validate the twelve run identities,
all five sealed-checkpoint metadata records and logical payload-size consistency. These checks
authenticate recorded metadata, not absent checkpoint tensors or newly executed training.

Rebuild each validation plan from the frozen data identity and final checkpoint. Inspect all
4,096 saved candidate-score/FP32-metric records per run with the unchanged scalar replay policy;
recompute summaries and selection. No BEIR score is a selection input. Authentication may hash
the complete archive, but the selection calculation receives only validation data.

Rebuild the v3 BEIR checkpoint wrapper and cache key for all sixty jobs. Resolve only declared
local roles, using lexical relationships to retained original location strings. Parse all 840
task results with existing pinned revision/split/subset/runtime/model checks. Reject missing,
duplicate or extra cells and unused local-role files. Preserve original provenance bytes without
opening the producer's locations or looking up files by basename/hash alone.

Recompute all ten original outcome tables with unchanged selection, 50,000 shared resamples,
all rates/stages and undefined-value rules. Write a separate new reconstructed bundle before
comparing the stored aggregate bytes; retain failing fresh numerical output without a success
receipt. Do not install results into the manuscript or claim geometry/functional inference,
retrieval/encoding repetition, physical cross-host execution or actual primary admission.

Upgrade only a new explicitly synthetic integration namespace to the complete checkpoint
metadata schema; never weaken production requirements for the preserved minimal placeholders.
Exercise all raw records/cells in a relocated, network/old-path-blocked process using archived
sources, and reject rehashed wrong summaries, selection, tables and identity/coverage changes.
Preserve any failed attempt, run focused/full regressions and independently replay the complete
audit before bounded acceptance. Actual missing primary authoring must still reject.
