# Inspected remaining publication boundary

This is a code-backed handoff, not implemented v3 publication or permission to
release. Finish the joint cold audit, independent complete replay and acceptance
first. Do not repeat the accepted standalone raw readers as missing work.

## Existing interfaces are not interchangeable

| Surface | Current implementation | Remaining v3 work |
| --- | --- | --- |
| Primary evidence include | `corrected_publication.load_publication_evidence` requires old summary manifests and old parent protocol hashes | Consume authenticated v3 outcome, geometry and original-bridge bundles; reconstruct all headline/secondary/dynamics/weight/bridge evidence |
| Functional include | `primary_v3_dimension_inference.gather` computes the complete tables and rendered LaTeX but explicitly does not install them | Add source-bound authoring and publication admission; preserve the existing four named predictors and fixed inference family |
| Portable functional publication | `dimension_release.selected_files` requires old canonical paths, old scopes and an old export handoff | Select the actual v3 closure after complete checkpoint-backed authoring; use externally anchored local roles for reconstruction |
| Manuscript audit | `paper_audit._primary_publication_status` and `_dimension_publication_status` call the old publication consumers | Integrate reviewed v3 consumers without accepting arbitrary non-pending includes or merely updated hashes |

The active completion table in `docs/completion-gates.md` still names historical
execution/export contracts. AGENTS.md and PROJECT_STATUS.md supersede its old
controller-transition instructions. It needs an explicit versioned v3 handoff,
not execution of the archived commands or silent alteration of old locks.

## Required scientific preservation

- Keep all three all-rate primary optimizer contrasts, three validation-selected
  secondary contrasts, fifteen optimizer-stage rows and all twelve recipe rows.
  Selection must still use validation, not BEIR. Rates are not training seeds.
- Preserve all nine original geometry predictors, all doses/stages and undefined
  fits. The separately named exact-spectrum/projector sensitivity is not a
  replacement for the original bridge and is not included in the current joint
  raw reconstruction payload.
- Preserve the nine-contrast functional task family, twenty-seven rotation
  contrasts, four genuinely named predictors, every prediction and all figure
  points. Sampled rotations do not establish arbitrary-basis invariance.
- The old publication loader also requires an execution-sensitivity engineering
  report. That historical input is not v3 scientific evidence. No implementation
  incident belongs in any manuscript section; never manufacture a compatible
  historical report to satisfy the old loader.
- Primary authoring must still require actual complete checkpoint-backed inputs.
  A raw reconstruction receipt alone is not that admission and cannot release
  draft source/runtime/primary parents. Every synthetic marker must remain a
  hard publication refusal; do not install the current fixture's rendered text.
- Regenerate exact manuscript bytes from authenticated fresh evidence and verify
  the strict paper audit against the new consumer. Preserve visible pending
  markers until real evidence exists. Keep report generation distinct from
  manuscript installation and external publication authority.

The crossed state-by-operator continuation remains a separate integration:
its routed AdamW control is rejected by the current primary-only training policy.
Neither publication work nor a source-hash replacement supplies that experiment.
No formal training, deployment, controller transition, commit/push or HF operation
is authorized by this note.

## Training candidate failure set, read back from its preserved full suite

`reports/engineering-archive/dense-full-identity-v1/tests/full-first.xml` still
contains eleven failures among 963 cases. Six concern the old analysis consumers
(execution sensitivity, geometry, outcomes and retrieval bridge), all reporting a
`config.py` source-binding mismatch. Five concern the factorial implementation or
completion source contract, including the bound trainer. These are not eleven
newly observed Muon numerical failures, and they are not waived by the 2,573-case
development suite. Keep this exact failed source/test evidence.

`primary_v3_training.execute` calls `require_execution` before importing the
training stack, then requires exactly four ranks, the actual admitted training
source path and runtime, one declared GPU pool and complete run identity. It
forbids short-horizon overrides. The checked-in protocol remains
`prepared_not_execution_authorized`; do not add a compatibility flag to bypass
that boundary while preparing the assembled consumer/source transition.
