# Agent handoff instructions

## Read first

1. [PROJECT_STATUS.md](PROJECT_STATUS.md): current evidence, blockers and exact next work.
2. [CURRENT_PROGRESS.json](CURRENT_PROGRESS.json): artifact-only legacy execution counts, not a heartbeat or scientific verdict.
3. [README.md](README.md): study design and safe entry points.
4. The frozen protocols and accepted receipts for the surface you will change.

This file consolidates the former chronological handoff. Its full predecessor is preserved at
[the before-copy](reports/engineering-archive/dense-v3-dimension-interventions-v1/before/AGENTS.md).
Older milestone counts and launch instructions are historical where superseded below. Preservation,
approval and scientific acceptance rules are not relaxed by this documentation consolidation.

## Goal and scientific boundary

DenseOn only: compare AdamW, Muon and NorMuon on the same revised deterministic 500k queries,
one positive and seven negatives, no in-batch negatives, maximum length 8192, four rates per
optimizer and five retained stages. Deliver a defensible NAACL paper and reproducible repository/
model-analysis artifacts. No new LateOn work and no standalone article/blog.

The story is reached weight states and trajectories -> functional representation utility ->
held-out retrieval prediction, with a separately bounded crossed state-by-operator continuation.
Intrinsic optimizer spectral/row behavior, a one-step proxy, tests and diagnostic numbers are not
optimizer-quality or mechanism findings. Learning rates are not independent training seeds.
Native-coordinate deletion is basis-dependent; agreement under three sampled rotations does not
prove arbitrary-basis invariance. Predictive support and crossed continuation are not mediation.

No packing, padding, execution incidents, candidate-breadth debugging or other implementation-error
narrative belongs anywhere in the manuscript, including its appendix. Preserve such evidence only
as repository engineering provenance. Never import held/historical or synthetic results into paper
tables, figures or claims; do not hand-edit generated findings or erase pending markers.

## Current state and next action

The old 12 runs / 60 checkpoints are preserved but on scientific hold. No accepted full-horizon
v3 primary run exists. Data/core/validation/outcome, approximate and exact geometry, original and
exact-feature bridges pass their stated bounded preparation checks. Do not repeat them as missing.

The accepted [export/feature archive](reports/engineering-archive/dense-v3-dimension-chain-v1/README.md)
connects complete v3 state admission, actual fixed probe identities, raw-vector production and
literal feature reconstruction. Actual CPU loading matches all 134 pretrained tensors; complete
61-state synthetic wiring and relocated fresh-process replay pass. The synthetic kernel fixture
is 8D, expanded only for schema testing; it is not the separate real 768D numerical verification.
That accepted parent retains 688,128 real rank checks, 80 masks, three rotations, and all 224 probe
rows / 2,016 texts against 42 pinned files. No primary model was encoded or admitted by simulation.

The new [functional inference archive](reports/engineering-archive/dense-v3-dimension-inference-v1/README.md)
connects the nine-contrast task family, 27 rotation contrasts and four genuinely named predictors.
Independent Torch/SymPy checks and relocated synthetic numerical replay pass: 240 exact predictions,
16 fold errors, four pooled decisions and 68 figure points. Its full downstream wiring uses explicit
upstream simulation; it does not admit real primary vectors or certify end-to-end primary relocation.
The first oracle failure is preserved; production inference and scientific rules were not changed.

The [portable preparation](reports/engineering-archive/dense-v3-portable-reconstruction-v1/README.md)
now closes actual source dependencies: 238 files load the same contracts and twelve identities
in a fresh relocated process, using only its 69 archived package imports. Explicitly simulated
upstream authoring tests the local transport separately. Original location fields are preserved;
the transport reader uses only externally anchored local roles. Missing primary inputs still fail.
This does not reconstruct raw-vector features or numerical paper evidence on a new host.

The new [raw-vector reader](reports/engineering-archive/dense-v3-vector-reconstruction-v1/README.md)
passes complete full-width cold checks. Its 38 focused / 49 ordered tests and
2,419-case full retry pass. The first full run's 17 mixed-test-module refusals and exact source
are preserved; only test isolation changed and an explicit foreign-source refusal was added.
Production gates and scientific numerics are unchanged. All 61 full-768D synthetic fixture states
have been computed; both cold audit 72073 and independent replay 7506 are terminal/pass,
including both numerical refusals on each attempt. All 61 states, four complete tables and 432
numerical files match exactly, using only 70 archived package imports. The raw-vector archive's
validation.json binds this limited scope. Do not rerun these completed checks as missing work.
This is synthetic, same-host relocation, not real primary admission or physical cross-host proof.

The [original-outcome reader](reports/engineering-archive/dense-v3-outcome-reconstruction-v1/README.md)
now passes 62 focused cases, both complete cold audits and a 2,481-case isolated full retry.
All 49,152 raw validation records, 840 task units and ten tables are reconstructed, with 21
fully rehashed semantic refusals per cold audit. Only 71 archived package modules are imported.
The first test-message assertion and old editable-search-path refusals are preserved; only the
new test and audit launcher changed. Production readers and scientific kernels stayed unchanged.
The complete fixture still simulates checkpoints/scores/timing; authentic data identities do not
make it primary evidence. Its first minimal raw fixture remains separate and unchanged.

The new [geometry reader](reports/engineering-archive/dense-v3-geometry-reconstruction-v1/README.md)
now passes 67 focused cases and the 2,548-case full regression. Complete first cold audit 13684
is terminal/pass, including 28 fully rehashed refusals and an independent scalar/set oracle.
Independent replay 20415 is also terminal/pass; all eight numerical outputs match the first
audit and all 28 controls fail as required on both attempts. All owned numerical/test calls
are finished; do not poll or restart them as missing work. The bounded scope is recorded in
the geometry archive. The new zero-statistic schema check
retains all nine real diagnostic rows exactly. Its initial omission and separate test-helper
failure are preserved. All full-shape admissions/measurements remain explicitly synthetic.

The new [joint reconstruction](reports/engineering-archive/dense-v3-joint-reconstruction-v1/README.md)
is under verification. Its corrected 25 focused tests pass; the complete synthetic fixture
and independent joined numerical oracles have returned pass. Full regression **47895** is now
terminal/pass: 2,573 cases, zero failures/errors/skips. Cold audit **18914** is also terminal/pass,
including all 477 numerical outputs and all 21 semantic controls. Independent complete replay
**74613** is live at 07:45 UTC. Follow RUNNING.md;
do not modify its 13 bound sources, restart these handles or claim a terminal acceptance.
The first new fixture's rate-decoding failure and reader/fixture provenance-order mismatch
are preserved. No frozen kernel or old accepted parent changed. Its raw-component reuse
is explicit; both positive cold readers recompute every raw branch. At 08:07 UTC, the
independent positive replay matches all 477 outputs; its 21 controls are still running.
The development-suite result is not joint acceptance.

A separate [primary rendering candidate](reports/engineering-archive/dense-v3-primary-renderer-v1/README.md)
now preserves undefined bridge comparisons and exact decisions in generated text. Its 14
component tests and four synthetic layout samples pass. It is archival candidate code only,
not integrated package/publication code; it does not change the 2,573-case whole-suite scope.
The old-parser counterexamples and initial style-path failure are preserved. No bound joint
source, training kernel or manuscript changed.

Finish original/functional inference reconstruction and primary publication using local roles; follow
the geometry archive's inspected next-bridge-boundary.md. Retained
spectral-health measurements are not freshly measured from bases alone. Keep
the four genuine names and every original geometry feature. Then integrate the routed factorial
control and reviewed assembled-source/runtime/release-parent transition. The primary-only training
policy currently rejects the routed factorial AdamW control; replacing hashes is not integration.
All formal-run/publication gates remain unmet.

## Source trees and test scope

- Live unchanged numerical source: /root/embedding-optimizer-study, main f231a6430712388778f32ad1736a4cb6de3bec3e.
- Development: /root/embedding-optimizer-story-refactor, narrative/weight-space-spine, same base, preserved WIP.
- Numerical parent: /tmp/dense-correction-source.0YHywN.
- Identity candidate: /tmp/dense-identity-source.8rUgLF.

Use absolute development src/repository PYTHONPATH for every test and child process; the installed
package can otherwise import live main. On this host use /usr/bin/python: the live .venv has a
different, unvalidated stack. The raw-vector reader passes both full-width cold audits;
its 38 focused and 49 ordered-reproducer cases pass. All prior source/transport and inference
parents, including failed attempts, remain unchanged. No gate was relaxed.
The latest isolated full regression passes 2,573 cases under the pinned interpreter, with no skips or failures.
The current outcome reader adds 62 focused cases and two complete passing cold audits; its
preserved initial full attempt has exactly one test-message assertion failure, not a kernel failure.
The distinct identity candidate still has 963 cases / 11 unwaived source-contract failures. One tree's green suite
does not clear another tree or prove the whole experiment. Source copies and exact failed attempts
are part of the evidence; never silently update a frozen binding or relax a failed gate.

## Runtime and external authority — hard limits

Never inspect, read, edit, signal, stop, replace or otherwise touch gpu.py or its processes.
Do not use broad process/GPU-process inspection. Only the exact owned study handles in the accepted
handoff may be inspected; scripts.audit_dense_natural_data.handoff() is the narrow existing reader.

The old controller/wrapper/scheduler are stopped in place (T), not free to restart:
196647/start246327790, 870313/start257721545, 870864/start257733056.
The original main ledger has four completed steps and active BEIR, contract 4152531e...,
ledger SHA-256 95c69483f60badddd3c13e175951bcb52682523589c62bf898a57eb786d4e4ef.
The factorial remains waiting for main at zero executed steps, contract 6605090d....
Preserve exact identities, leases, completed prefixes, logs and backups. Never launch a competing
controller, casually resume/kill these handles, or use an old zero-step migration on the nonzero
main prefix. Any approved transition must be reconstructed and verified for the actual deployed
checkout; isolated and experiment-host hashes need not match.

Formal training/retraining, deployment, controller transition, merge, commit/push and WIP source
publication remain separately reviewed actions. The owner has not answered the outstanding WIP-
commit exception and post-acceptance twelve-run rerun questions (sent September 6, 18:09 UTC).
Preselected options and automatic goal continuations do not grant that authority. Do not repeatedly
ask while useful safe preparation remains. A draft/status-only release fails bound-parent admission;
release requires an explicit reviewed assembled, committed source/runtime/parent transition.

The existing pre-commit gate still requires complete manuscript results, relevant tests/audits,
no pending result macros and no Type 3 fonts. Do not self-waive it. The GitHub integration returned
403; do not retry the same write or use alternate credentials. Current local WIP is not published.
No claims about remote source availability may be inferred from a local edit.

HF withdrawal was safety-rejected for 9,629 paths / 366,252,912,201 logical bytes. Nothing was deleted.
Never retry, split or bypass that rejection. A later approved historical erasure needs exact scope,
current-backup/shared-object dependency checks and preserved/re-audited current payloads. Existing
LateOn and shared artifacts remain protected. Never print or commit credentials.

## Protocol and evidence discipline

- Dense scope: configs/dense_scope_amendment.json.
- Primary v3: configs/dense_primary_v3_protocol.json and its immutable data/identity parents.
- Weight/bridge analysis: configs/dense_no_packing_analysis_protocol.json plus the separately named
  v3 approximate/exact geometry and original/exact-feature bridge protocols; preserve all original
  features, changed-entry denominators, full-vs-truncated entropy distinctions and undefined coverage.
- Functional dimensions: configs/dense_dimension_utilization_protocol.json and
  docs/dimension-utilization.md. The old primary export/publication locks do not admit v3 identities.
- Factorial: read its scientific/implementation/publication/completion locks, claim-wording and
  handoff amendments, and docs/state-operator-factorial.md before changing that surface.
- Publication: read paper/README.md, the source-bound publication protocols and docs/completion-gates.md.
  Require actual accepted primary evidence, complete portable reconstruction and strict paper gates.
- Historical runtime migrations: read the exact archived predecessor instructions, migration source,
  immutable ledger/parent bindings and accepted receipts before proposing any approved action.

Retain all rows/rates/stages/seeds and frozen selection rules. Do not replace undefined values with
zero or available-case means unless a separately declared rule explicitly requires that estimand.
Distinguish input authentication, numerical replay, primary inference and final release. Preserve
checkpoints and all old/failed evidence; use new output namespaces, never overwrite a historical run.

## Working and reporting

No subagents are requested by this repository. Continue locally unless the owner explicitly asks.
Use apply_patch for authored edits, rg for discovery, and new temporary directories for diagnostics.
Do not commit unrelated WIP. Refresh CURRENT_PROGRESS only from the live experiment working
directory with an explicit output path; a missing tree in the development checkout is not lost runs.

Keep the user informed in concise Chinese during work. Track exact owned sessions to terminal
status; an observation timeout is not completion. Do not claim a diagnostic as a formal run.
Classify each goal turn by actual evidence/progress and keep the full goal active until all required
scientific results and publication/reproducibility deliverables are verified.
