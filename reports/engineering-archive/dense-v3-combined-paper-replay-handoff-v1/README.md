# Complete-paper portability: queued behind real outcomes

**PROGRESS, 2026-09-12 22:11 UTC.** The existing GPU evaluation, native statistical
collectors and complete-paper author remain unchanged. A new CPU-only consumer
is now waiting for their actual complete paper, so full numerical/PDF portability
can begin automatically after that evidence exists. **No complete combined paper
or combined replay exists yet.**

Live entry: `/tmp/dense-v3-combined-paper-replay.URO4uk9L/combined.py`.
Session **56449**, PID **865702/start322276697**, live/S at 22:11:13 UTC;
no failure, replay child or combined bundle yet. Do not edit, restart or duplicate
this source-bound consumer.

Source SHA:
`1a9c50dba2f58cc68b110749ccbc40d427445d53f0dc6c994b8f78c02096d566`.
Authority SHA:
`c66a9e434789d9d428e7fbe273df4db2fc2f383aabfb2145e97d8473dfcb7d41`.

## What the new entry does

1. Wait for the original document consumer's genuine completion; any upstream
   recorded failure stops this consumer and preserves its attempt.
2. Authenticate the original actual native primary/factorial/document chain,
   complete 168/60/840 outcome coverage, actual collector exits, original
   statistical definitions and strict native PDF. Copy those inputs and the
   already completed primary closure to a new closed directory.
3. In a fresh CPU-only process, run the unchanged complete primary replay as a
   component of the combined paper. Recompute all six factorial tables using
   the original four numerical functions and original validation/adapter code;
   repeat the original independent rational algebra and 100,000-draw bootstrap
   check at its unchanged 2e-15 tolerance. Regenerate the original factorial TeX.
4. Assemble all four result includes and both empirical figures from those
   recomputed outputs; compare every paper input with the actual native paper.
   Use the unchanged complete-document component for fresh no-shell-escape
   compilation, source recorder, no-pending, abstract, page, font and float checks.
   Require the same source inspection, layout, page/font counts and extracted
   PDF text. PDF binary metadata is not the scientific comparison target.

The original primary bundle is unchanged at manifest
`88861e52218f56b860851489106cd30e35dd79bb038eb9082967e3376db6457d`.
Its completed numerical reconstruction is not missing work; replaying it later
inside the **actual combined** paper establishes that new composition, not a
new primary experiment. No native model/encoding/SVD/coordinate computation is
repeated by this new command. Saved queue metadata and real assertions support
pure numerical functions; no synthetic or always-success admission callback is
provided. Actual native admission remains authenticated upstream evidence.

The new process's Python audit hook rejects original producer-root file opens
and network connections. Its primary child uses its existing independent Python
I/O boundary. The external TeX process is covered by no-shell-escape and its
strict compiler-input recorder, not by an inherited Python audit hook. This is
not an OS sandbox, physical second-host execution or GPU-resume verification.

## Preparation and exact observation

Final **13** synthetic operational/numerical controls pass (terminal `cdf24d`,
exit 0). They cover complete synthetic 2×2 inference/independent arithmetic,
missing/duplicate/nonfinite cells, all probe stages/tasks, frozen bootstrap
settings, inventory/anchor/symlink refusal and preservation of old outputs.
They are not actual factorial findings or a real full-paper test. The first
passing source/tests are preserved separately; their count is not added as
thirteen extra experiments. No original scientific function changed.

Actual preparation `4bcf10` exited zero, authenticating the original document
authority and all 151 inputs plus the completed primary closure. Launch
`fd4335` returned live session **56449**. Use only the exact owned observer:

```bash
CUDA_VISIBLE_DEVICES='' PYTHONPATH='' /usr/bin/python -B \
  /tmp/dense-v3-combined-paper-replay.URO4uk9L/observe.py \
  --output /tmp/NEW-UNUSED-ABSOLUTE-PATH.json
```

At **22:11:13 UTC**, both BEIR coordinators are live/S, **2/168** tasks accepted,
all eight exact registered ClimateFEVER workers live/R, and neither pool failed.
Original summary/document/two GPU-resume waiters are live/S without actual
collectors, complete-paper output or resume ranks. All 24 scientific training
runs, 120 remote checkpoints and sixty continuation probes remain complete.

The original archive preserves **24 files**, all copy-hash verified, with no
credential-shaped findings. Manifest SHA:
`052ab737ca2458ca6d29e1fc5fe7c3e939a89120d13e93c60bcb3de3065b7a74`.
Later documentation is recorded separately. No GPU/helper/lease/historical
controller change, GitHub write retry, historical HF deletion or source release
occurred. The authoritative manuscript is unchanged.

After actual completion, inspect `run/replay.exited.json`, `actual/complete.json`
and `actual/io-boundary.json`, review the complete PDF visually and finish
durability, authoritative paper installation and source-release gates. A live
waiter or green synthetic test does not satisfy any of those remaining tasks.
