# Complete original/exact publication preparation

This adds the separately declared five-feature exact sensitivity to the complete
v3 primary publication entry. It preserves the nine original weight features,
all four functional predictors, every learning-rate dose and all undefined results.
There are still **zero accepted full-horizon primary runs**. None of the synthetic
values or PDFs in this archive is an optimizer finding or an installed manuscript.

## What is implemented

- The actual entry first runs both existing checkpoint-backed gathers, rejects
  simulated admission, requires their common original-bridge evidence to agree,
  and checks both stored output bundles against fresh numerical expectations.
- Input snapshots precede inspection; they are checked again after later readers
  and output handling. A changed file cannot be authenticated as a new version of
  an already-inspected input. Canonical exact rational differences preserve `0` as
  a rational string and undefined as null, using an additional decoder rather than
  changing the frozen original decoder.
- Ten generated files retain the original primary include as an exact separate
  copy and unchanged prefix. The original functional include, its labeled manuscript
  include and primary summary remain byte-identical. The new table/finding reports
  all five counterparts, distinguishes unsupported, undefined and baseline-equivalent
  predictions, and explains full-entropy/nonzero-denominator estimand differences.
- A separate v3 layout reader requires the new exact table alongside every existing
  registered main/appendix float and keeps the eight-page main-text limit. The old
  layout reader and actual paper/Makefile are unchanged; final release integration
  remains a separate requirement.

The source-bound preparation protocol is
`configs/dense_primary_v3_exact_publication_protocol.json`, SHA-256
`e8409f534931b85ad855fbdd3804e3ef8d771b0ee22ee99d9b81bf07d605f83e`.
It is a draft and grants no execution, installation or publication authority.

## Verified evidence

- Initial 57 focused cases pass. Two additional input-timing counterexamples fail
  on the first adapter (both fail to refuse the changed input). Their exact source
  and XML are retained. Moving the snapshot boundaries fixes both; the expanded
  65-case retry passes. With the new layout checks, **all 95 focused cases pass**.
- Complete generation from the accepted synthetic exact-reconstruction population
  passes twice. All ten numerical/text outputs are identical before and after the
  input-timing correction. Each attempt recomputes original/exact decisions and
  rejects all ten rehashed output edits. The IO controls reuse freshly generated
  expected bytes; they are not ten additional raw-data replays.
- The first complete PDF is generated but the old layout reader rejects its
  additional required exact table. That failure, PDF and logs remain in `layout-first/`.
  The v3 layout retry passes: main endpoint page 7, eight registered floats in their
  required regions, conservative abstract budget 178/200, no overfull boxes and no
  Type 3 fonts. Pages 1, 5 and 9 were visually inspected; retained PNGs are in `visual/`.
  This is not visual inspection of every PDF page or a real-results release audit.
- The independent exact raw-reconstruction parent is accepted at
  `d9888b172648555b54895c8a0b0acc751a3b8a345b7872ebf929ebf7038f5c59`.
  Its complete two cold runs and all other parent checks are finished; do not repeat them.

The first new layout helper also had an import-order lint error; its pre-format
source is retained. No original numerical kernel, scientific threshold, frozen
parent, training source, actual manuscript or owned dispatcher was changed.

**Whole-development regression 7103 is terminal/exit 0**, observed at 12:15 UTC.
The preserved `full-first.xml` reports 2,808 tests and zero failures/errors/skips;
it serializes 2,799 testcase elements. The nine-element discrepancy also occurs in
the preceding milestone and is explicitly recorded in [RUNNING.md](RUNNING.md).
No handle in this archive remains live. This completed regression is not a combined
preservation acceptance or an admission of the distinct training candidate.

## Remaining end-to-end work

Actual checkpoint-backed positive authoring, source-authenticated portable final
publication, the complete versioned strict manuscript consumer, routed factorial,
reviewed assembled source/runtime release and all real primary experiments remain
required. Pure synthetic generation and a compatible PDF do not supply those gates.
Use the existing complete reconstruction and these new outputs when implementing
the complete consumer; do not substitute another historical compatibility path.
No training, controller transition, commit/push, HF deletion or remote publication
is authorized by any file in this archive.
