# Portable current handoff and intact chronological history

2026-09-13. The current reader/agent documents are separated from accumulated
chronological execution records. No experimental evidence, native protocol,
scientific result, numerical source or original release guard is changed.

| Document | Previous lines | Current lines |
| --- | ---: | ---: |
| AGENTS.md | 1,469 | 158 |
| PROJECT_STATUS.md | 2,380 | 121 |
| CURRENT_EXPERIMENT.md | 523 | 118 |

The complete predecessors, including original producer locations, receipts,
failed attempts, unknown exits and historical permissions, are retained
**byte-for-byte**, not redacted, encoded or discarded:

- [Full agent history](before/AGENTS.md), SHA-256
  `8e9ae951d0f3a71c3329de50083d5b20b8f8f39821103aaf2cd96746960929fd`.
- [Full status history](before/PROJECT_STATUS.md), SHA-256
  `fc5bc3c19ddc00a42cdf7dc822cc169cc3e0c5c09f37eefeccca341c697e4969`.
- [Full experiment handoff](before/CURRENT_EXPERIMENT.md), SHA-256
  `be9857e55d8d1e7817b5c2cac75f5dfd745fa856a54fe5d9667199090bdbff5a`.

The old documentation mixed obsolete live-job descriptions with current
work. Current documents now distinguish complete science, current manuscript
reproduction, failed exact recovery and incomplete source release. They use
explicit source/location roles and evidence links instead of treating a
producer checkout path as a reader's installation requirement.

All safety/integrity boundaries remain explicit: protected helper and stopped
controllers, both lease namespaces, no broad process inspection, original
unknown exits, preserved failed guards, no tolerance weakening, no bypass of
GitHub/HF denials, pending access question, no unauthorized source publication,
no synthetic/historical paper results, and no engineering narrative in the paper.
Historical per-surface instructions remain binding where not superseded;
agents must read the preserved detailed contracts before changing those areas.

The repository retains historical archives, which were already outside the
explicit wheel/sdist data-file allowlist. No packaging exclusion was added to
conceal a current source dependency. The unchanged original distribution audit
is used for the fresh build; runtime/input and negative-control paths remain
unresolved until genuinely integrated. Documentation cleanup is not proof that
training/scientific reproduction or full source release has passed.

The actual verification, fresh build and original audit results are recorded
under `actual-verified/`. All 86 focused tests pass; the fresh build exits zero.
The unchanged original audit exits one: four documentation findings are resolved,
and six source/test producer-path findings remain. Full distribution admission and
source release are still false. The driver subprocess exit receipts are observed;
its outer tool/session result was lost during context compaction and is **unknown**.
It was not rerun to manufacture an observed outer exit. The comparison receipt
authenticates the completed stages without asserting that missing observation.

The first preflight exited one (tool terminal `07b02d`) before build/tests because
the shortened instructions omitted the explicit phrase `source-release gates`.
The phrase was restored; the original assertion was not relaxed. The first source
and failure are retained in `verify_and_build.first.py` and `actual/`.
Earlier ten-finding distribution failures stay in their original archives.
A first tool-call construction had an unescaped JavaScript backtick;
it failed before execution and changed no file. The subsequent apply_patch
completed the intended handoff replacement. This is not an experiment failure.
