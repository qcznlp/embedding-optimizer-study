# Complete exact-feature branch of active-v3 publication

Prepared 2026-09-07, before changing any existing source. The preceding goal turn
was a verified wait on publication replay 64182. This work is local preparation,
not permission for formal training, deployment, manuscript installation or release.

The active paper consumer must retain the original nine weight features and the
separate five-feature exact sensitivity. The completed original/functional reader
does not include the latter. Connect its actual checkpoint-backed authoring and
an externally anchored local-role reconstruction, without changing frozen kernels,
protocols, existing outputs or scientific estimands.

The portable exact reader will authenticate every full singular spectrum and
resolved FP64 basis, recompute spectrum-defined metrics, statuses, all checkpoint
aggregates and pair/projector aggregates, then the complete five-feature exact
bridge from freshly reconstructed original inputs. It will retain all undefined
states and original/exact comparisons. Recorded SVD reconstruction residuals and
weight norms are authenticated measurements, not fresh measurements from model
weights. Neither archived bases nor a reconstructed table prove model admission.

The actual authoring path must first require the complete real primary publication
and exact-bridge gathers; synthetic fixtures use explicitly separate helpers. A
new versioned local closure will preserve all previous non-source bytes and add
exact roles plus their real source/parent dependencies. Original locations remain
lexical provenance only. Existing archive anchors are never updated in place.

Verification must include spectrum/status/basis and complete-population controls,
the full original/exact bridge correspondence, real retained diagnostic readback
where available, source-isolated full-shape synthetic reconstruction and independent
replay, and the whole-development regression. Positive simulation cannot become
scientific acceptance. The routed factorial and complete strict manuscript/release
consumer remain separate unfinished requirements.

All AGENTS.md restrictions remain binding. No gpu.py or process associated with it
may be inspected or changed. No formal run, controller transition, remote mutation,
commit or gate waiver is authorized by this plan.
