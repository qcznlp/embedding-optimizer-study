# Exact primary numerical-source integration

2026-09-13. This integrates the implementation actually used for the completed
primary training into the current checkout. No scientific result, numerical
kernel, historical source lock, checkpoint or manuscript was changed.

## Completed integration

Four existing files (`config`, `optimizers`, `short_branch`, `train`) differed
from the actual primary snapshot. Two modules (`dense_numerical_contract` and
`dense_run_contract`) were absent. The six modules were brought over **byte for
byte**, not independently rewritten. Their four preimages are preserved here.
Both corresponding original test files and the missing historical preparation
configuration are included unchanged. The latter is a test/parent fixture, not
the v3 experiment's current data/run matrix. All **33 primary modules** and all
**56 original assembly bindings** now match the real training snapshot.

The original `PrimaryV3Contract` loads with this checkout as both repository
and training-source root. Every one of its twelve original source identities
equals the current runtime's source identity. This actual read is session
74548 / terminal b77680, exit zero. The original draft execution refusal still
applies; copying source does not declare it released or restart any training.

Final focused integration: **271 tests**, zero failures/errors/skips, session
91116 / terminal ef3f6d, actual exit zero. This includes the primary numerical/
saved-state tests, optimizer/loss/configuration tests, complete calibration
tests using this checkout (no alternate source assembly), portable-role tests,
and current-paper/distribution unit checks. Three additional current-source
controls pass separately (99318 / 125623 / exit zero): all twelve actual source
identities, original draft refusal, and the real one-checkout `--inspect` CLI.
The tests are engineering evidence, not new scientific runs or GPU equivalence.

## Preserved failures and outstanding factorial boundary

The initial driver 63720 / f9bced exits one. The 125-case primary group has
34 fixture setup errors because the historical candidate YAML was not yet
copied, and one README wording failure after the earlier concise handoff edit.
The unchanged YAML was included; the README again explicitly says the two
four-GPU pools are disjoint. No assertion or numerical check was weakened.

The broad 409-case factorial group reports 152 failures at the missing source
parent file; this original full output/XML remains in `actual/`. After including
that file, a direct comparison and original reader show the **only remaining
66-file parent difference is `factorial_v3_inputs.py`**. Its portable mapping
was changed in the preceding task and independently returned the exact genuine
input result, but its bytes differ from the old source lock. The original
factorial reader therefore still refuses the current assembly. See
`actual-final/factorial-parent-boundary.json`. The broad factorial suite was
not repeatedly rerun against the same deterministic refusal, and is not
represented as passing by the narrower 271-case result.

No historical source SHA, factory guard, synthetic identity fixture or checkpoint
identity was replaced to hide this compatibility boundary. A genuine explicit
factorial source-version transition and completed scientific-consumer integration
remain next, followed by the reviewed release transition. Primary source
consolidation does not certify the entire historical/current codebase.

## Distribution and scope

The candidate YAML and current-source guide are declared package data. No archive
or negative control is excluded to force the original distribution checker.
The fresh build, unchanged full audit and extracted-wheel source-identity check
all pass (88114 / a0a35b, actual exit zero), recorded under `distribution/`.
A final import-order lint finding in the new source test was corrected without
changing its test body or any production code; its preimage is retained. The
final package build/audit/wheel check also passes (35434 / e72c95, actual exit zero),
with separate evidence under `distribution-final/`. The wheel check imports all
ten loaded study modules from its own extracted package and compares all twelve source
identities; it does not train or reconstruct raw scientific statistics.

The original exact-resume failures are unaffected and unresolved. This turn does
not change optimizer-state restoration, numerical kernels, any GPU/controller
state, the protected helper, remote HF contents or GitHub access. Current paper
inputs/results and original distribution scanner remain unchanged.

Work directory: `/tmp/dense-v3-numerical-integration.DUnt6osB`.
Original source assembly, preimages, current sources, all failed/passing test
evidence, actual contract readback and package artifacts are retained with a
content manifest. This is concrete **PROGRESS**, not overall goal completion.
