# Genuine factorial worker lifecycle — bounded implementation work

2026-09-10. Primary training and checkpoint/weight-artifact preservation are
already complete. Existing full-corpus evaluation, validation and functional
queues retain priority and remain unchanged. The one-GPU priority request is
unanswered, not an authorization. No GPU work is launched by this milestone.

Implement the missing fresh-run lifecycle inside an already admitted four-rank
worker: authenticate the unchanged 69-file factory assembly plus the new worker;
consume both genuine calibration chains through the original factory; call the
actual Trainer for all 391 updates; save all five original stages and final
package; reread every native model/optimizer checkpoint and the full input chain;
then emit separately scoped worker provenance. No implicit resume or automatic
retry. Every failed attempt is retained. Add a separate CPU-capable full reader.

Only the new worker, focused tests and this archive are authored. Prior factory,
run component, calibration, source/data/scientific locks and live numerical or
dispatch files are unchanged. New tests must explicitly distinguish mocked
orchestration from real Trainer/GPU execution. Full model loading, CPU Trainer
numerics and earlier component tests are complete; do not repeat them as missing
experiments. Check the real assembled-source closure and an actual CPU refusal
without allocating a device or loading another model.

This is not an admitted launcher, scheduler, controller transition or source
release. The separate source/runtime/main-completion/resource handoff, genuine
GPU calibration and default-topology GPU verification remain prerequisites to
formal execution. All twelve actual branches, complete probe/BEIR outcomes,
portable scientific consumption, final NAACL manuscript and authorized source
release remain required. No implementation narrative is installed in the paper.
