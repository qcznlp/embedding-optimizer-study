# Complete continuation-outcome backup waiter

**Queued, not completed.** At **2026-09-12 22:57:22 UTC**, exact coordinator
**875351/start322559189**, tool session **35024**, is live/S. No full outcome
manifest, upload attempt, remote verification or anonymous download exists yet.
The original native statistical summary is still waiting for complete BEIR.

The CPU/network-only successor preserves all twelve final checkpoints × fourteen
full-corpus tasks, all original start/exit proofs, final shared model/run metadata,
and all six native statistical/probe tables. It reuses the genuine original
complete collectors and independent inference proof; it does not fabricate native
admission, repeat model reading/encoding/bootstrap or modify their source.

After that original summary completes, the new entry hashes and screens the
complete selected data/metadata, admits only regular new HF additions, and writes
one content-addressed subtree in the existing public dataset. It checks all old
root/subtree entries remain unchanged, checks the immutable remote file hashes,
then actually downloads every new file anonymously and verifies the full inventory.
No executable source, raw example text, model payload or unreviewed paper is
uploaded. The sixty existing probe/model backup links are reused, not reuploaded.

Frozen entry: `/tmp/dense-v3-factorial-outcome-durability.cq4oNAJ7/outcomes.py`.

- Source SHA: `a27fbbd07c3b6bbe1b8d2675be7c4ad58ec13c6ca74d8ed66c97d993aa7c4183`.
- Authority SHA: `4b48d60de683ea3af68808987a29499f32369dabdb5cf35356ba5e5e269c0fba`.
- Existing public HF parent: `cff3f190e169548931fbd33eadcf1279439798e1`,
  actually read at preparation; a changed parent stops the single write attempt.
- New namespace: `qcz/embedding-optimizer-study-analysis-artifacts`, dataset,
  `corrected-dense-correctness-v3/complete-continuation-outcomes-v1/{manifest-sha}`.
- Upstream: original summary **4529**, PID **813139/start320676124**,
  source `4be265ce12cc649a595afaec9e5ef8b68a50eaa347ccb583d4d32ba3e100c5a1`,
  authority `47485b8a16af1b0be419e67c4cf45ede08cdacbd01059e42357e16c97b9eb92c`.

**Do not edit, restart or duplicate this live waiter.** An upstream failure,
source/parent drift or partial upload stops it, preserving the exact attempt.
There is no automatic retry or deletion. This is not a replacement for either
existing evaluator, native summary, paper author, combined-paper replay or GPU
resume waiter. All GPU work, lease namespaces and protected processes remain
untouched. The GitHub 403 and rejected historical HF withdrawal remain unchanged.

Fourteen separate synthetic population/path/screening/remote-preservation controls
pass (actual terminal **6764d4**, exit zero). Actual partial-input preflight
**5eb816** exits zero: fifteen present metadata/result files screen successfully,
both installed upload-mode client sources match, and the public parent is exact.
Final shared run settings are still provisional; this is not full actual outcome
admission. Preparation **d65acc** exits zero, launch **72e7ae** yields session
35024, and exact observation **6b4433** confirms the running waiter. No synthetic
native collector, real-result substitution, final release or new science is claimed.

Read-only observation (new output path required):

```bash
CUDA_VISIBLE_DEVICES='' PYTHONPATH='' /usr/bin/python -B \
  /tmp/dense-v3-factorial-outcome-durability.cq4oNAJ7/observe.py \
  --output /tmp/dense-v3-outcome-backup-status-NEW.json
```

After actual success, retain `artifact_manifest.json`, `preflight.json`,
`upload-mode-preflight.json`, original upload start/return, immutable remote
inventory and `download-verified.json`. The latter supplies the real revision,
prefix and complete recovery counts. Then publish a concrete restoration guide
and integrate durability with the full paper evidence. Do not invent those
currently unknown values or claim that this queued snapshot is already available.
