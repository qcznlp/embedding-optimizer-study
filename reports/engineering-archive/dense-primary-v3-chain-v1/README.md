# V3 core-chain integration and actual CPU rehearsal

The revised inputs now have their own primary training, checkpoint backup, BEIR admission and
whole-grid consumers. The [new protocol](protocol.json), SHA-256
`4400f1ce26ef423873a4eb509db48e046a07b90159f6d5ef10da084f5171107b`, remains a **draft**. It binds the
preceding data amendment, observed input identities and successful natural-entrypoint evidence.
It does not authorize deployment or formal execution, and is not complete downstream integration.

The v2 protocol and all its consumer sources remain unchanged. New v3 consumers share the retained
pure sealed-checkpoint, complete-run, deep optimizer-state and task-score readers. The v3 I/O
function bodies are AST-identical to v2 after declared version/class-label substitutions; transfer
operations, immutable upload commits, verification and existing-prefix refusal are retained.
The v3 training entrypoint additionally verifies the entire revised data directory before training
preparation. Both data inventories use root-relative paths for byte-preserving relocation; no data
is copied into a production namespace by this milestone.

All twelve full-horizon identities retain the numerical training implementation, untrained base,
scientific hyperparameters, five stages, fourteen tasks and statistical estimands. Only the explicitly
amended data and new namespaces differ. Validation selects minimum mean contrastive loss, with an
exact tie selecting the lower learning rate. This is the primary execution/outcome rule; the earlier
exploratory validation spec's margin tie breaker is not adopted. No BEIR-driven selection is added.
The validation scorer/selection and statistical/mechanism consumers still require v3 integration.

## Actual execution evidence

[rehearsal.json](rehearsal.json), SHA-256
`3abd941dddf1686576a17ebf8ceffc7fe9ebc68cd610b1de1e965ecd77d536c1`, is produced by a CPU-only actual
invocation at 16:45:15 UTC. It records:

- Both complete revised input directories accepted by the new v3 consumer.
- Both old input directories rejected on actual changed Arrow content, not missing directories.
- All 60 real old primary checkpoint directories rejected for missing complete new run identity;
  their existing Trainer-state markers are authenticated and remain unchanged.
- All three successful natural-data diagnostic run directories rejected as v3 primary runs because
  their actual full identities differ. Their diagnostic success is not relabelled a failure.
- The actual training, backup and BEIR entrypoints reject the draft before creating output, loading
  a model or uploading anything. These are three authority checks, not failed formal runs.
- All twelve real inspection CLI calls exit zero and reproduce the exact prepared recipe.
- The actual whole-grid CLI rejects absent v3 primary runs at its first complete-run gate. No
  historical result, synthetic fixture or short diagnostic supplies the 840-cell scientific grid.

These give **68 actual rejection cases**, twelve successful CLI inspections and two accepted real
data partitions. There is no actual complete v3 primary checkpoint/run/grid positive yet: those
models have not been trained. The copied deep readers' earlier diagnostic positives remain separate
evidence, not an actual full-horizon v3 result.

Both focused invocations pass **48 tests**; the first precedes a lint-only lambda-to-def rewrite,
and the final invocation verifies the current source. The complete current suite passes **1,710
cases**, zero failures/errors/skips. Tests cover strict version and dataset binding, unchanged
scientific rules, draft side-effect refusal, false-release refusal, exact I/O operation preservation
and all-runs-before-distinct-840-cells collection. Synthetic grid values are test fixtures only.
The separate numerical candidate's eleven unwaived old source-contract failures among 963 tests
are unchanged; this different checkout's suite does not clear them.

The final validator preserves the preceding 32 bindings through before-copies, rechecks the prior
external artifacts and new input markers/source, and verifies unchanged live numerical cores,
manuscript, main ledger and exact stopped three-handle BEIR chain. This milestone starts no GPU
worker, training, evaluation, upload or controller transition. All owned calls have exited.

## Continue from here

The **core** v3 train/backup/BEIR/whole-grid implementation and this bounded actual rehearsal are no
longer missing. Next implement and verify v3 validation scoring/selection, outcome statistics,
weight-space/dimension and publication consumers, followed by the separately routed factorial
integration and reviewed assembled-source/runtime handoff. Preserve all old locks and evidence;
do not merely update old source hashes or repeat completed data/natural/core diagnostics.

Old 12/12 executed runs and 60/60 preserved checkpoints remain on scientific hold. The three
earlier natural GPU runs and nine diagnostic checkpoints remain preserved in their explicit
temporary directory; this CPU milestone adds no new model state. Owner WIP/deployment direction,
the pending-results pre-commit rule and previous GitHub/HF access/approval boundaries remain.
Nothing is committed, pushed, uploaded or deleted here. Everything is local engineering provenance,
not a scientific optimizer finding, and belongs in no manuscript section.
