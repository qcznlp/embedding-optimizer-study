# V3 factorial training integration

Started 2026-09-07, following terminal regression 5462. The preceding turn
completed work: the owned full regression terminated with exit 0 and its XML
reports 2,962 tests, zero failures/errors/skips, and 2,953 serialized cases.
The recurring nine-element discrepancy remains explicit, not silently repaired.

## Purpose

Implement the real routed AdamW/Muon training and persistence path needed by the
crossed continuation, beginning with a named optimizer-state boundary. Retain
the frozen two source recipes at step 2345, two continuation operators, three
orders, identical 50K intact groups, fixed calibration and all outcome rules.
V3 source-state and revised-data admission must be explicit; held checkpoints
and old completion reports cannot be adopted by refreshing expected hashes.

The primary numerical policy and deep reader both reject `hybrid_adamw`. The
factory already has the desired three groups. A separate factorial reader must
bind actual parameter names and order, not merely the number or shape multiset
of optimizer states. Calibration history must not enter reset training state.

## Work and verification

1. Add explicit routed optimizer construction, named state persistence and
   load-before-mutation verification for AdamW and Muon. Preserve the primary
   factory and primary numerical/checkpoint policy unchanged.
2. Exercise actual optimizer steps, reset behavior and save/resume on CPU,
   including independent AdamW/Muon calculations and equal-shaped routing
   counterexamples. Verify the full DenseOn named topology separately from tiny
   numerical fixtures and test against the immutable corrected candidate too.
3. Integrate the actual factorial Trainer and source/calibration/data identity,
   deep whole-run admission, and then the versioned completion/publication path.
   Document exactly which of these remain incomplete at handoff; no outer
   always-failing release facade or fabricated positive receipt.

Formal GPU execution, deployment/controller migration, WIP commits/publication,
GitHub writes and HF changes remain unauthorized. No protected helper or broad
process inspection. Existing candidate/primary sources, old ledgers, manuscript,
frozen parents and completed diagnostic archives remain unchanged.
