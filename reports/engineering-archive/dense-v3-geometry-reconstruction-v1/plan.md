# Local original-geometry reconstruction

Previous goal turn: **progress**. Original-outcome acceptance is
`41eb6c7cd643ef68071914ac1f3a793f6efa4542005ff9754ed1b0f625e203b9`, observed
2026-09-07 04:39:02 UTC, with 54 bindings, 39 preserved parents and 92,423 external checks.
Both outcome cold audits and every owned regression/acceptance call are terminal. Preserve
that archive unchanged. The current four front documents and live handoff are copied in `before/`.

Implement the full twelve-run/five-stage original geometry reader on externally anchored local
roles, using the same complete run and reference metadata as vector/outcome reconstruction.
Keep the original four tables: 60 checkpoint rows, 660 run pairs, 60 optimizer summaries and
10,560 subspace-health rows. Preserve all 88 hidden matrices, both displacement kinds, original
rank-64 sketches/rank-16 bases, four CPU threads, exact-entry denominators and zero/undefined rules.

Require exact per-run descriptors, stage inventory, schema, tensor population, dtype/shape/finite
bases and consistency of recorded zero/nonzero rank health with entry counts and raw metrics.
Recompute checkpoint rows and all pair/optimizer aggregates with unchanged kernels, then compare
every original table byte. Stored raw metrics/health are authenticated primitives: never claim
they were newly measured from absent checkpoint weights or inferred from the bases alone.
No new spectrum/gap support threshold or orthogonality tolerance is introduced.

Do not call the old weight-reading `operate`/`inspect_run` as a portable no-model reader. Preserve
original authoring provenance; use only local role paths. Write fresh numerical output before
comparing stored derived values, so a rehashed wrong aggregate is refused with evidence retained.
Do not replace any of the nine original bridge features with new functional or exact-spectrum
features. The original and separate exact sensitivity families remain distinct.

Test complete schemas against existing real diagnostic records where available, without repeating
already accepted weight/SVD computations. Build a separately labeled complete synthetic 60-state,
88-matrix native-shape integration fixture, including zero displacement, same/different bases and
nonuniform run/stage values. Cold replay must use only archived package sources with original
paths/network blocked; retain negative controls and failures, independent replay and regressions.
Actual primary authoring must continue to refuse missing runs. No model, formal run, controller,
publication or protected-helper operation is part of this preparation.

Continuation 2026-09-07 05:10 UTC: the intervening user-requested status turn was
**no progress** toward scientific completion. It rechecked the exact three stopped
dispatcher handles and the archived test XML, but changed no authoritative result.
The next safe action remains this complete native-shape geometry reconstruction,
not another status-only turn, a repeated completed audit or an unauthorized launch.
The real-record primitive source is preserved in `real-record-check/source/` and
still matches its accepted nine-checkpoint readback. New integration checks below
must not be described as coverage from the earlier 2,481-case suite.
