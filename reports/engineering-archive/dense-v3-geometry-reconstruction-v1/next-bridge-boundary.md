# Inspected next surface: connect all raw reconstruction branches

This is interface preparation, not an implemented/accepted combined reconstruction.
First require terminal geometry tests, cold audit, independent replay and bounded acceptance.
Do not edit frozen kernels, omit original features or restart completed standalone diagnostics.

## One population, not three independently simulated admissions

The accepted vector, outcome and current geometry fixtures were deliberately standalone tests.
Their synthetic run metadata is not interchangeable: vector admission originally had minimal
checkpoint records; outcome fixtures upgraded complete seal/payload inventories; geometry also
varies synthetic timing. In particular, the complete-run digest enters every vector state plan.
Do not merge those trees and update only the outer archive hash into apparent compatibility.

A complete integration fixture needs one authoritative simulated twelve-run admission with
all five complete checkpoint metadata records. Rebuild the 61 vector state plans, geometry
descriptors and validation/BEIR plans from that same admission. Keep the upstream simulation
explicit. Use real native shapes and 768D vectors, not the earlier reduced-8D wiring kernel.
Standalone parent fixtures and their numerical outputs remain unchanged.

## Original bridge

`primary_v3_outcome_reconstruction.expected_outcomes(inputs)` returns a freshly rebuilt outcome
plan, evidence, ten tables and local addresses. Its reconstruction entrypoint saves a fresh
bundle and compares every original byte with `inspect_bundle`. Similarly, the geometry reader
recomputes all four tables, per-checkpoint rows and the original path-neutral geometry summary.
It authenticates the original raw metrics/health without remeasuring absent model tensors.

After those branches are verified, use the unchanged `primary_v3_bridge.bridge_tables` on the
fresh checkpoint geometry, run-pair overlaps and outcome `run_stage_scores`. Preserve every
original feature, exactly as `FEATURES` defines it: nine summaries, 36 fold rows, 540 prediction
rows and nine residual-association rows. The five-feature exact-spectrum sensitivity remains
a separately named family; it does not replace the original bridge.

The original bridge evidence contains source bindings for twelve outcome files, five geometry
top-level files and 132 raw geometry files. Preserve the original path strings. Resolve local
files by explicit roles and validated provenance subtrees, never original-path access, digest-
only matching or ambiguous basename fallback. Rebuild numerical diagnostics and the complete
bridge output plan, save fresh output before comparing stored tables, and preserve exact
rank/undefined rules. Do not call the old model-reading `gather` as a portable no-model reader.

## Functional inference and rendering

`primary_v3_dimension_inference.gather` originally verifies the entire original bridge, then
the vector/feature reader. The portable replacement must do the equivalent numerical work on
local archived inputs, not trust its stored `dimension_feature_reader` or `decisions` summary.
The raw-vector entrypoint already reconstructs complete full-width features; use that admitted
result rather than inventing an alternate coordinate calculation.

Call the unchanged `dimension_inference.summarize` with freshly rebuilt feature tables and the
original bridge panel. Retain the complete native nine-contrast task family, 27 rotation contrasts,
68 figure points, four genuinely named functional predictors, all dose/stage rows and declared
undefined outputs. The original bridge's nine features must still be reported independently.
Recompute `dimension_inference_render.render`, the full evidence object and inference plan, and
compare every table and rendered byte. Never insert synthetic output into manuscript includes.

The current inference evidence combines original bridge, vector and feature source bindings.
Keep these provenance bytes intact while validating their exact local-role mapping. Its
`original_bridge_reader`, `original_bridge_evidence`, `dimension_feature_reader`, decisions and
rendered LaTeX must agree with fresh computations, not only with updated file hashes.

Missing real primary runs must still fail actual authoring before later inputs/output. A fully
rehashed synthetic inconsistency must fail its numerical/semantic check, not merely stale file
identity. Same-host source isolation is not physical cross-host execution. Combined numerical
replay is still separate from checkpoint-backed publication, reviewed source/runtime release,
routed factorial integration, authorized formal training and a finished scientific paper.
