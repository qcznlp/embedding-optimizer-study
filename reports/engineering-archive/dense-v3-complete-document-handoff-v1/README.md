# Complete document handoff and all continuation probes

Dated observation: **2026-09-12 20:38 UTC**, not a heartbeat. This is operational
evidence; the full NAACL paper and source release are not complete.

## Actual progress

All **60/60 continuation checkpoint probes** completed. Both original coordinators
returned actual exit zero: tool sessions **54795 / 34578**, terminal records
**066902 / 992eba**. The [original complete probe records](complete-probes) retain
both full pool completions, all sixty requests, starts, actual zero exits and native
verification receipts. The pretrained reference is authenticated reuse, not a new
encoding. Do not poll or restart these completed handles.

Their two released GPU slots have automatically joined the existing exact BEIR
queues. At 20:38:11 UTC, all **eight registered ClimateFEVER workers** are live/R;
the two SciFact pilots passed, **2/168** tasks are complete, and neither pool has a
failure receipt. Existing inference and GPU-resume waiters remain active. All
12 primary runs and 12 continuations / all 120 checkpoint backups remain complete.

## What was added

The new [authoring entry](actual/author.py) reuses the genuinely completed primary
native assembly, loads the real primary/publication/exact contract objects, and
rechecks the twelve whole-run and sixty model/seal joins. It generates only the
seven current constants. All native SVD/encoding/coordinate work remains reused
through completed evidence, not repeated. Preparation bound **151 current inputs**;
actual preparation **53764** returned exit zero (terminal **c6c21b**).

The [complete-document component](actual/document_component.py) explicitly binds
four result includes, three external vector PDFs, four figure captions and nine
table captions, including every exact and exploratory sensitivity table. It
preserves the original no-pending/implementation-narrative checks, section order,
conservative and actually expanded 200-word abstract limits, eight-page main-text
limit, all original float placements, embedded non-Type-3 fonts, and fresh compiler
dependency recording. Old narrower document/guard functions are unchanged; their
previous failures are not relabelled as passes.

Final controls pass **24 test methods**, including all 27 synthetic sign branches
of the unchanged factorial formatter. Two separate compiler/layout controls also
pass: twelve total pages, main ending on page eight, 161-word expanded abstract,
and 25 embedded non-Type-3 font records for each. These controls use **synthetic
factorial cells**, are explicitly isolated under
`actual/layout-controls-NOT-SCIENTIFIC-RESULTS`, and cannot enter the authoring
consumer or manuscript. They are not new experimental findings or a completed
scientific paper. The actual current pending draft is correctly rejected.

## Active handoff — do not launch again

The only new live waiter is **tool 65801**, PID **850717**, start tick
**321699320**. At 20:38:11 UTC it is live/S, with no failure and no actual paper
output started. Its CPU-only entry is:

`/tmp/dense-v3-document-integration.Xz1qvTME/author.py`

| Binding | SHA-256 |
| --- | --- |
| Live author source | `cac9486b6e92acedb190bad67e34a5fd647d1c10a691071483fbf001406b1f9d` |
| Complete-document component | `dbed8e2d4d418ce89d5d56d75da4e433e9b9b0888194a9c44ca053dd03d2684b` |
| Owner/content-bound authorization | `ca7a0503b9324d51ce62a9c19cd1ec380e06311a0ff498b84ea30e683670e0e4` |
| Final control receipt | `882b028f78a83471fc7cade91e1dfaf7eeacc2a3d2e496bda649ade15aad1437` |

Read-only exact-handle observation, with a new output filename each time:

```bash
CUDA_VISIBLE_DEVICES='' /usr/bin/python -B \
  /tmp/dense-v3-document-integration.Xz1qvTME/observe.py \
  --output /tmp/dense-v3-document-observation-NEW.json
```

It waits behind the already live outcome inference **4529**. After all 168 BEIR
units and 60 probes have passed its original native collectors, the new entry
requires the actual complete collector chain, replays the unchanged six-table
numerics and independent arithmetic, and renders all three estimands with the
unchanged narrowed interpretation policy. It then generates and freshly compiles
the complete paper under a separate `actual-complete-paper` directory. It cannot
substitute these synthetic tests, a historical summary, or edited result digests.
Failures are preserved, never automatically retried with changed tolerances.

No authoritative manuscript, frozen numerical/dispatch source, protected helper,
lease, stopped controller, Git commit or remote source was changed. The live
entry and its bound files must not be edited or restarted.

## Remaining real work

1. Finish the existing full BEIR panel; the completed probe vectors/metrics also
   need their new durable data-only backup, without repeating older complete backups.
2. Let the original native inference and this current-document consumer finish;
   inspect actual cells/contrasts and visually review the real resulting PDF.
3. Finish queued genuine GPU-resume checks, full current-paper copied-source/data
   reconstruction, tracking/source audits and the existing release requirements.
4. Install final manuscript artifacts only after complete actual evidence; preserve
   pending status for unfinished release requirements. The existing GitHub 403
   prohibition is unchanged: no retry or alternate credential.

The archive contains **328 verified copies / 2,448,045 bytes**, manifest
`60dd7bb39021af1dc7790851eb18405766f759f9cd9a6e0113918ad3004e84c4`.
Its [tool responses](actual/tool-responses.json) retain actual terminal outcomes.
`before` preserves the four previous handoff files. Later dated observations and
this README are additional records, not edits to the immutable archive manifest.
The archive is not a full portable scientific source/data closure or public release.
