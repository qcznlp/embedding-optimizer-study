# Source-authenticated portable reconstruction

Previous goal turn: **progress**. Functional inference acceptance is
`ad04bba6f2bec212a35ad6aee6a71aa725bec1f7673a2b524d9fb7cb11e7989b`; its 64 new tests, independent
statistics/exact predictions and synthetic replay are complete. Preserve its bound sources/docs.

Current inspection identifies location coupling below the outer inference bundle:
`primary_v3_io.evaluation_plan` stores `results_root`; `inspect_evaluation` compares that exact plan;
validation source receipts and original/functional bridge snapshots carry absolute paths. Merely
copying a summary or removing the path field is not full source-authenticated reconstruction.

Keep two explicit stages. Authoring must run the existing complete checkpoint-backed consumers
on their original valid inputs before selecting archive files. Reconstruction must authenticate
the externally trusted archive hash, use only declared local roles, and never read a recorded
producer location. Original files and their digests are preserved; locations are not edited into
false original provenance. A reconstruction is not re-training, re-encoding or a new primary result.

The intended closure includes all 61 raw-vector states and per-state provenance, every dimension
feature/attribution, the complete original geometry/outcome/bridge evidence, saved validation
scores and task-result inputs, and required source/protocol files. No recursive whole-workspace
copy, raw training/probe text, model payload upload, credential access or remote write. Separately
archived checkpoint payloads remain necessary to repeat encoding and weight-derived measurements.

Recompute the literal dimension kernel and exact genuine four-feature bridge from the closed
inputs; retain all rates/stages, undefined cases and frozen inferential rules. Numerical/file
reconstruction and authoring acceptance are distinct from final paper installation/release.
The clean relocated reader must reject missing/corrupted/rehashed inputs without old-host fallback.

Use new local files/output directories and the pinned CPU interpreter. Formal execution, source
publication and all controller transitions remain gated; the protected helper is untouched.
