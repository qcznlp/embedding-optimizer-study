# Portable literal-vector reconstruction

Previous goal turn: **progress**, accepted by source/transport receipt
`bc18daa331507bc95481f7e99430157906d1c8ed4e3eebb5848915f872f283f0`.
Its exact files and all earlier failed/accepted evidence remain immutable.

Implement the numerical reader on the existing externally anchored local-role payload. Load the
actual archived v3 contracts and verify the running package bytes. Authenticate the original
checkpoint-backed authoring metadata without opening its producer paths. Reconstruct all 61
vector plans from the frozen state population, original checked-run metadata and fixed probe
row identities. Authenticate all four raw FP32 arrays and retained loading observations, explicitly
without claiming to repeat model encoding, inspect checkpoint tensors or re-read raw probe text.

Use the unchanged literal FP64 deletion kernel, all 768 coordinates, all 80 masks and all three
endpoint rotations. Compare every per-state table/FP64 attribution and every aggregate exactly.
Write only a new, separate reconstruction receipt; no manuscript output or formal admission.
Keep source/version compatibility distinct from numerical equality: do not introduce tolerances
or allow producer-location fallback. Preserve failure evidence and partial numerical outputs.

Exercise complete synthetic wiring, and separately use a full 768D numerical reconstruction
control without the earlier reduced-dimensional fixture. Clearly label simulated checkpoint
authoring and never count these controls as primary runs/vectors. Actual missing primary inputs
must continue to fail at the existing authoring entry point. Re-run source mutations and fresh
relocated-process reconstruction under the pinned CPU interpreter.

This closes the raw-vector-to-feature portion of the larger portable analysis. Original outcome/
geometry primitive reconstruction, original/functional inference and final paper publication
remain separate required work; do not claim this milestone finishes them. Existing scientific
protocols, training source, manuscript, stopped study handles and external services remain untouched.
