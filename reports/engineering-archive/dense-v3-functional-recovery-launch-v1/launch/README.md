# Functional analysis recovery — genuine execution, 2026-09-12

The direct owner message “你有权做一切事情，目标是尽快完成任务” answers the
new-directory recovery question. `owner-approval.json` records its narrow use here.
The original failed attempt, accepted vectors and all numerical sources remain
unchanged. This is not a committed-source or scientific publication release.

Nineteen focused tests passed at 14:01:54 UTC. These include eleven AST-identical
operational bodies, native lease-check ordering, complete production coordinator /
feature / observer composition with explicit stubs, and failure preservation.
They are not GPU experiments. An initial fixture coordinator-ID mismatch is
preserved in `/tmp/dense-v3-functional-production-candidate.krPeuQIL`.

Actual native preparation succeeded: session 86421, terminal `84f65b`, exit 0.
It authenticated the unchanged 61-state population and complete original input
proofs before writing the new authority. Source SHA:
`f0d732610f715b6463e92a9acc1c6c3e9fc414d981c5384ae7ab0aca3e491040`.
Authority SHA:
`50414c3541ea3031c4388c9b21274b33faf0676ce74abd44d3ccd1bfea398122`.

The real coordinator started at **2026-09-12 14:04:07 UTC**, session **27585**,
PID **766302**, start ticks **319358599**. Its immutable started receipt SHA:
`6d6c33eccc9b0987e165c6e1d3519e266907e4197c51d62472657ab7aac18a1c`.
The first new worker, AdamW 1e-5 / step 782, was actually launched on token 0
with both inherited descriptors 8 and 9: PID 766391 / start 319359939.
These are dated launch facts, not a perpetual assertion of liveness.

Only observe this new source-authorized chain:

```bash
CUDA_VISIBLE_DEVICES='' /usr/bin/python -B \
  /root/embedding-optimizer-v3-experiment/launch/functional-dimensions-recovery-v1/observe_recovery.py \
  --source-sha f0d732610f715b6463e92a9acc1c6c3e9fc414d981c5384ae7ab0aca3e491040 \
  --authorization-sha 50414c3541ea3031c4388c9b21274b33faf0676ce74abd44d3ccd1bfea398122 \
  --started-sha 6d6c33eccc9b0987e165c6e1d3519e266907e4197c51d62472657ab7aac18a1c
```

Do not relaunch or edit this deployed source while its campaign is active.
Missing handles require terminal-record reconciliation, not automatic restart.
Failures preserve outputs. No GPU/process enumeration or protected-helper access.

The campaign copies the single previously accepted pretrained state with two
native checkpoint-backed inspections, then encodes all remaining 60 checkpoints
serially under the original dual leases. The complete vector matrix is natively
read back before a CPU worker calculates and independently recomputes all 61
functional feature states. Progress counts actual native verified receipts;
neither directory existence nor synthetic fixtures count as completion.

The new output root is
`/root/embedding-optimizer-v3-experiment/analyses/dense-primary-v3-functional-dimensions-recovery-v1`.
Existing source contracts and formal publication guards remain unchanged. The
remaining downstream work is the complete functional inference, crossed
continuation, durable data recovery and paper/source release.
