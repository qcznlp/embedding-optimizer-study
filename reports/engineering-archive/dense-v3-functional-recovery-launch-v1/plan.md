# Production recovery-entry candidate, not an authorized launch

The accepted original primary results, functional numerical definitions and failed
attempt stay untouched. The previous full-flow fixture and actual native copied-
pretrained inspection are complete and will not be repeated as missing work.

Prepare a new production entry in this isolated source directory, preserving
original numerical/worker/finalizer/feature bodies and the original priority/dual
lease rules. Bind the new entry, unchanged original dependencies, exact new
namespace, full 61-state order, accepted pretrained origin and existing genuine
native-copy proof into a proposal. A proposal is not execution authority.

The entry must refuse installation in the original attempt, reject absent owner
approval, retain the old false release/scientific flags, and require its tested
source/approval/proposal bindings before creating a new authorization. No such
approval/authorization will be created in this turn. Test production refusals and
operational composition in isolated fixtures, with all model/process/lease calls
explicitly mocked. Do not invoke original or new preparation/coordinate/worker
modes on the real experiment, install the candidate, or inspect GPU processes.

Actual execution still requires an explicit owner response, reviewed source-bound
installation, native input authentication, exact launched handles and original
dual leases. Component/source tests cannot waive those requirements.
