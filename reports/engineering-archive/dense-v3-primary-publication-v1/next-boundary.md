# Remaining complete primary publication boundary

The new authoring/preparation entry, full synthetic generation, and complete
synthetic layout are implemented. The full regression is terminal/pass at 2,636
cases. Finish preservation acceptance, then the integrations below; do not repeat
accepted joint reconstruction as missing.

Code inspected after this implementation:

- primary_v3_publication.gather calls checkpoint-backed functional gather first,
  refuses simulation markers, then authenticates the saved inference bundle and
  reconstructs seven result files. Its manifest is complete_preparation, not a
  source/runtime release or manuscript-publication receipt.
- primary_v3_reconstruction_sources.source_selection accepts the functional
  contract shape and selects its loaded parents. Although it copies package
  modules, it does not automatically select this new publication protocol,
  publication result files or its source/authoring metadata.
- ReconstructionInput.load requires the original functional authoring metadata,
  original evidence digest and runtime. running_sources checks every imported
  package module against the archived source map. These checks must not be
  bypassed to mix old and new closures or use an installed/live package fallback.
- paper_audit._primary_publication_status still calls corrected_publication's
  old canonical report consumer. The dimension publication status also uses the
  old publication/portable contract. A populated new include cannot satisfy
  either gate; no audit integration or compatibility hash replacement occurred.

Required next work is checkpoint-backed publication selection plus complete
externally anchored local-role reconstruction of both exact inference text and
the manuscript-specific includes, followed by strict manuscript-consumer wiring.
Preserve the complete existing raw reconstruction and its import/source closure.
Keep manuscript installation and external release separately gated. Do not
rewrite historical records into the new scope or accept synthetic metadata as
real primary authoring. The separately declared exact-feature sensitivity and
routed factorial control remain separate required surfaces.

This note is not a released source transition or permission to commit, deploy,
resume controllers, train, publish or erase HF history. Formal accepted v3 runs
remain 0/12, and the outstanding owner/access decisions remain unchanged.
