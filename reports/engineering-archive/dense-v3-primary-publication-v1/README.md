# Complete v3 evidence authoring — preparation, not publication acceptance

Updated 2026-09-07 09:28 UTC. New package code connects the real v3 outcome,
geometry, original bridge and functional inference formats. It does not install
manuscript files or authorize training, source release or external publication.

The joint parent is accepted at SHA-256
`e6fd1dd99d14d8eec86937f63352a7e5b75083c4ef519d2b07fd56e8b8e85073`.
Both complete joint audits and its preservation checker are terminal/pass. Their
bounded scope remains synthetic same-host raw-to-inference reconstruction.

## Implemented

- The public build/inspect entry calls complete checkpoint-backed functional
  admission before any later publication input or output. Actual missing runs
  and explicit simulated admissions are rejected before output creation.
- Generation recomputes all ten outcome tables and original bridge joins/fits.
  It retains three primary and three secondary contrasts, all rates/stages,
  the exact validation selector, explicit per-run-maximum system sizes and all
  nine geometry predictors, including undefined comparisons and exact decisions.
- Seven result files preserve all structured tables, findings, primary LaTeX,
  literal functional inference LaTeX and its manuscript-specific version. The
  latter adds only the missing label to the actual co-primary dimension table.
  No standalone article or implementation-incident narrative is generated.
- Exact byte reconstruction rejects altered outputs even after rehashing. The
  preparation manifest remains distinct from primary/publication acceptance.

## Evidence and preserved failures

The first 50 tests passed. A subsequent full CSV roundtrip check found three
failures in four valid bridge cases: integer-valued exact rational strings had
been decoded as numbers. The new reader now preserves those declared wire types;
the old decoder and all numerical kernels are unchanged. The failed test source
and XML remain in exact-csv-attempt-1/. The first retry passes 57 tests.

Full generation from the single accepted joint synthetic population passes both
before and after that fix, with all six then-existing outputs identical. The
first complete-paper layout compile produced a PDF but failed the required
dimension-table label check, not the page budget. Exact pre-change source,
logs/PDF and literal functional text remain in layout-attempt-1/.

The new label-only adapter retains every original inference byte separately.
The final focused suite **87580 is terminal/pass: 63 tests**, zero failures,
errors or skips. Complete generation **81259 is terminal/pass**; all earlier
scientific values/text are retained, with only the declared table label added
to the manuscript-specific functional include. Full-layout retry **96597 also
exits zero**: main text ends on page 7, the complete expected float topology
passes, the conservative abstract budget is 178/200 words, and no overfull box
or Type 3 font is present. Pages 4, 5 and 9 were visually inspected: the primary
table, four-panel figure and complete appendix tables are readable and unclipped.
This verifies one complete synthetic layout, not the eventual real-results paper.

The newly generated canonical draft is
configs/dense_primary_v3_publication_protocol.json, 12,180 bytes, SHA-256
`39c8740962fbfa57770bc21058bfc18b3f862cf97f7b12f48ad29dcd04b43f81`.
Its preparation and source checks pass; all execution/installation flags remain
false. Full development regression **44536 is terminal/pass: 2,636 cases**, with
zero failures/errors/skips. Its complete JUnit record is retained as full-final.xml.
Follow RUNNING.md; no call in that record remains live.

## Remaining

The 09:14 preservation precheck passed all 54 direct joint-parent bindings and
349 generation/layout bindings, including exact historical sources. Both six-file
numerical cores and the real manuscript are unchanged. This is not a new whole-suite
or publication verdict. Use [commands.md](commands.md) for bounded reproduction and
[next-boundary.md](next-boundary.md) for the remaining source/consumer integration.

This is not a positive actual checkpoint-backed publication, a new primary
experiment, portable primary-publication reconstruction, a reviewed source/runtime
release, or strict manuscript-consumer integration. Those remain required, along
with the separately routed factorial control and real scientific results.
The latest whole-development scope is 2,636 cases. The distinct 963-case training
candidate's eleven source-contract failures remain unwaived. The separate validate.py
checks the frozen generation/layout sources, preserved failed attempts, full-suite
record, original cores and joint-parent bindings; it never reruns the accepted joint
numerical work or upgrades these synthetic checks to real publication acceptance.

Formal accepted v3 training remains 0/12. The real manuscript, six numerical
cores, historical artifacts and stopped controller chain are unchanged. All
AGENTS.md safety and authority restrictions still apply.
