# Complete training-artifact durability

2026-09-10. Add an immutable numerical/metadata-only snapshot to the owner's
already public qcz/embedding-optimizer-study-analysis-artifacts dataset. This
continues the owner's request to preserve the experiment for later analysis on
another machine, not source publication or historical-artifact deletion.

Select all 143 files from the already verified native training-observation
bundle, including all 132 original run/checkpoint JSON files and the original
all-twelve admission. Include its two independent numerical audit records,
preservation verification, the sixty-checkpoint index and a readable description.
Keep every original file byte-for-byte; add a separate transport manifest.
No WIP program source, raw training/evaluation text, pickle, credential or model
execution is in scope. Verify exact populations, content bindings and publication
content before any write; preserve all original false scientific/release flags.

The read-only remote inspection observed parent
209b4517e64ac5373db47b04ada39104e9080016, 21 root entries and the existing
corrected-dense-correctness-v3/weight-space subtree. Only the new
corrected-dense-correctness-v3/training-dynamics/<protocol>/<manifest-sha>
namespace may be added. Keep all other root entries and the existing corrected
weight subtree identical. Require the unchanged remote parent and original owner;
perform one additions-only commit and preserve a started receipt before that call.
No overwrite, deletion, retry of uncertain writes, root-card edit, or new identity.

Verify the complete new prefix anonymously at the immutable returned commit,
then actually recover every file into a fresh directory and independently check
all file identities offline. This is same-host recovery, not a physical second
host or a complete source/scientific release. Keep staging and recovery directories.
Update only the restoration guide and current handoff, preserving their originals.

Current numerical producers, all GPU workers/coordinators, resource leases,
protected helper, stopped historical controller chain, source locks and manuscript
remain untouched. No GitHub write, rejected HF erasure retry or optional training.
Full retrieval, all-twelve validation, functional analysis, genuine factorial
continuations and final NAACL/portable-source acceptance remain required.
