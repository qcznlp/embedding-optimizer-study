# Complete training-artifact recovery; original root-byte exception retained

The genuine v3 training-observation bundle now has an immutable public backup.
**All 149 files / 26,985,403 logical bytes were actually recovered anonymously**
and independently verified. The recovered native inputs also pass the existing
complete numerical auditor. This is numerical/metadata durability, not source
publication, a second-physical-host experiment or complete scientific acceptance.

## Snapshot and contents

- Dataset: `qcz/embedding-optimizer-study-analysis-artifacts`.
- Immutable commit: **3c95da08a4c817d5bcd58b5c84df777716a02ca9**.
- Parent: **209b4517e64ac5373db47b04ada39104e9080016**.
- Manifest: 38,307 bytes, SHA-256
  **9a69191e47b25eca459a08eb1a3ddb2958d0f652e94b3c089e1c5685a3cf7d6e**.
- Prefix:
  `corrected-dense-correctness-v3/training-dynamics/4400f1ce26ef423873a4eb509db48e046a07b90159f6d5ef10da084f5171107b/9a69191e47b25eca459a08eb1a3ddb2958d0f652e94b3c089e1c5685a3cf7d6e`.

| Population | Files |
| --- | ---: |
| Complete unchanged native bundle: all tables, figures, manifest and inputs | 143 |
| Original two independent numerical receipts and preservation verification | 3 |
| Exact sixty-checkpoint index | 1 |
| Readable snapshot description | 1 |
| Separate transport manifest | 1 |

All 132 native run/checkpoint JSON inputs and the complete original 19.5-MB
admission metadata are retained. Publication scanning found no raw query/document
examples, executable program bodies or credentials in the selected payloads.
No model/optimizer tensors, source files, pickle or new scores were uploaded.
All original scientific/release flags, source paths and unobserved exit codes
remain unchanged. Read [the snapshot description](ARTIFACT_README.md).

Use [training-analysis-restoration.md](../../../docs/training-analysis-restoration.md)
for an immutable download and independent offline verification. The data can be
read without W&B or a GPU; access to the full program source and complete
scientific reconstruction remain separate requirements.

## Original upload failure and separate read-only reconciliation

The upload commit returned at **14:03:45 UTC**. The original upload process then
exited **1** at the strict root-byte preservation check. Its failure is retained
in [the original log](actual/upload-first.log); its [upload receipt](actual/upload.json)
still has `durability_verified=false`. No original `remote-audit.json` was created.
The original uploader and its failed guard are unchanged. Do not rerun the one-shot
upload, forge its missing success receipt, or treat its exit as zero.

The exact difference is [preserved here](root-difference-observation.json):
`.gitattributes` grew from 2,745 to 3,237 bytes by appending two literal-path LFS
rules, one for the new `native/inputs/admission.json`, one for its new PDF figure.
All old attribute bytes remain an identical prefix. The other nineteen root
payload/card entries and the existing corrected weight-analysis subtree retain
their exact original Git identities. The original repository card is unchanged.
This is **not** universal root-byte preservation, and that original condition
remains false. No historical model/analysis content was replaced or deleted.

A [separate read-only verifier](recover_observed_commit.py) checked both immutable
remote trees and the exact downloaded `.gitattributes` bytes. It admits only those
two exact new-file paths, not wildcards, altered old rules or other metadata changes.
It repeats the original guard and requires its original failure to remain. Its
[new receipt](actual/observed-commit-verification.json), observed **14:09:44 UTC**,
verifies all 149 remote payload digests while explicitly retaining
`original_root_byte_preservation_passed=false`. No second commit or remote
mutation was made; no rule was removed and no historical erasure was retried.

The first attribute diagnostic's SHA-256 values and downloaded bytes were valid,
but its Git blob header used a literal backslash-zero. Its unvalidated Git blob
fields remain in [the original diagnostic](attribute-difference-first.json).
The separate verifier uses the existing authenticated hashing helper and matches
the actual Git blob IDs **44f7860b09dc12ab8654e8a1cb072ea1f85693bc** and
**012d079632f27fb26d636af9b4a81396d09670cd**. This was a diagnostic calculation
error, not a remote payload change. Original files and failed evidence are retained.

## Actual recovery and numerical replay

The separate [complete anonymous download](actual/download-verified.json) finished
at **14:10:47 UTC**, actual process exit **0**. Every file matches original size,
SHA-256 and remote LFS/Git digest. Logical recovered bytes are not a measurement
of network traffic or a claim about a second physical host.

At **14:12:24 UTC**, the [original independent numerical auditor](actual/recovered-native-numeric.json)
passed on the downloaded bundle: all 4,692 raw records, sixty saved log prefixes,
sixty stage summaries/timing records and twelve systems/run summaries. It checked
44,244 scalar fields using the original pure systems/timing references and exact
Fraction stage arithmetic. It used retained local reference source, not code
published by this snapshot. No model or optimizer tensors were loaded.

The recovery guide's standard-library offline block also actually ran and exited
zero, checking all 149 files without importing the uploader/project or accessing
the network. Only its destination placeholder changed. The guide's download
convenience block was syntax-checked, not counted as another transfer.
[Exact guide execution](guide-execution.json) and [raw output](actual/guide-offline-first.log)
are preserved.

The original **33** bounded transport tests pass. The subsequent combined **39**
cases pass, adding six exact attribute-append controls. These are small synthetic
guard tests, not numerical experiments or proof that the original root-byte
condition passed. Both XMLs and exact source versions are retained.

## Source and continuation

[Commands and actual exits](commands.json) keep the successful preparation/tests,
the real failed upload process, read-only diagnosis/recovery and numerical/offline
reads distinct. All owned calls in this milestone are terminal. The original
uploader is 19,606 bytes, SHA-256
**4dbfac65e1aaee5f004bd2af1d4cc4200d9d193329c1d7aaf0858d45bfbdad41**.
The read-only recovery source is 9,180 bytes, SHA-256
**abaa50a8b3e6fba64e097d258b57c27622b1d6ca0a38f37166f0f536e9a0be37**.
Keep [source-first](source-first/), the original [plan](plan.md), [before-copy](before/)
and all original receipts. No live training/evaluation numerical source changed.

The exact work root is `/tmp/dense-v3-training-artifact-backup.j8qukV`. Both complete
`staging/` and `download/` trees remain there. Do not remove either, rerun the old
upload, or use a completed handle as a live-job identifier. The
[preservation read](verification.json) binds this limited actual outcome.

At **14:15 UTC**, [exact observations](observations.json) show primary BEIR
**43 / 840**, baseline **14 / 14**, eight exact primary workers live/R, validation
**7 / 12**, functional **0 / 61**. Both latter coordinators remain live/S with no
failure receipt. The previously asked resource-priority question remains unanswered.
No GPU job, lease, protected helper, old stopped controller, manuscript or GitHub
source publication was changed.

Continue the complete existing retrieval and validation grids, functional
interventions/held-out-dose inference, separately admitted real crossed branches,
and final NAACL/portable-source work. Training-curve durability does not fill
missing retrieval results or establish a useful-dimension mechanism. No overall
goal completion, new blog or complete scientific release is claimed.
