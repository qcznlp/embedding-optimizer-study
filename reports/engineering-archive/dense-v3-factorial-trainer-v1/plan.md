# Actual factorial Trainer integration

Started 2026-09-07 after terminal observations of 21015 and 64963. The previous
status turn completed the specific owned tests and therefore yielded new evidence;
this is continued implementation, not a repeated wait or formal training approval.

Preserve all six sources and receipts in dense-v3-factorial-routing-v1. Integrate
their actual named optimizer and balanced 50K sampler into the pinned ST Trainer,
including initialized four-rank loader, inherited single-normalization path,
wrapped optimizer/scheduler and real save/load hooks. Use new CPU-only diagnostic
namespaces and an explicitly nonscientific component checkpoint scope. Do not
relabel primary/factorial historical checkpoints or simulate formal admission.

First exercise argument rejection and actual prepared dataloaders; then perform
four-rank CPU toy InfoNCE training and actual checkpoint resume. Compare consumed
row identities, model tensors, named optimizer moments and scheduler state with
uninterrupted execution. Keep the 50K/391 horizon and actual inherited training
step. Toy CPU topology is explicit and cannot satisfy default DenseOn admission.

The outer source-state/calibration/data/release binding, whole-run consumer,
actual DenseOn distributed validation and formal experiment remain required.
No controller, GPU helper, old numerical code/protocol, manuscript or external
publication action is authorized by these diagnostics. Preserve failed attempts.
