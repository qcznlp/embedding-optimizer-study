# Durable copy of the six existing endpoint contrasts

**16 / 16 files, 218,466 bytes:** remote preservation, actual anonymous recovery
and independent recovered-byte/table/figure checks pass. This milestone transports
the already completed endpoint analysis; it does not add an experiment, bootstrap
sample, scientific contrast or manuscript result.

Read the [restoration guide](../../../docs/endpoint-statistics-restoration.md) for
the immutable public revision and download instructions. The exact revision is
`13b110a0d067948ff518a0f720ee84c2e2f8c6b1`, adding only
`corrected-dense-correctness-v3/endpoint-statistics/` to the existing dataset.
The manifest SHA is
`cddee2dcfb68c25e42931c82f129c0f57590434cecf2b48bbc106da0dfc38c2e`.

## Actual execution

- The original endpoint verification authenticates fourteen selected original
  files. A standalone artifact README and manifest complete the sixteen-file
  snapshot. All six comparisons and all PDF/PNG/SVG figure formats are retained.
  No raw examples, command logs, program bodies or credential-shaped content
  are uploaded. The original native metadata's host paths are historical only.
- Both source versions pass the same eighteen focused transport tests. These
  are guard checks, not optimizer experiments or additional scientific evidence.
- The first invocation stopped at ordinary-file checking of HF's default
  metadata-cache symlink, before any upload-mode preflight, upload-start record,
  LFS upload or commit. Its exact source, preparation and exit-1 tool result are
  retained. The helper's no-symlink requirement was not weakened.
- The new source differs only by materializing parent metadata into an explicit
  task-local directory. It uses a fresh source-bound staging attempt. Its exact
  final-path preflight finds fifteen regular blobs and one PNG LFS file. The
  pre-existing PNG LFS rule suffices; no `.gitattributes` change is allowed.
- Actual upload session 89991 exits zero (terminal chunk e87071). Commit creation
  completes at **18:52:34 UTC**; the independent immutable remote inventory and
  strict root/subtree checks complete at **18:52:35**. All twenty old root
  entries and all three old corrected subtrees match their original identities.
- Actual anonymous download session 54709 exits zero (terminal chunk 146f94) at
  **18:54:17**. All sixteen downloaded files match. The ordinary unauthenticated
  rate warning did not cause a credential switch or failed transfer.
- The independent standard-library checker exits zero (chunk a401ac) at
  **18:55:02**: all bytes, forty CSV rows / 246 numeric fields, six figure rows /
  24 numeric fields and all six unit-converted interval mappings match. Both
  native readout receipts agree except their original observation timestamps.
  It does not repeat the original statistical resampling or model computation.

[commands.json](commands.json) records the actual tool calls and their outcomes.
[actual/final/remote-audit.json](actual/final/remote-audit.json),
[actual/final/download-verified.json](actual/final/download-verified.json), and
[actual/final/recovered-independent-verification.json](actual/final/recovered-independent-verification.json)
are the actual acceptance records. The original upload receipt retains its
pre-audit false flag; the separate remote audit establishes durability.

## Source and failed-attempt preservation

The original `source/backup_first.py` SHA is
`cc00cb5fd855be277b79f92add526a3c39ca4c8cc446714692a850f9fb587bf6`;
the new `source/backup.py` SHA is
`1234b243cb0028d232674ad1a261601f546aace51c37086bdc365aa576accb8e`.
The unchanged parent transport and hashing helper remain separately authenticated.
The first work root is `/tmp/dense-v3-endpoint-statistics-backup.9U6tS4tb`;
the successful root is `/tmp/dense-v3-endpoint-statistics-backup.L93O9iID`.
Do not rerun either upload against the completed namespace. The archived uploader
is host-specific execution evidence, not a portable release command.

The original endpoint archive remains byte-identical, including its source and
all numeric outputs. Its former live handoff binding is retained in
[before/CURRENT_EXPERIMENT.md](before/CURRENT_EXPERIMENT.md). The new current
handoff links this separate backup, without altering prior acceptance records.

The [dated observations](observations.json) read only the existing primary and
functional observers. No job, frozen numerical/dispatch source, lease priority,
historical controller, manuscript or protected helper was changed. The original
840-task evaluation and 61-state functional campaign continue; twelve genuine
crossed branches, full scientific/reconstruction gates, source release and the
NAACL paper remain required. No GitHub retry or historical HF deletion occurred.
This milestone is not a physical second-machine experiment or whole-goal completion.
