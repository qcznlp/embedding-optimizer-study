# Endpoint statistics: separate immutable analysis backup

The completed six-contrast endpoint readout is newer than the raw-score backup.
Preserve all of its numeric tables, native readout, relocation receipt and three
figure formats as a new additions-only snapshot in the existing public analysis
dataset. This is transport of existing measurements, not new inference or a
source/manuscript release. The 840-cell and 61-state original queues continue.

The read-only remote inspection found parent
`3883b677f87b1982f06016e9fadb8bb95e0cfc96`, with only the existing weight-space,
training-dynamics and complete-endpoint-evaluations corrected subtrees. The new
subtree is `corrected-dense-correctness-v3/endpoint-statistics`.
Source evidence is the original endpoint verification
`63865889287feab7a7eccd71f6c7f9b4601ce8bc099ae15cfe426ca0334e9911`.

Select exactly fourteen original data/provenance files plus a standalone artifact
README, followed by a content-addressed manifest. No executable source, raw text,
checkpoint, command log, credential or evolving process snapshot is uploaded.
Inspect all text and retain every contrast, including inconclusive comparisons.

The original transport primitives remain unchanged. The three-format mode probe
found regular PDF/SVG and LFS PNG. The existing immutable `.gitattributes`, SHA
`d7518c3119b61f65892c8c1b1608536862f89fcc91180c0511f1e75ce0edf7df`, already includes
`*.png filter=lfs diff=lfs merge=lfs -text`; no new root rule is expected or allowed.
Recheck modes at the final exact paths, permit only that PNG's existing LFS rule,
then perform at most one parent-bound commit. Preserve the original root entries
and all three earlier corrected subtrees byte-for-byte. An unexpected change or
failed attempt is retained, never silently retried or reinterpreted.

Actually download all files anonymously into a fresh directory and independently
verify recovered bytes and the six table/figure mappings. Do not rerun the already
completed 50,000-draw inference or claim a physical second-host experiment.
Only after recovery succeeds, add the immutable link to a restoration guide and
the current handoff, retaining its preceding bytes and the original archives.

## Retained first local transport failure

The first upload invocation (session 99143, terminal chunk 200e83, exit 1)
stopped before upload-mode preflight, `upload-started.json`, LFS upload or commit.
HF's default metadata cache returned a symlink, rejected by the unchanged ordinary-
file hash helper. The original source is preserved as `source/backup_first.py`,
SHA `cc00cb5fd855be277b79f92add526a3c39ca4c8cc446714692a850f9fb587bf6`,
with its original staging and preflight at
`/tmp/dense-v3-endpoint-statistics-backup.9U6tS4tb`.

The new version changes only that metadata download to an explicit task-local
directory. The helper, file identity, secret/source screening, expected parent,
upload modes, root preservation and one-shot mutation guards remain unchanged.
Use a new work directory and preparation receipt; do not relabel the first call
successful or reuse its source-bound staging as the new source version.
