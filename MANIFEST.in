# The Dense-only distribution surface is declared explicitly in
# ``tool.setuptools.data-files``. Retained legacy audit inputs are deliberately
# absent from that allowlist and therefore need no warning-producing ``exclude``
# directives here.
