% Replaced atomically by embed-optim-render-candidate-breadth after the post-hoc probe completes.
\newcommand{\CandidateBreadthConclusion}{\ResultPending{The source-bound nested-candidate decision
is pending. The final text will report supported, partial-attenuation, or not-supported exactly as
specified by the frozen endpoint rule, retain the post-hoc causal boundary, and supplement rather
than alter the validation-frozen three-seed comparison. It will state both Muon-family endpoint
outcomes at widths 7 and 2,048 and the corresponding bounded interpretation.}}

\newcommand{\CandidateBreadthDiscussion}{\ResultPending{The source-bound nested-candidate decision
is pending. The final discussion will replace conditional outcome language with the exact frozen
decision while retaining the post-hoc causal boundary and the independent three-seed comparison.}}

\newcommand{\CandidateBreadthFigure}{%
\begin{figure*}[t]
\centering
\fbox{\parbox[c][0.20\textheight][c]{0.84\textwidth}{\centering
\ResultPending{Post-hoc nested candidate-breadth figure pending.}}}
\caption{Post-hoc nested candidate-breadth diagnostic on 224 balanced, query-disjoint queries.
The final figure reports the frozen width-7 calibration reproductions and paired endpoint contrasts
through width 2,048; this topology-faithful placeholder reserves the appendix float.}
\label{fig:candidate-breadth}
\end{figure*}
\paragraph{Candidate-breadth uncertainty and paired prevalence.}
\ResultPending{Exact high-dose-minus-retrieval-optimal endpoint intervals and paired query win
fractions are pending. The final appendix text will report both Muon-family optimizers at widths 7
and 2,048, the first joint sign reversal on the six-point grid, and descriptive source-stratified
95\% bootstrap intervals from 50,000 resamples without changing the frozen support rule.}
}

\paragraph{Candidate-breadth diagnostic.}
\ResultPending{The source-bound 224-query nested-candidate evaluation is pending. This post-hoc
diagnostic will report the frozen decision, both Muon-family width-7-to-2,048 loss/margin point
contrasts, and the three width-7 loss/margin calibration pairs. Its uncertainty and query-level
prevalence remain in Figure~\ref{fig:candidate-breadth}, and it cannot affect the three-seed optimizer
comparison.}
